# Addendum — 2026-08-09: reference superseded

    **`PressPackagePricingTableRuns.tsx` (in this folder) is now the source of truth for the press Catalog page.** It supersedes `PressPackagePricingCatalog.tsx` for layout and copy. The punch list in PUNCHLIST-2026-08-08.md still applies; on top of it, the new reference adds:

    1. **Left rail says "Catalog"** (not "Vinyl catalog") — CDs and cassettes are coming.
    2. **Page header**: H1 "Catalog", then an Apple segmented pill **Vinyl / CD / Cassette** — CD and Cassette disabled (gray, no pointer, `title="Coming"` on hover). No FORMAT dropdown.
    3. **Two-tone heading**: "Build your vinyl catalog. From scratch." with eyebrow VINYL · PACKAGE PRICING.
    4. **No save cluster in the header.** When there are unsaved edits, a frosted floating bar appears bottom-center ("Edited · Save catalog", blur + hairline + soft shadow). Nothing shows when saved.
    5. **Run-size price list** is the vertical per-run rows (100 can be Off/On request), with "+ Add run size" — not the 3-up card grid.
    6. **Print templates use blueprint icons** — die-line SVG drawings of the piece (jacket/labels/inner/booklet; solid = edges, dashed = folds/hidden). Filled tiles show the filename + hover Replace; empty tiles ghost the icon at 55% with "Upload or paste a link". Same icons as the artist package builder.

    Rule zero unchanged: replace the page wholesale from this reference; keep live data wiring; placeholder numbers are placeholders.
    