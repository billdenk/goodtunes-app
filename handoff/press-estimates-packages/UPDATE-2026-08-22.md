# UPDATE — Aug 22, 2026 — PressPackageBuilder save section rework

Bill-approved revision of PressPackageBuilder.tsx. **Replace the presentational
code verbatim; wire data only** (handoff law applies — delete-first, no
reconciling). The file in this folder is the source, character-for-character.

## What changed (Section 7 · Save)
- Header (two-tone "The build." heading, build-summary line, "Priced at N
  units…" anchor note) now spans the FULL card width, above the two columns.
- LEFT column: the package stage at full quantity-stage size (jacket 321px,
  inner sleeve tucked at the qty-stage geometry, record peeking; hover slides).
  Vertically centered so it runs from the top of the Per-record box to the
  bottom of the Full-run-total band.
- RIGHT column widened 400 → 460px; the honest-math box (Per record expandable,
  Run, Setup expandable, Full run total gradient band) unchanged inside.
- Save moved OUT of the right column: below both columns, right-aligned to the
  card's right padding (same right margin as the math box). The quiet note
  "Packages skip quantity and price — artists pick their quantity later." sits
  ABOVE the button, right-aligned.
- Save is just save — no second naming prompt; the package name comes from the
  "How artists will see it" card ("cardName", falling back to seed title).
- Pricing anchor everywhere = the smallest VISIBLE quantity card (anchorQty),
  never a fixed minimum-run constant.

## Must work (one line per control — anything not listed is decorative)
- Every builder step control (size / discs / weight / color / label / jacket /
  sleeve / insert / sticker / quantity cards): updates live state and the math.
- "Per record" row: toggles the component-line breakdown open/closed.
- "Setup" row: toggles the setup-line breakdown open/closed.
- Quantity cards: selecting/hiding cards re-anchors pricing (anchorQty).
- "How artists will see it" name + sell-line inputs: feed the saved package.
- "Use the artist's artwork instead…" toggle: swaps house art ↔ artist art.
- Save to catalog / Save changes: persists the package to Product Specs ›
  MRP Packages and shows the saved confirmation note.
- Hover slide on both album stages: pure CSS, ships as-is.

## Acceptance
Full-page screenshot diff at 1440px (and 1024/768), BOTH themes, per the
states checklist in this folder's README. Any visual difference other than
data values is a failure.

## Also in this push — live-app sweep items (Bill, from live screenshots)
1. Size pills (Components · Pricing segmented control + any shared pill):
   order 12″/10″/7″ is now correct, but the labels are NOT vertically
   centered — they ride high in the pills. Center them (line-height/align).
   Keep the queued word-drop: no "Single/EP/LP" suffixes.
2. Components chip row (Catalog › Components pages): must sit flush left with
   the content column (same left edge as breadcrumb and H1), not the pane edge.
3. Blue-button sweep: header/toolbar actions are quiet dark-gray-outline
   pills, never filled; filled #319ED8 is reserved for the ONE earned confirm
   per screen (canon rule ratified 2026-08-18, apple-canon.md). Bill spotted
   filled-blue header actions live (e.g. Run payouts on Dashboard) — sweep
   every screen.
