<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Launch theme. On pre-Android-12 the OS shows this window background
         while the activity cold-starts: a solid navy backdrop with the
         centered "G" (drawable/splash_screen) instead of the old stretched
         full-screen splash.png, so it matches the brand and never flashes
         white. The androidx core-splashscreen compat attributes describe the
         same navy + "G" so the library renders it identically where it
         applies. On Android 12+ the native SplashScreen attributes in
         values-v31/styles.xml take over. postSplashScreenTheme hands the
         window back to the normal app theme once content is ready. The legacy
         splash.png drawables are retained as a fallback. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>