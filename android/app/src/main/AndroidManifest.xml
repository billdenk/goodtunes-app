<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links: open https://my.goodtunes.music/* in the
                 app when installed. autoVerify=true makes Play check the
                 /.well-known/assetlinks.json on the domain before honoring it.
                 We intentionally do NOT claim the bare goodtunes.music apex:
                 that host is the Webflow marketing site (it can't serve our
                 assetlinks.json), so claiming it would only produce a
                 permanent verification failure. Fan-shareable content links
                 live on the app subdomains, not the marketing homepage. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="my.goodtunes.music" />
                <!-- Match every path on the host. The web SPA's router
                     already knows what to do with unknown paths (404
                     page), so handing the whole namespace to the app
                     keeps fans in-app instead of bouncing to Chrome on
                     any deep link we haven't enumerated. -->
                <data android:pathPattern="/.*" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Task #535 — Push notifications. Permission is declared now so the
         install asks the user once, on Android 13+, even though we don't
         send payloads yet (FCM wiring is a follow-up task). On Android 12
         and below, POST_NOTIFICATIONS is implicit; declaring it is a
         no-op there. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
