// Test stub: image-asset imports (e.g. "@assets/...jpg") resolve here under
// the test tsconfig so the tsx test runner does not try to load binary
// files as modules. The actual URL is irrelevant to component logic tests.
export default "";
