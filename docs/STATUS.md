# GoodTunes — Living Status

Last updated: 2026-08-27

- **PMP record pricing seeded for Estimates (Aug 27 2026, Task #3409)**: PMP's white-label quote builder and Components → Pricing page now price the vinyl record line from Jonathan's 2026 sheet (client Price column only). The sheet's four families map onto PMP's vinyl TYPE rows — Black→black; Color→color/opaque/translucent; Mixed→splatter/splatter-4/-5/mix-swirl/splatter-2-colors/black-splatter-2-colors; Handmade→deed — matched by candidate key list because dev and prod carry different generations of PMP's color library (prod restructured Aug 26; prod has no Handmade row — honestly skipped). Seeded as per-size quantity ladders (`rungsBySize`, source-tagged `pmp-pricing-2026`) on BOTH dev and prod via the marker-guarded seed (`pmp_record_pricing_2026_v1` beside the Aug 26 `pmp_component_pricing_2026_v1`); never overwrites an operator cell or existing ladder; re-sync (`mergePricingRows`) now carries ladders + pricingSource through. Pricing page shows ladder-priced rows as "$lo–$hi · qty ladder" hints and counts them in "N of M priced" (typed cell still overrides; clearing falls back). Honest gaps stay pending: 180 g (no heavyweight sheet prices), 7" Handmade, beyond-top-rung quantities, Tri-Fold. Mapping interpretation documented in `docs/vendors/pmp.md`.

- **OCR small-text detection for outlined/raster completed art (Aug 26 2026, Task #3411)**: the minimum-text-size check now covers art the live-text parser can't see. Outlined PDFs (no font dicts) get an OCR pass — pages render at 200 DPI via pdftoppm, Tesseract (new in `replit.nix`, so it ships in the deploy image) reads word-level boxes, and glyph heights convert back to points with ascender/descender-aware em factors (`server/validators/ocrTextSize.ts`). TIFF/JPG completed-art uploads are now ACCEPTED on the check route (direct uploads only; pasted links stay PDF-only): signature-sniffed before the PDF scanner, they get a warn file-type row ("structural checks can't run on a raster") plus the same OCR text-size row, with physical scale from embedded DPI metadata or, when absent, the component's expected flat width (basis disclosed in the verdict). Contract kept: OCR findings are WARN-ONLY — below-floor text warns even when the press set `minTextPointSizeBlocking` (OCR misreads decorative type; false failures train operators to ignore the check); no min-text rule or no detectable text ⇒ rows byte-identical to before; any OCR failure degrades silently to the old advisory wording. Existing live-text check untouched. Locked in by `server/validators/ocrTextSize.test.ts` (pure parsing/conversion + real ImageMagick→Tesseract fixtures at known point sizes, skipped where the toolchain is absent).

- **Cross-press project import — wired, held OFF (Aug 26 2026, Task #3394)**: a customer can bring their own project SPECS from one press to another — everything ships behind flags that stay OFF (per-press `manufacturers.cross_press_import_enabled` operator toggle on the press page; GoodTunes "My projects" behind compile-time `CROSS_PRESS_MY_PROJECTS_ENABLED=false` in `shared/crossPressImport.ts`, so the route never even registers). Walls (Bill's ruling): customer-initiated only — zero events/notifications/report rows to either press, the source press's data is byte-untouched by an import; **specs travel, never commerce** — allowlist serializers + a `findForbiddenPriceKeys` deep-scan firewall on every response (a price key 500s loudly); the other press is **never named** in press-portal copy or persisted draft payloads ("saved project specs on your account"); translation is honest — exact / ranked closest-match the customer must confirm / plain "no equivalent", never a silent swap or fabricated price. Canonical spec dictionary: `canonical_attrs` jsonb on press color tiers/colors/jackets (effect family / color family / jacket construction), seeded from name heuristics on dev+prod (`scripts/seed-canonical-spec-tags.ts`), operator-reviewable at `/admin/manufacturers/:id/spec-mappings` (platform-staff-only routes). Customer flow: one-time dismissible entry card on the white-label project home → `/projects/import` wizard (list → per-field match confirmation → pre-filled Draft estimate at the destination press). Masters-release request rides to the SOURCE press as a normal inbound request from its own customer (table has no destination column by design; `release_fee_cents` exists as a clearly-marked stub — charging is an open question for Bill, nothing bills it). Ruby brief: `docs/handoff-briefs/cross-press-import-ruby.md`; surfaces render plain until her handoff. Locked in by `shared/crossPressImport.test.ts` + `server/crossPressImport.db.test.ts` (flag-off = zero surfaces, price-free payloads, no source-press identity, source press untouched). Launch is a separate deliberate decision — do NOT flip flags without Bill.

- **Inbound pricing push API for MRP's ERP — Matilda (Aug 25 2026, Task #3379)**: MRP's bespoke ERP (Matilda Tech) has no public API to pull from, so the integration inverts: Matilda POSTs pricing JSON to us at `/api/erp/v1/pricing/validate` (pure dry-run, echoes parsed rows + structured per-row errors) and `/api/erp/v1/pricing/pushes` (strict — any error rejects the whole payload). A real push lands as a **pending** run in the shared sync history (`press_pricing_syncs`, source `erp_push`) that an operator previews as a diff and commits — the exact Coda preview→commit safety model, so a push can never silently rewrite live pricing; operator-locked rungs survive commits. Auth is a per-press API key (`gtpush_…`, `X-API-Key` header) minted/revoked operator-only on the manufacturer page (one-time reveal, secret envelope-encrypted in the new `press_push_credentials` table, constant-time verify, rate-limited). The manufacturer page (Overview tab, beside the Coda card) gets the key management, "pricing last received" freshness line, and pending-push review UI. The shareable spec for Matilda's developer is `docs/matilda-integration-kit.md` (schema, curl examples, error catalog — no internals). v1 is default-jacket pricing ladders only; the `/api/erp/v1/` family leaves room for orders/inventory later. Tables created on dev + prod; post-merge block added. Needs prod publish.

- **Vinyl generator center-label logo notch alignment (Aug 24 2026, handoff 3dd5929)**: the MRP mark on the black label is nudged UP so the natural notch at the skyline base (the arc origin, y=143.3 of the 272.4 viewBox ≈ 52.6%) lands exactly on the spindle hole — the hole sits in the dip, not the buildings. Derived from the logo's own geometry so it scales with every disc size (stage, type cards, search rows all ride the shared `DiscLabelArt`); the black label itself and the catalog arc text are untouched. Theme-independent (white mark on black label both themes). Needs prod publish.
- **Vinyl "Add your vinyl" quantities & weights are now per-size (Aug 24 2026, gogoods' bug)**: on the press Components → Vinyl setup, toggling an LP-count or weight (or adding a custom weight) changed EVERY size because one shared offered list backed all three record sizes. Quantities and weights now key off the picked size (`quantitiesBySize`/`weightsBySize` in the vinyl component blob, optional in the schema): legacy saves keep behaving as one shared list until a press edits a specific size, at which point every size's current state is frozen into its own list and only the picked size changes. A newly added custom weight joins the ladder but is offered only on the size it was added under. Sizes themselves ("offered by" on 7"/10"/12") stay global as before. Needs prod publish.

- **Artist rail nav canon ratified + Team invite dialog (Aug 24 2026, Bill & Andrew run-sheet 3363c35)**: the artist portal rail is now the canon order everywhere — Dashboard, Releases, Audience, Acquisition, Orders, Buyers, Referrals, Shopify, Reports (no Overview, no People, no Catalog group). Audience/Acquisition/Buyers moved OUT of the Reports hub to their own top-level rail items ("one home per thing", each with its own heading + range switcher); the Reports hub slimmed to the Payments/Earnings ledgers only (stale `?tab=reports&rtab=audience…` deep links canonicalize to the top-level tab). GoodTunes registry drives it (`registry.ts` artist rows), and the MRP white-label rails (`ArtistDashboardMRP`, `ArtistDashboardNextStepsMRP`, `ArtistProjectHomeMRP`) match structurally — Projects renamed Releases (breadcrumb "RELEASES ›"; body copy about pressing keeps "project"), People/Overview dropped, Team pinned at the rail bottom (MRP skin has no Settings page; GoodTunes keeps Settings pinned with Team inside it). Settings › Team "Invite someone" is live: dialog with Bill's verbatim copy ("Invite your manager, bandmates, or anyone who helps run your releases. They'll get their own sign-in, and you stay in control."), name/email, and a role picker — Admin → inviteRole `team`, View → new `team_view` tier (whitelisted through `/api/artist/invites` + the shared handler; lands as membership `sub_role` at accept). Success confirms the email or falls back to a copy link; review-held invites say so honestly. Needs prod publish.

- **Package builder quantity curation + save section ported to the Aug-22 handoff (Aug 24 2026)**: the live `PressPackageBuilder.tsx` regained both regressions vs `handoff/press-estimates-packages/PressPackageBuilder.tsx`. Quantity grid: every tier card carries a quiet hover ··· menu (Hide from artists / show again, remove for custom tiers, last-visible guard), Hidden chip is word+icon, a dashed "Add a quantity" card mints custom tiers priced by honest interpolation between the real tier anchors (never a made-up flat number). All pricing surfaces (header strip, stats band, artist card "From …", math box, saved `minRun`) re-anchor on the smallest VISIBLE offerable quantity. Save section: two-tone "The build. Everything you picked." heading, package visual stage (jacket+sleeve+disc with press logo), math box ("This exact build, at N units", one-time setup, full-run total), MINIMUM RUN segmented picker deleted, Save button below the math box; all live persistence (canEdit gate, PUT/POST, naming, saved state) kept. The ··· trigger uses the IconButton primitive; design-lint baseline re-snapshotted for the handoff-verbatim sizes. Needs prod publish.

- Releases wall "New Release" pill (Bill & Andrew run-sheet cc260e2, 2026-08-24): quiet outline pill (Plus + "New Release") top-right of the "Releases." heading row in the artist portal, visible for every artist including an empty catalog (empty state copy points at it). Opens the name + format walk → creates the prepping draft under the artist's own scope (existing scope-forced POST /api/admin/albums) → lands on the project page; wall query invalidated so the draft shows as "In preview". The handoff mock's solid-blue pill and mock-only compare chooser were superseded/dropped per the run-sheet. Both themes.
- **White-label domains LIVE end-to-end** (Aug 21 2026, Task #3279): makesvinyl.com + pressesvinyl.com apexes serve production with valid TLS (200, neutral page, no 301 to goodtunes.music); wildcard CNAME added in GoDaddy so any slug resolves. Replit issues no wildcard certs, so each press subdomain must be individually linked in the Replit Domains panel (one-time, ~1 min, zero GoDaddy edits thanks to the wildcard). Live now: mrp/memphis on both domains. Memphis Record Pressing assigned slug `memphis` — branded landing, skinned login, and estimate `/e/:token` viewer all verified on memphis.makesvinyl.com; unassigned linked subdomains show the neutral page.

This file is the current-state summary for the design studio. It is kept
current, not appended to: stale lines are overwritten when things change.

## 1. Recent changes

- **/investor-update standalone page (Aug 24 2026, gogoods)**: Bill's externally-built GoodTunes Ecosystem deck now serves at `get.goodtunes.music/investor-update` — same pattern as `/investors` (file at `server/assets/investor-update.html`, streamed no-store, X-Robots-Tag + injected noindex meta, unlinked, every host). Live in dev; **needs a publish to reach prod**.

- **Create → Packages rail entry + package builder loses the estimate math (Aug 24 2026, gogoods walk-through)**: the press rail's Create group now carries BOTH builders — Estimates and Packages (opens `/vendor?tab=packages&view=builder`; registry id `packageBuilder`, aliased in `usePressPortalNav`). Create = where you build; Product Specs › «Press» Packages stays the saved-builds index; rail highlight and god-view breadcrumb distinguish the two. The package builder's "The build." card and top strip drop ALL run math — no Run row, no Setup, no Full run total; only the specs line, the per-record component roll-up, and the "From $X.XX /unit at {min} units" anchor remain, so the "Packages skip quantity and price" caption and the UI finally agree. Estimate builder untouched. God view (AdminManufacturer) mirrors both changes. Needs a publish.
- **Copy rule (Bill/gogoods, for Ruby too)**: the possessive of Memphis is **"Memphis'"** — apostrophe only, no trailing s. Audited user-facing copy: no "Memphis's" currently ships (all hits are code comments).

- **PMP component pricing seeded from the 2026 sheet (Aug 26 2026, gogoods)**: `scripts/seed-pmp-component-pricing.ts` (marker `pmp_component_pricing_2026_v1`, run on dev+prod, wired into post-merge for fresh clones) loads Jonathan's client prices: component ladders (jackets incl. wide-spine/gatefold/7", inserts, printed sleeves, 7" center labels, download cards, booklets 8–24pp, stickers), 36 flat-rate service items (sleeves, poly bags, shrink wrap, 12" center-label runs, setup fees, lacquer/plating, test presses, labor, surcharges, PMP XTP), and 20 component→price links (single jacket links to PMP's own jacket ladder — records and jackets are priced separately at PMP, unlike MRP's all-in tiers). Deliberately NOT seeded pending gogoods: record pricing (Black/Color/Mixed/Handmade ↔ catalog swatch-family mapping is ambiguous; the brief's "Splatter = Color × 1.41" doesn't match the sheet's Mixed prices) and setup-fee engine rules (Color Setup $300/$200/$150 flat-vs-per-color semantics unconfirmed — the 3 lines are seeded verbatim as service items). Block semantics reminder (docs/vendors/pmp.md): PMP price breaks belong to the NAMED block, not the typed quantity — estimate-builder UX must keep block choice explicit.
- **Package builder "Save to catalog": no second naming prompt + no name, no save (Aug 26 2026, gogoods addendum / Bill's rule; handoff update `37a282f`)**: the footer's inline "Name this package" input is gone — the package is named ONLY in the "How artists will see it" section (Package name + Sell line + Cover). Until that field has content, Save to catalog rests as a quiet outline pill (subink text, hairline, transparent) and does nothing, with the helper line reading "Name your package above — the name is what artists see."; it fills blue the moment a real name is typed. The "Untitled package" save fallback is dead (the card PREVIEW keeps the placeholder text per the mock). Same button gates Save changes on reopened rows. Corrections 1–2 (strip clipping + Type collapse) are task #3402, not this change. Needs a publish (missed tonight's).
- **Help & feedback dialog rebuilt from Ruby's handoff (Aug 26 2026)**: the partner-portal Feedback dialog (shared `FeedbackLauncher` in OperatorShell, every portal) now wears the apple-canon dress from `handoff/help-feedback/` — two-tone heading, New report / My requests segmented control on its own row, rounded-full Bug/Feature pills, gray-circle X, quiet Cancel, Send earns blue only once a title is typed (details now optional). The auto-screenshot is VISIBLE: captured when the dialog opens, shown as an attachment card with a live thumbnail + Remove/Include toggle; clicking it opens the preview sheet where partners drag-to-highlight (numbered blue pins, % coords stored in new `partner_feedback.highlights` jsonb — dev migrated, prod picks it up at publish via post-merge/schema diff) that travel with the report and render as pins on the operator triage screenshot (AdminFeedback). My-requests rows use word+icon statuses (✓ Received / clock In review / ✓ Shipped / Closed); empty state is the Ruby-sanctioned quiet card line. Kept beyond the mock (flagged to Ruby): operator `publicReply` shows as a quiet sub-line in the row — the old dialog surfaced replies and dropping them would lose real function. Needs a publish.
- **Template/art color check is now RGB-usage aware (Aug 26 2026, gogoods)**: dieline swatches saved in the palette as RGB, and dielines drawn on hidden or "does not print" layers (PDF optional-content groups with default-off state or /PrintState /OFF), no longer fail the completed-file color check or the press art-inspect banner. The scanner now proves RGB usage the same way it proves spot usage — painted `rg/RG` operators (layer-aware), RGB colorspace selections, or RGB image XObjects count; palette-only definitions don't. Anything unresolvable stays fail-closed (legacy behavior), old stored scans unchanged, placed RGB raster art still fails. 12 new tests.
- **Parked (cancelled with the #3387 card, never built — re-queue when wanted)**: (1) portal UI for a press to configure its own setup-fee rules (today they're operator-seeded only); (2) pre-fill the splatter color count from the chosen color's recipe so estimates start right.
- **Art preflight missing-font prompt + point-size check applied by hand (Aug 26 2026, task #3388 card stuck in Applying — gogoods cancelled, Otis cherry-picked the 5-commit branch squashed; card must stay Cancelled, never Apply)**: per-font missing-font detection in the completed-template scan with an outline-or-upload prompt, a font-file upload slot stored privately with the submission (authed album-scoped download for operators/press/partners), and a per-press advisory minimum text point-size check (silent until a threshold is configured). Bonus: the branch carried `docs/handoff-briefs/mrp-build-brief.md` — the missing MRP build brief the PMP brief rides on — now on main.
- **Per-press setup-fee rules engine applied by hand (Aug 26 2026, task card stuck >5 min in Applying — gogoods cancelled, Otis cherry-picked the 4-commit branch squashed; card must stay Cancelled, never Apply)**: shared rule vocabulary (`setupFeeRulesSchema`) + `computeSetupLines` derive stamper / color-setup / press-setup lines from the build with operator override; splatter counts bounded by press-configurable `maxSplatterColors` (MRP 3) — any present-but-invalid persisted count is REFUSED (pending, no stale-row fallback) so the estimate send gate fails closed; poly bag folded into one 37¢ line; builder UI, server /send gate and estimate email in lockstep; MRP values seeded marker-guarded via post-merge; presses without rules stay byte-identical.
- **PMP build brief received (Aug 26 2026, Ruby via mirror `d20612b`)**: `docs/handoff-briefs/pmp-build-brief.md` pulled onto main. Physical Music Products = second tenant of the white-label skin system — brand tokens + assets only, never a fork. Key facts: green #6CA460 fills w/ white text (one per page), square corners token, Poppins site chrome, black portal top bars w/ whitened PMP icon (brightness(0) FIRST then invert(1)), black sign-in header + green minimal footer, persona Jonathan Hibma, catalog Black 4 / Color 18 / Splatter 12 (no translucent, no EcoMix — never show MRP's line). 19 screens arrive per phase as verbatim handoffs. **Open item: the referenced `mrp-build-brief.md` (Phase 0 / phases / money walls / ground rules) is NOT on the mirror — asked Bill & Andrew to have Ruby push it before phase files land.** Known issue already flagged in-brief: 5 PMP estimate screens still spec "Ruby translucent"; await re-spec, not canon.
- **Press copy rule: "Type", never "Style" (Aug 26 2026, MRP request via gogoods — standing rule for ALL presses, Ruby notified separately)**: every press-facing "style" label became "type" — the vinyl page ("Pick a type. What kind of vinyl?", "Create a vinyl type.", "Type name", "Change/Save/Archive type", "More types", the Type column header, "Vinyl type" picker label), the jacket/label/sleeve/insert component pages ("Select a jacket type", "Which label types?", "Pick the … types you offer", "Every sleeve type comes cut…"), both builders (package + quote: "Select a jacket type", "Which label type?", "… sleeve/insert types available", the 7" no-inserts line), and the server-side custom-quote note ("selected jacket type"). Copy/aria/placeholder only — identifiers, testids, file names, routes untouched; "Old-Style Tip-On" keeps its name (product name, not the style concept). Needs a publish.
- **Press ERP identifiers applied by hand (Aug 26 2026)**: the #3385 card wedged in "Applying changes" (>5 min) and was cancelled; its single commit was cherry-picked onto main from the local task branch — generic press job # / SO # fields on pressing orders with per-press labels (MRP seeded as "MRP #"/"SO #"), operator edit in the press-orders queue + read-only display on the Sell-tab stepper, and a super_admin-only press-scoped customer profile card on the manufacturer Overview tab. Post-merge migration ran clean on dev+prod (schema-drift smoke: no drift, all 150 tables agree), the 11 new route/resolver tests pass, mirror synced. **That task card must stay Cancelled — never Apply it again.** Needs a publish.
- **Third round of stuck task merges applied by hand (Aug 25 2026, evening)**: four cards were bouncing "Apply changes" → back to Ready-for-review (MERGE_BUDGET_EXHAUSTED). Verified the art-test seated preview + Replace flow work (#3370) was ALREADY on main from an earlier landing (both the pdftoppm full-artboard raster commit and the explicit Replace control commit), then cherry-picked the two genuinely missing ones from their local task branches: Spine crop raster misregistration fix (#3374, `rasterCssLayout` + crop transform tests, 15/15 pass) and Hide-MRP-marketing-bar-when-signed-in (#3371, three MrpSiteHeader copies). Clean picks, no conflict markers, mirror synced. **Cards #3370, #3371, #3374 must be Cancelled via ⋮, never Applied** (re-applying would duplicate). #3372 (Labels after Vinyl) has NO local branch — that one needs one more Apply click to fetch it (then manual apply if it bounces again). Needs a publish.
- **Second round of stuck task merges applied by hand (Aug 24 2026, afternoon)**: the merge queue wedged again (cards bouncing at "Waiting for main agent"/post-merge). Verified #3355/#3356's content was already in main from the morning's manual applies, then squash-applied the three genuinely missing ones from their local task branches: MRP meeting briefs (#3360, docs-only), estimate-sender press name (#3365, with Reply-To routing + sender-header test), and the lazy full-artboard raster backfill (atomic CAS, no DDL). The estimate-link sign-in task was already in main too. **All of those task cards must be Cancelled via ⋮, never Applied** (re-applying would duplicate). Order Desk/Spinney verify and Delete-vinyl-colors (#3368) were NOT applied (no local branch) — those two can still be Applied normally. Needs prod publish.
- **Stuck task merges applied by hand (Aug 24 2026)**: Tasks #3355 (server-generated full-artboard raster fallback for very large art masters, 600MB cap) and #3356 (File-history rows reopen past revisions in the template viewer; `file_url` persisted per upload event) exhausted the platform merge budget and were cherry-picked onto main manually. Post-merge DDL ran on dev+prod; a publish-drift DROP of `completed_template_file_events.file_url` (prod) was re-applied afterward — column + backfill verified on both DBs. The two task cards must be **cancelled/dismissed, never re-applied** (would duplicate). Needs prod publish.
- **MRP meeting briefs committed (Aug 24 2026, Task #3360)**: three plain-text docs for Bill's MRP meeting — `docs/mrp-cto-brief.md` (hosting/health endpoint/monitoring, Postgres + Object Storage/GCS, Mux-signed-HLS-only playback, admin 2FA, SSRF guards, cert pinning + encrypted offline downloads, Stripe security posture; honest about no formal SLA), `docs/mrp-cfo-brief.md` (fan payments via embedded Stripe Checkout + Stripe Tax + real shipping + webhook-authoritative refunds w/ transfer reversal; payouts via Connect Express earmark → operator release → idempotent transfer on Stripe's standard settlement; fees presented as operator-configured/agreed-per-partnership, no invented numbers; bank-transfer ledger path; mechanical royalties as a separate operator-driven ledger), and `docs/mrp-setup-questionnaire.md` (fill-in checklist: custom domain/DNS, branding, staff invites, Stripe KYC owner, pricing source e.g. Coda sync, ledger payment method, accounting contact). Docs-only.
- **MRP portal marketing nav removed when signed in (Aug 24, 2026).** On the white-label client-portal pages that wear MRP's site header (next-steps, project home), the marketing links (Home … Contact, Sign in) drop away once a client is signed in; the account chip stays and now opens a menu with Sign out. Signed-out visitors (sign-in page) keep the full site chrome. Needs a publish.
- **MRP portal left rail now navigates (Aug 24, 2026).** The white-label client-portal rail rows with real pages are wired: Dashboard → `/dashboard`, Releases/Projects → `/projects` (all four MRP screens: next-steps ×2, dashboard, project home). Rows without a built screen (Audience, Acquisition, Orders, Buyers, Referrals, Shopify, Reports, Team/Settings) stay inert with a default cursor until their pages exist. Needs a publish to reach memphis.makesvinyl.com.

- **Artist Payments tab shows the manufacturing ledger (Aug 24 2026)**: the artist release Payments tab ("Money out to the plant") previously read only one-off `payment_requests`, so a Shopify+ manufacturing-ledger step (e.g. "Deposit for Balance of Run", requested from the artist) never appeared — the artist saw "Nothing owed on this release" while the operator saw "Waiting for artist". `ManufacturingLedger` (the same component partners use in the embedded album view, with bank-transfer/card pay flow and the 403 no-access note) is now exported from `ShopifyPlusPanel.tsx` and mounted in `ArtistRelease.tsx`'s Payments tab (read-only, pay-enabled; server still gates via `gatePayouts`). Renders only when the release has ledger steps. Needs prod publish.
- **Press bring-your-own custom domain (Aug 24 2026, Task #3339)**: the White Label tab's "Your own domain" tier is real — a press saves a subdomain of THEIR domain (e.g. `vinyl.memphisrecordpressing.com`; validation refuses bare apexes, `www.`, and our own domain families; case-insensitively unique across presses), the tab shows the exact CNAME record (`→ makesvinyl.com`), a word+icon status ladder (Pending DNS → Pending activation → Active), and a working Verify domain button that does a real DNS check server-side. Activation stays a manual operator step (Replit Deployments → Domains link, one TLS cert per host): god-view press Details gets a "White-label custom domain" card with Mark linked & active / Deactivate, plus `GET /api/admin/custom-domain-requests` (operator-gated). Once ACTIVE, the custom host serves the press's full white-label skin identically to the makesvinyl subdomain (public branding-by-host lookup, client candidate-host detection incl. the index.html first-paint detector, portal host scoping, invite landing), and newly minted estimate/invite/email links prefer it (prod-only, fallback custom → slug → request host); makesvinyl links keep working. Fail-closed everywhere: unknown or non-active custom hosts render the neutral page — never an error, loop, or another press's skin. Runbook: `docs/whitelabel-custom-domain-runbook.md`. Locked in by `server/whitelabelCustomDomain.test.ts` + `server/whitelabelCustomDomain.db.test.ts`. Needs prod publish.
- **Press-invited artists land in the white-label portal (Aug 24 2026, Task #3331)**: an artist accepting a press-referred invite ON the press's white-label host (memphis.makesvinyl.com) now lands straight in the MRP client portal (`/dashboard`) instead of the GoodTunes "Welcome to GoodTunes" ladder — server accept handler returns the portal landing only when the request host's slug resolves to the SAME press the invite is homed to AND that press's portal skin is active (`email_branding` set, data-driven, never a name check). `/welcome-invitee` is now brand-aware on white-label hosts (press logo/name/accent via the sanitized branding endpoint; skinless presses get a branded card with today's CTA — graceful fallback, no loop). New `WhitelabelArtistSteer` guardrail steers signed-in press-homed ARTISTS off GoodTunes entry paths (`/welcome-invitee`, `/artist`, `/admin/albums`) back to `/dashboard` on skinned hosts (operators/press users untouched). `/api/press-client/portal|dashboard` now also accept ADMIN-kind identities (invited artists are admin kind; estimates match by email, host-press scoping unchanged). Locked in by `server/pressInviteWhitelabelLanding.db.test.ts` (branded-host landing + GoodTunes-host control + skinless-press control + portal read with the fresh bearer). Needs prod publish.

- **Press vinyl color tool rebuilt to Ruby's approved handoff (Aug 23 2026, handoff 01282b2)**: press portal → Components → Vinyl "Rebuild this color" sheet now carries the approved Gradient / **Advanced Gradient** toggle — an Illustrator-style ramp with draggable stop positions (click the bar to add a stop up to 8, drag an added stop off the ramp to delete, tap a chip to open its color popover centered above the bare hex chip), positions persisted per swatch (`gen.locations`, additive schema field) and honored by the disc render (sorted stop/location pairs feed the gradient map). Stop labels are purely numeric ("Color 1, Color 2, …"), the "Pick N colors" heading is gone for gradient styles (Andrew ×3), metallic-swirl hint removed. **Eyedropper now samples from the press's uploaded reference photo** (canvas tap-to-sample panel inside the picker; the run-sheet Must-work list overrode the handoff tsx's screen-pick API, which remains only as the no-photo fallback). All Must-work items exercised headlessly (four picker tabs, wheel drag, Cancel/Select, Compare their photo, size chips, Replace as the one filled action); both themes screenshotted at 1440/1024/768. Raw hex stays press-side only. **Ready for the next Publish — flagging Bill (Andrew wants it in).** Needs prod publish.

- **Package tab ("Design your package") canon pass (Aug 23 2026, gogoods' run-sheet 0da8224)**: the artist-viewer package builder joined the tab-by-tab canon batch — caps micro labels ("REQUIRED · VINYL", "TRACKS", "COLOR") to sentence case, raw vinyl color codes ("T01 Ruby") replaced by swatch + friendly name via the shared `displayPressColorName` detector (raw codes stay press-side only), shared PageColumn/PageHeader shell at tab-rule width, one filled blue action (Save), word + icon statuses, both themes via the `--apple-*` vars. Dressing only — every interaction unchanged; design-lint violation count dropped. Needs prod publish.

- **MRP demo runbook committed** (Aug 23 2026): `docs/mrp-demo-runbook.md` is the team-facing dry-run checklist for the MRP demo — pre-demo setup (hosts/TLS, mail liveness, `PRESS_ESTIMATE_REVIEW_RECIPIENT` redirect, unveil flag, pricing seeded), the 1-2-3-4 estimate builder flow, artist-side receipt (`/e/:token` + accepted page, TEST-MODE pay caution), white-label client portal walk, invites, and known gaps. Claims verified against current code. Docs-only.

- **Niina's template + art-card fixes (Aug 23 2026, run-sheet 4e366c6/037c7d4)**: (1) Full Template view — raster panel art (e.g. a square front cover on a wide jacket spread) now seats registered in the best-matching side-panel die-line (Front wins ties) instead of floating centered over the whole spread; full-artboard exports and spread-shaped art keep their existing seats. Shared decision in `artPlacement.ts` (`computeRasterArtRect`, unit-tested), wired into BOTH the artist art-test viewer and the press live-test page. (2) Assets → Vinyl art cards — uploaded art now fills the card window edge-to-edge (scale-to-fill, center, crop overflow; never stretched, never letterboxed); the empty drop zone keeps the identical window. Needs prod publish.

- **GoodSync™ renamed LyricFlow™ (Aug 23 2026, gogoods' call)**: "sync" is reserved for TV/film sync licensing in the music industry, so the karaoke-style lyric alignment tool is now LyricFlow™ everywhere users see it — admin lyrics panel, album-wide wizard, toasts, buttons ("Add LyricFlow™ to your album", "Re-run LyricFlow™"), reports tab ("Plays & LyricFlow™"), welcome-back email, what's-new cards, and the sales/investor docs. Copy-only rename per the Maker/Reseller precedent: identifiers, DB columns (auto_goodsync_status), routes (/rerun-goodsync), and testids unchanged. Needs prod publish.

- **Splatter colors show swatch-only, never raw codes (Aug 23 2026, Bill's ruling)**: press-facing color surfaces hide vendor code names ("O15 w/ O08 O09 O22", "T08 w/ black") — the swatch image alone identifies the color, and the name reappears only once an operator renames it to something human. Shared detector `client/src/lib/pressColorName.ts` (code token = capitals glued to digits, unit-tested); applied to the Components → Pricing rows (which also gained a small swatch circle resolved from the Vinyl component config), Vinyl styles tiles/popovers/previews, and the Packages catalog color cards/captions/search/archived rows. Press-agnostic and safe: human names ("Blood moon splatter", "180g Black") always show. Needs prod publish.

- **Album Overview rebuilt to Ruby's approved handoff (Aug 23 2026)**: `handoff/AdminAlbumOverview.tsx` ported per handoff law (old eleven-panel Overview deleted, presentational code verbatim, real data wired). At-a-glance strip (Status / Release date / Press / Share link / Needs attention with in-place blockers expander → "Fix on Physical" deep-links to Physical → Art), then four groups: The record (hover-pencil edits, lineup), Where fans find it (Release/Pre-save segmented chip, GoodTunes link tile Open/Copy, six streaming service tiles — real brand SVGs on white carrier circles, linked = URL + Open/Copy/Remove, unlinked = paste field with Save earning blue on a valid URL), Marketing (button color swatch, per-format hero graphics, campaign gallery, real Preview email), GoodDeed® & giving (real NPO splits + post-sale lock). Split shipments evicted to Physical → Fulfillment; SPIN sits under a quiet Legacy settings disclosure — super-admin + pre-Nightbirde (legacyGogoodsId) only, ABSENT elsewhere (also gated the header badge). Partner viewers keep change requests, share link, and reviewer/preview-link management only. Both themes via body.gt-admin-dark. Honest gaps: no distinct pre-save URL in schema; button color shows "Custom color"/"GoodTunes blue" (hex-only storage). Needs prod publish.

- **Admin catalog renamed "Releases" (Aug 23 2026, Bill's ruling)**: rail item, list-pane heading, search entries, add-CTA ("Add release"), album breadcrumb. Routes/testids/identifiers unchanged; discography Albums/EPs/Singles grouping (musical type) stays. "Project" reserved for in-production efforts. Releases LIST page redesign still HELD for Ruby's mock.

- **Overview review round fixes (Aug 23 2026, after Bill's publish)**: architect review of the Overview rebuild caught a partner data exposure — the operator-only Overview queries (invited press, upload validations, lineup, gallery, labels, NPO split) fired for partner viewers before their reduced view returned, and the NPO-split read had no album scope check server-side. Both fixed: queries gated `enabled: !isPartnerViewer`, and `GET /api/admin/albums/:id/npo-beneficiaries` now 403s any partner without a membership in the album's own label/artist scope (regression test `server/npoBeneficiariesScope.db.test.ts`). Also fixed a missing `Mail` icon import that crashed the whole Overview tab at runtime (tests didn't catch it — screenshot pass did), and the at-a-glance "Needs attention" cell now shows a quiet "Nothing blocking" check state instead of "⚠ 0 art blockers". **These landed AFTER Bill's publish — the demo build needs one more publish to include the entire Overview batch.**

- **Track-row regression fixed (Aug 23 2026, Bill's law: a canon pass changes how rows LOOK, never what they DO)**: artist Player-audio + Vinyl-audio track rows regained the full disclosure set (expand, drag-reorder, per-track audio detail, bonus content, LyricFlow™, lyrics, splits, Upload masters on populated lists) via a thin adapter onto the shared TracksPanel — quiet dressing kept, interactions in lockstep with super-admin Digital. All other row-treated lists audited vs the pre-pass build: visual/copy only.

- **Pricing walk fixes (Aug 23 2026)**: GoodDeed Printing section now collapses behind the same toggle as Finishing (wired to the existing `active` flag; rates keep when off) — the typical-range hint numbers are officially dropped (Bill). Segmented-control labels vertically centered in the shared SegmentedPillToggle + tonight's local segmented controls (size chips, funded-by pair). OPEN: Splatter pricing rows still show raw color codes ("O02 w/ O03 O22") — the stored names ARE the codes; waiting on Bill's ruling for what replaces them.
- **Super-admin album canon dress pass + shared page shell (Aug 22 2026, late night)**: Physical Art/Fulfillment/Side Breaks + Payments + Customers + Early access tabs brought to canon (word+icon statuses, quiet callouts, segmented sub-tab controls, one filled action per page/section, estimate-not-quote in admin, sentence-case headers, dark comped cards) — Overview tab HELD for Ruby's redesign; shared PageColumn/PageHeader shell + type tokens (centered 1240px column, heading rhythm, breathable rows, shared meta lifted above lists) adopted on artist release tabs + super-admin album body; artist Vinyl Art cards uniform height grid; artist "+" add-format chip restored (3-card modal behind it). Needs prod publish.

- **Ruby punch-list batch (Aug 22 2026, night)**: artist Master-tab Upload masters action (Player/Vinyl inherit + link there), honest Store checklist + single-status vinyl-art cards (upload targets), retired Shopify+ removed from sell-model picker, physical-format modal collapsed to Vinyl/Cassette/CD, estimate-not-quote copy sweep, iPad rail collapse (<1024px, all portals) + contained sleeve/record slide, theme-aware loading splash (never GoodTunes logo on white-label), connections rows wired (Shopify) / honest Coming-soon (Payout, white Stripe logo), charcoal template-editor stage, GoodDeed typical-range hint mechanism (config empty — Ruby supplies numbers), phantom Album-URL toast fixed, Shopify rail item removed, Billing pay moment restyled to handoff/billing-tab mock (accepted page only — press_estimates has no release linkage), press email-keyed invite now requires press editor. Needs prod publish.

- **Monday demo gaps landed (Aug 22 2026, evening)**: (1) **Stripe pay tap TEST MODE** — accepted estimates (/e/:token/accepted) show "You owe" + one filled blue "Pay $X"; amount server-derived, session reused (no double charge), paid-session recovery, Paid state + press-side Paid chip. (2) **Press Downloads sub-tab** — finished print files per component + all-print-files, per-track master originals + all-masters ZIP, honest empty/health states, authed blob downloads. (3) **PQ cutting-master sheet** — signed-out /pq/{token} online sheet (tap-to-play on Mux-ready tracks, 17/20/25 verdicts, confirmations never pre-ticked, zero authed requests) + two-page PDF twin; "PQ sheet" entry under Physical → Audio. (4) **Wave mastering callout removed.** Run-sheet statuses updated in docs/handoff-briefs/mrp-demo-monday-runsheet.md. All of this needs a prod publish to show on memphis.makesvinyl.com.
- **Fix: god-view press Projects cards no longer bounce to /admin** (Aug 22 2026): on /admin/manufacturers/:id the project cards linked to /vendor/albums/:id, which the vendor route guard bounces for super-admin sessions; god-view now opens the operator album page (/admin/albums/:id?tab=press). Real press portal behavior unchanged.
- **Ruby: two more briefs — pricing model/source in White Label + component↔pricing lifecycle** (Aug 22 2026, Bill): `docs/handoff-briefs/ruby-briefs-mrp-sow.md` briefs 8–9 — per-press pricing model picker (MRP all-in-ladder style vs Viryl component-itemized) driving both the operator Pricing page and "My price list"; pricing source selector (GoodTunes native / CODA.io / Odoo, Shopify-connect feel, sync-lock + honest gaps); duplicate-carries-pricing + external-source rename/duplicate reconciliation moments.
- **Ruby: four more MRP SOW briefs** (Aug 22 2026, Bill): `docs/handoff-briefs/ruby-briefs-mrp-sow.md` — Short Run preset quantities (#1–3), free quantity entry need (#5), metalwork cutting selector (#9), next-price-break callout (#27). Needs, not solutions; MRP skin rules apply. Otis wires each as your designs land. Monday status sheet for Bill's deck: `docs/mrp-sow-status-monday.md`.
- **Ruby: new brief — client estimate download page** (Aug 22 2026, Bill): Bill has ruled the MRP SOW's "PDF attached to the estimate email" ask (#30–31) is done our way instead — the client logs into their tokenized estimate page and downloads a designed estimate PDF from there. Your brief is `docs/handoff-briefs/estimate-download-spec.md`; MRP's own example sheet to beat is `docs/handoff-briefs/mrp-example-estimate.pdf`. Meanwhile Otis is building #37 (mock-up image rendered server-side into the estimate email + PDF).
- **Memphis client portal shipped (Ruby handoff `handoff/memphis-client-portal/`, mirror SHA `b912fb6`)** (Aug 21 2026): Memphis clients now get the full MRP-branded light journey — the redesigned MRP-flavor estimate email, the light `/e/:token` estimate page (run-size tiers, per-record/setup breakdowns, Ask-Brandon + Share sheets, Start-this-project confirm with account creation), the `/e/:token/accepted` confirmation, and a signed-in client portal on the white-label host: `/next-steps` (logged-out sign-in + logged-in project checklist with real file upload), `/dashboard` (+ `/dashboard/next-steps`) with KPI strip, range-switched recharts chart, activity feed, top projects, channels + giving, and `/projects` project home (archived toggle, rail search). All real data from `GET /api/press-client/portal|dashboard`, keyed by acceptedByCustomerId/sentTo email; honest zeros for fresh clients. Scoped to presses whose branding sets `skin: "mrp-light"` (Memphis only today); other presses keep the prior dark surfaces. New standing white-label rule: browser tab shows the press's name + square-logo favicon on white-label hosts (`WhitelabelDocumentHead`, brand-driven, all presses). Fan-only chrome (new-fan welcome sheet) suppressed on white-label hosts. Estimate emails temporarily deliver to bill@gogoods.com for review (`PRESS_ESTIMATE_REVIEW_RECIPIENT` in `server/mail.ts` — remove when review done). Dark-mode addendum (`3acb222`) deliberately not applied yet.

- **Photoshop-style vinyl mockup builder ported (Ruby handoff, orphaned mirror SHA `13ac133d`)** (Aug 21 2026): the press portal's Components → Vinyl page now carries Ruby's rebuilt PressVinylPhotoshopMockup features, applied verbatim on top of the already-wired Aug 20 port: **match-from-photo** — rebuilding an uploaded-photo color runs client-side palette extraction (`extractDiscPalette`, 96px canvas sampling ONLY the vinyl ring between label and disc edge — background, label, and groove shadow excluded) and `suggestDiscStyle` applies one explainable first guess (splatter flecks, split+splatter, metallic-blend lightness sweep, blended-vs-marble by edge speckle) ONCE into an untouched sheet only; the caption "Suggested from their photo — a first guess. Change anything." shows only while the suggested style stays selected; a "From their photo" chip strip in the compare drawer click-fills the next empty color row; **eyedropper** — TEMPORARY (Andrew) screen-pick button (EyeDropper API, Chrome/Edge) in the color picker footer, honest alert elsewhere; **form rule enforced** — the rebuild/creator sheet no longer dismisses on outside click, Esc/Cancel/Save close it (the handoff tsx contradicted its own README here — README's Must-work list won; flagged in the task summary). Handoff recovered from the ORPHANED mirror commit `13ac133d` via the GitHub API (a later push clobbered mirror main) and preserved in-repo under `handoff/press-components/` (tsx + README + Andrew's three real MRP disc photos). All 9 README states screenshotted both themes at 1440px (`screenshots/task-3281/`). Client-only; prod needs a publish.

- **Press white-label branding engine** (Aug 21 2026): each press now has a persisted white-label brand — one accent hex, corner style (rounded pill vs squared), and a single contact line — stored on `manufacturers` (logos keep living on the existing Details logo kit). The Settings › White Label tab is wired to real endpoints (GET/PUT `/api/press/:id/branding`, press-editor gated): Save persists and survives reload, corner style + contact line thread into Ruby's live estimate/email previews, and a "Prefill from your site" action (POST `/api/press/:id/brand-suggest`, SSRF-safe scrape) suggests the site's logo + an accent palette — suggestions only, nothing saves without the operator hitting Save. Branding applies to the customer surfaces: the public `/e/:token` viewer (accent on tiers/total/CTAs, corner style, letterhead footer now uses the press's real logo + contact line — hardcoded MRP demo assets remain only as Memphis's fallback), and press-referred invites (branded invite email with a builder welcome line; the invite accept/login screen shows the press logo, accent-tinted fields/submit, corner style, and a welcome banner pointing at the project builder). An unconfigured press falls back cleanly to today's GoodTunes look everywhere; the "Always GoodTunes" surfaces (fan-funded story shimmer/explainer) deliberately stay GoodTunes blue. Public estimate read stays a strict sanitized allowlist (route-tested: exact keys, no shareToken/press-id leak; branding write gates 401; invite pressBrand is display-only). Tab remains functional-not-final for Ruby's restyle; custom domains stay a separate task. Note: the estimate EMAIL itself now ships as the fully-designed Task #3271 template, whose accent flavor reads `manufacturers.email_branding` — unifying that flavor switch with this task's `brand_accent_color` is follow-up material.

- **White-label client estimate email shipped (Ruby handoff `handoff/press-client-estimate-email/`, mirror SHA `e86b169`)** (Aug 21 2026): when a press sends a client an estimate, the recipient now gets the fully-designed email instead of the old one-line note — 600px single dark charcoal column, table-based HTML with inline styles and explicit background colors everywhere (dark canvas survives light-mode clients, degrades full-width on small screens), the fully-expanded numbers for the ONE quantity the press prepared (per-record lines, setup lines with $0 = "Included", run, accent-tinted Estimate total — recomputed server-side from the stored builder state + the press's CURRENT pricing rows via `computeQuoteEmailBreakdown` in `shared/quotePricing.ts`, omitted entirely rather than shown partial when a legacy row can't verify), one filled "Open your estimate" button to the private tokenized `/e/:token` page, one quiet other-run-sizes line, the GoodTunes hook as one static quiet line, "Questions? Just reply" sign-off, and a press-letterhead footer. Two flavors from ONE shared structure with a swappable accent bundle: GoodTunes blue default, Memphis Record Pressing gold (`#D6A63F`, dark ink on the filled button) — resolved from the new `manufacturers.email_branding` jsonb (MRP seeded on both DBs, keyed by domain; no press-name string checks in the template), so future per-press twins are additive data. Sender: From stays a GoodTunes address with display name "<press contact> · via GoodTunes®"; Reply-To is the contact who prepared the estimate (falls back to the press contact email), so "Just reply" reaches them — true per-press sending domains (`From: brandon@memphisvinyl.com`) need per-press SPF/DKIM DNS and are deliberately later work per the handoff README. Copy rules honored: real ®, commas in dollars, "estimate" never "quote" (test-enforced in `server/mail.pressEstimateEmail.test.ts`; builder is pure/exported: `buildPressClientEstimateEmail` in `server/mail.ts`). Both send and resend paths use the new email. Needs a republish.

- **"Download all masters" is now one zip named after the album** (Aug 21 2026): on the album Physical → Audio panel, the button saves a single `<Album Title>.zip` (e.g. `Hope.zip`) instead of a pile of loose per-track downloads. New authed streaming route `GET /api/admin/albums/:id/masters/download-all` (operators + the album's assigned press, same gate as per-track): same source preference (original upload first, served playback fallback), unusable tracks (no master / un-mirrored external link / file missing from storage) are skipped inside the archive — the existing pre-flight toast still tells the operator which ones. Entries keep the `NN Title.ext` naming; zip is streamed end-to-end (store, no deflate), never buffered whole — the client mints a short-lived album-scoped signed link (`POST .../download-all/link`) and navigates a plain anchor, so the browser's download manager streams to disk instead of buffering a multi-GB Blob in JS memory (token = stateless HMAC on SESSION_SECRET, ~2-min TTL, access re-checked at download). Per-track downloads in View Masters unchanged. New dep: `archiver`. Route tests in `server/mastersZipDownload.routes.db.test.ts` (10, incl. signed-link auth boundary).

- **Publish DROP-warning defusals #2 and #3** (Aug 21 2026): republish migration screens wanted to DROP prod-only columns that in-flight task merges had landed on prod but not dev — first `manufacturers.email_branding` (8 rows) + `songs.source_url` (1,482 rows), then `manufacturers.estimates_white_label_enabled` (boolean not null default false). All added to dev with matching types and idempotent `run_sql_both` guards appended in `scripts/post-merge.sh` so the drift can't recur.

- **Niina Shopify go-live: historical backfill + held emails** (Aug 21 2026, Task #3259): connecting a store with past sales no longer strands past purchasers. New per-store `History` panel on Admin → Shopify: **Preview (dry run)** pages the store's historical paid Shopify orders via GraphQL, filters to mapped products, and shows count / date range / projected GoodDeed numbers; **Backfill** mints them through the exact webhook materializer strictly in original order-date sequence (earliest purchaser gets #1; orders keep their original created_at; idempotent by Shopify order id; refunded/cancelled/unmapped/no-email skipped; platform-fee ledger + NPO credits accrue; no Shopify write-backs, no fulfillment). Backfilled orders send **no email** — each is stamped held; a clearly-labeled **Release N emails** control sends the redemption emails as a batch (atomic per-row claim, failed sends retryable, refunded never release). NPO beneficiary credits (EndoFound $1/unit) now also mint on **live Shopify webhook orders** (previously direct-checkout only). Album Email-appearance test-send now accepts an optional named recipient (e.g. Ruby), still `[Test]`-stamped with a placeholder code. Operator sequence documented in `docs/niina-shopify-golive-runbook.md`. New orders columns `redemption_email_held_at`/`redemption_email_released_at` (dev + prod via post-merge).

- **Three production 500s fixed** (Aug 21 2026, from ops alerts/Sentry): (1) admin Orders list no longer fires per-order item + legacy-SKU lookups for up to 500 orders at once (pool exhaustion → "timeout exceeded when trying to connect"); items are now batch-loaded in two queries (`getOrderItemsForOrders` in commerce.ts). (2) Master download 500'd on song titles with non-ASCII characters (em-dash in "Hope — a note from Jane" → ERR_INVALID_CHAR building Content-Disposition); filename is ASCII-folded with the true UTF-8 name in RFC 5987 `filename*`. (3) Admin shell black-screened for a fan session reaching /admin (`null.canInvite` — role payload is `null`, not `undefined`); null-safe now. Prod needs a publish to pick these up.

- **Catalog › Components switcher aligned to the content column** (Aug 21 2026, gogoods): the six-way segmented control (Vinyl / Jackets / Inner Sleeves / Center Labels / Inserts / Stickers) on every Components page now sits inside the same centered 1240px + 40px-inset column as the breadcrumb and heading, so its left edge lines up with CATALOG › … at every width; narrow panes scroll the control, never the page. Same component on press portal and super-admin. (The Templates-style track/thumb restyle itself had already shipped.)

- **Honest component quotes while gatefold pricing is pending** (Aug 21 2026): the press estimate builder no longer carries any hard-coded demo prices — every line (vinyl by tier/color name per size, labels, jackets, sleeves, inserts, stickers, assembly/shrink and the setup lines) resolves solely from the press's Components → Pricing rows. A component with no real price (e.g. Viryl's Wide Spine / Double Gatefold jacket, Custom Quote on their 2026 sheet) shows an amber "Pricing pending · custom quote" line, is excluded from the estimate total ("Estimate total · incomplete"), and blocks Send-to-artist client-side AND server-side while drafts still save. The server gate is authoritative: `/send` RECOMPUTES the pending lines from the stored builder state (which now persists the catalog color/tier names) + the press's CURRENT pricing rows via the shared `shared/quotePricing.ts` module — it never trusts the client's `pricingPending` flag, and fails CLOSED (an estimate with no saved builder state 409s: nothing to verify = nothing sends) — a sent estimate's payload is also immutable via PUT and send is one-way — Sent/Viewed can never downgrade back to Draft, and calling /send again is a pure RESEND (re-emails the already-minted link, never touches status or payload, never regresses Viewed) — duplicate into a new draft to change a sent build, and estimates can't be minted or flipped to "Sent" via direct POST/PUT status writes (409 → use /send). Every line the builder charges is now a manageable row on Components → Pricing: standard flat rows (labels, jackets incl. gatefold, sleeves, inserts, stickers, and the assembly/shrink/setup service lines) are seeded blank for every press and show/count under every size chip, so a press can price itself fully sendable. Viryl's page was backfilled from the already-loaded 2026 data — all 47 vinyl cells plus the 2026-derivable flat lines (cutting $487.50, plating $300 w/ stampers Included, test pressing $86.25, colour setup $93.75, insertion 11¢, shrink 15¢, blank labels 8¢, UPC sticker 8¢); Custom Quote items (gatefold jackets) and batch/variant-ambiguous rows stay blank on purpose (idempotent marker-guarded script in post-merge, never overwrites press-typed cells, decoy-press-safe). Route-level regression test: fully priced build sends; unpriced gatefold 409s even if the payload lies; direct Sent writes 409 (`server/pressEstimateSendGate.routes.db.test.ts`). Also fixed a latent `/send` crash (`users.name` → `display_name`). Legacy package ladders / SellPanel / fan pricing untouched; when Viryl supplies the gatefold price, pricing the `jackets:gatefold` row makes those builds price normally with no code change.

- **Component choices now link to their prices** (Aug 21 2026): each press's package component options (jacket styles, inner sleeves, inserts, download cards, stickers, poly-bags, shrink-wrap, insertion) carry an explicit price linkage — a Tier 3 component ladder, a Setup & Services item, "included in record price", or "custom quote" — editable in a new "Component pricing" section on the press catalog page. A shared resolver prices a package composition at a quantity with record-ladder rung-snap semantics (snap up; beyond top = custom quote) strictly from THAT press's own rows; unlinked options show honestly as "no price on file", never $0 — and any explicitly selected jacket style (single included) prices off that press's matching tier×jacket ladder or shows an honest custom-quote note (no silent default/legacy fallback). Operators reviewing an album's press package get an itemized "Package cost breakdown" card on the Sell panel (record line + component lines + one-time metalwork/setup/test-pressing services; operator cost data only — fan pricing/checkout untouched). MRP + Viryl linkages seeded (idempotent, per-link guarded, operator edits win; decoy-safe press resolution — prod's stray VIRYL shell rows repaired onto Viryl Technologies/viryl.ca; Viryl gatefold honestly custom-quoted; a jacket priced by the all-in tier×jacket record line is never double-charged as a component; $0 press-sheet services resolve as included). New table `press_component_price_links` (dev + prod migrated, post-merge guarded).

- **UPC purchase path researched + Ruby handoff written** (Aug 21 2026): two new repo docs, no code. `docs/upc-purchase-path.md` — GS1 US landscape memo (single GTIN $30 no-renewal license in the ARTIST's name; company-prefix tiers; Channel/Solution Partner programs are the only sanctioned way to sell GTINs in-app, application-based; $6,500 API Add-On; why a GoodTunes-prefix sub-license breaks Amazon/Verified-by-GS1; required customer/product data + disclaimer set; MRP's $35 UPC maps onto press_service_items as a flat adder, never a pricing-ladder rung; recommendation: press path first, GS1 guided referral second, Channel Partner application in parallel, never our own prefix). `docs/upc-flow-handoff-ruby.md` — end-to-end flow for Ruby to mock: preflight warning → Get-a-UPC sheet (press $35 vs GS1 $30 paths, per-format) → explicit agreement step → press/operator-only submission → Requested/Submitted/Assigned/Failed states → UPC lands on the Details row + barcode artwork unlocks, plus disclaimer copy requirements and 7 open design questions. Open business questions for Bill listed at the memo's end (press pricing confirmations, partner-program decision, markup policy).

- **Vinyl page "Don't offer" menu items shortened** (Aug 21 2026, per gogoods — a Bill/Ruby call, no handoff note existed): the style-card and color-swatch hide toggles now say just "Don't offer" / "Offer" instead of appending the full style/color name (long splatter names wrapped to three lines). Size offer toggles keep the short size label. NOTE FOR RUBY: the handoff's `Don't offer ${name}` grammar is superseded for styles/colors. Client-only; prod needs a publish.

- **Stuck task merges applied by hand** (Aug 21 2026): the platform's "Apply changes" bounced on two ready tasks, so their commits were cherry-picked onto main: equal-width CD silkscreen bands (`radial-gradient(circle closest-side, …)` sizes the band gradient to the disc radius so multi-ink silkscreens render as equal rings on the CD build page + chips) and the reliable hi-DPI crop render after first template upload (bounded retry + "Sharp preview unavailable" pill in the press live-test viewer and shared TemplateArtViewer, with new `cropSharpRender.ts` helper + tests). Both task cards were cancelled — they must NOT be re-applied. Client-only; prod needs a publish.

- **Press vinyl styles page rebuilt from Ruby's handoff** (Aug 20 2026): `handoff/press-vinyl-styles/` ported verbatim as the press portal's Components → Vinyl page (old page deleted per the delete-first law). Real config wiring: styles/colors persist to the press's vinyl component config (card select/Edit/Duplicate/Archive, Add-style gallery with gradient-map generator styles off `/vinyl-gen/`, per-color photo upload through the standard admin image pipeline, finish-bar dual-action pills, 12″/10″/7″ size + offer pills, press-branded center labels via the shared white-mark logo chain). Addendum: the Components chip row above the page is now ONE segmented control matching Templates'. Two standing sweeps also landed: **size pills everywhere** are largest-first 12″/10″/7″ with the Single/EP/LP marketing words dropped (builders, quote, pricing, sleeves, audio-spec grids), and the **blue-button sweep** across admin/press/artist portals (header/toolbar actions → quiet gray-outline pills; ready-state = blue border + text, no fill; solid #319ED8 reserved for the one earned confirm — canon note added to docs/apple-canon.md, shared `.gt-quiet-pill`/`.gt-ready-pill` utilities). Client + shared-schema (additive jsonb fields only); prod needs a publish.
### Open question for Ruby (from Bill, Aug 18 2026)
- Acquisition tab removed from ALL press portals (gogoods, Aug 19 2026): the fan buying funnel is artist/label/manager marketing data, not the plant's — dropped the press rail entry (registry.ts) and removed "acquisition" from PRESS_TAB_IDS so it's unreachable in press context; kept for label/non_profit. NOTE FOR RUBY: keep Acquisition out of the press left-rail handoff so the rail order stays aligned. Client-only; prod needs a publish.
- Press portal Acquisition tab Apple-canon lead (Aug 19 2026): the tab now opens with the standard big "Acquisition" H1 + plain-language subtitle like every other section, and the in-card funnel keeps a compact heading. Client-only; prod needs a publish.
- Press Projects tab copy (Aug 19 2026): the god-view "+ Add album" action and the empty-state "No albums yet." now say project(s), matching the tab name. Client-only; prod needs a publish.
- Circle avatars task applied manually (Aug 19 2026): task #3196 (perfect-circle person avatars — square center-crop at every photo save boundary + backfill of existing non-square photos) was stuck on a platform merge block, so its commit was cherry-picked onto main and the post-merge backfill ran (dev 4 photos fixed; prod already backfilled by the task). Prod needs a publish for the code half.
- Press builder discs now always carry the press's label (Aug 19 2026): estimate/package builder vinyl renders were missing the press logo for presses whose catalog colors are photo-based with no hex (e.g. PMP) — the photo branch skipped the center label entirely, the logo was gated to ≥70px discs, and color tiles used a flat hex dot. All three now match the catalog color-setup treatment: photo or generated disc + press label/logo at every size, and color tiles render the mini vinyl. Client-only; prod needs a publish.
- Scoped admin search 500 for press accounts fixed (Aug 19 2026): the manufacturer branch of GET /api/admin/search/scoped filtered albums on a submitted_press_id column that never existed in any database; all three queries (albums, roster, pressing orders) now scope via album_skus.press_id — the same press-assignment linkage the press portal uses. Prod needs a publish.
- Press add-artist 500 on “How???” fixed (Aug 19 2026): the restored prod Person was mislabeled “$how” but carried How???'s real Spotify URL, so the new unique index blocked the fresh insert with a raw 23505 500. Data: prod row renamed to its true identity (How???). Code: storage.createPerson now recovers a people_spotify_url_active_uniq conflict centrally by returning the existing active Person — covers press add-artist, credits import, and every other create path. Prod needs a publish for the code half.
- Template Test art placement (Task #3189, applied manually Aug 19 2026 after the platform merge stalled on the agent lock): full-artboard PDF exports now seat edge-to-edge via shared computePdfArtRect (press live-test + artist art-test viewers); bleed/cut-sized and transposed exports keep centered-on-GT-Bleed placement; pure-module regression tests pass. Task card should be dismissed, NOT re-applied.
- Spotify “Exact match” fixed (Aug 19 2026): the exact-match comparison stripped ALL punctuation/symbols, so searching “How???” badged “$how” as the exact match (and the server could auto-attach it). Exactness now keeps punctuation (still case/accent-insensitive) in the client badge AND both server auto-match confidence paths; loose matching remains for ranking only. Dedupe is now race-safe: partial unique index people_spotify_url_active_uniq (dev+prod+post-merge) with 23505 recovery returning the winning row.
- Spotify artist import fixed (Aug 19 2026): the Add People “Search Spotify” click was silently minting duplicate People (client read the wrong candidate fields, then called a nonexistent link endpoint) — candidates now map the server's real id/photoUrl/spotifyUrl shape, one POST carries photo + Spotify link, and POST /api/admin/people dedupes on spotifyUrl so a re-click returns the existing row. Prod cleanup: restored one clean “$how” Person (photo + Spotify link intact) after both duplicates were deleted.
- Builders now show each press's REAL color catalog (Aug 19 2026): PressPackageBuilder/PressQuoteBuilder "Pick a type"/color steps read press_formats→tiers→colors via a shared usePressCatalogSwatches() hook (admin catalog fetch in portal/god view; invited-press catalog on the artist side — no admin fetch); tier names become the type cards, DB swatch photos render on the discs; hardcoded MRP demo set is now only the empty-catalog fallback; saved packages with stale color ids snap to the press's first matching color — snapping waits for BOTH saved-state hydration and catalog resolution (no fetch-race clobber); sizes a press doesn't offer gray out as "Not offered"; the artist Start-from-a-package rail + name-based color prefill also resolve against the real catalog.
- **Artist Test page — press-supplied files**: when the file in a completed-art slot was pasted in from the press/operator side (e.g. MRP Jacket 08-08-26.pdf on Aug 10), the artist Test page currently shows its checks as if it were the artist's own upload. Bill saw this on a release where the artist had uploaded nothing and flagged it. Options we see: show it anyway (one shared slot); show it labeled with who supplied it ("Supplied by Memphis Record Pressing"); or hide press-supplied files from artists and show a first-upload empty state. Bill wants your call — also relevant: two policy decisions Otis made on the production lock that need your blessing: (a) the lock blocks only the album's own artist/label from replacing (operators + press stay unblocked), (b) only a genuine press-member download locks — an operator downloading on the press's behalf doesn't. A press-only unlock endpoint already exists for your future press-side control.

### For Ruby — coordination on today's overlapping surfaces (Otis, Aug 18 2026 evening)
- **Your release-it-all push is received**: all five handoff files (ArtistPortalRestructureFlow rev, ArtistTemplateTest rev, and the three nav-restructure rails) are pulled onto project main via a merge that keeps your commits in history. Build order next: verbatim re-replace of the artist Test page, then the goldenrod per-panel upload flow, then the rail standard.
- **No overlap found, one heads-up**: today I changed the *shared* GT overlay engine (`gtOverlayEngine.ts` — `extractGtLayers` now also returns `paintedLayerNames`; the press Live-test file-hygiene check keys off layers that actually PAINT content, because flattening leaves empty GT layer *entries* behind — Andrew's Viryl all-vector file false-failed on names alone). Your new ArtistTemplateTest drops the press-only hygiene check from the artist page, so no conflict — but any future handoff that reads GT layer names for a pass/fail should use painted layers, not the raw names list. Also shipped today (all server/live-test side, none touch your handoff files): vector-only art passes Image-resolution; authed masters downloads in the press panel; cut-rect parity restored in the artist check after #3184.
- **Still open for you** (unchanged): the press-supplied-file question + the two production-lock policy calls in the "Open question" block above.

### Done and live
- **Artist release Dashboard status banner removed** (2026-08-19, Bill ×3): the full-width "‹Album› is live for fans." / "is at press" / "still in draft" band at the top of the release Dashboard is gone for every release state — the format rows' own status pills (✓ Live / At press / Draft) already carry it, per the ArtistPortalRestructureFlow canon. The band now renders ONLY when a payment milestone is requested, carrying just the balance-due line + the one blue "Pay balance" pill; otherwise the Dashboard starts directly with the format heartbeat rows. No compact LIVE breadcrumb badge added (Bill: nothing replaces the banner). Needs a republish.
- **Package builder now wears each press's own brand** (2026-08-19): gogoods flagged Hellbender's builder showing the Memphis mark on disc labels and "MRP Packages" headings. The whole builder kit (package builder, estimate builder, saved-packages index, artist "Start from a package" rail, god view) now reads a per-press brand context — press logo on disc center labels/jackets/stickers (label logo, falling back to the main logo), initials in the arc text ("HV-001") and in "HV Packages" headings/breadcrumbs/save toasts. Memphis remains the default only where no press branding exists.
- **Print-file scanner: CMYK detected by paint operators, live text now warned** (2026-08-19): Andrew's Viryl file said "RGB only" because it paints with CMYK `k` operators without a literal /DeviceCMYK token, and its only RGB was an invisible Illustrator preview thumbnail — the scanner now reads the actual paint operators, so that file passes color. Live text with embedded fonts is now a warning ("outline all type before sending final print files") instead of a silent pass. Applies to dev now; production changes on next publish.
- **Small fixes** (2026-08-19): the dashed "Your next package lands here" card on the packages index is now a real button (opens the builder, same as Create package); reserved the `e` URL slug so public estimate links can never collide with artist/album share pages.
- **Press economics calculator live at /press-calculator** (2026-08-19): Bill's self-contained calculator page is now served at `/press-calculator` on every host (e.g. get.goodtunes.music/press-calculator) — same standalone-page pattern as /investors and /release-info (unlinked, noindex header + meta, no-store so a re-upload takes effect immediately, wins over the SPA fallback). Source of truth is `server/assets/press-calculator.html`; swap that file to update the page.
- **Send-loop hardened after code review** (2026-08-19): sending an estimate now saves the configuration first and lets the server's send call be the single Draft→Sent transition (previously a dropped connection could leave a "Sent" row with no private link and no mail). The builder also now reads the delivery result — if the link was minted but zero emails were actually delivered, the press sees "saved and link created, but the email couldn't be delivered — check the address and send again" instead of a false success. The public estimate page's Ask/Send/Start sheets remain the documented front-end staging posture pending Ruby's persistence call.
- **"Start from a package" rail on the artist Design-your-package page (bundle chunk g)** (2026-08-19): the artist in-release builder (album Package tab) now opens with Ruby's Aug 19 package rail when the invited press has LIVE saved packages — Apple-Music-wide cards (eyebrow "{PRESS} PACKAGE" · title · spec subtitle above the face; the 460×260 cover face with the press-designed vinyl arcing up from the bottom edge and the sell line over the top scrim; quiet "From $X.XX / unit at {minRun}" price line; quiet blue "Start from this package" affordance earning opacity on hover/selected; word+icon "Selected" badge). Picking one prefills the builder (size from the package's record, run to the package minimum, vinyl color matched by name into this press's catalog) and quietly says what was set — clicking again deselects without undoing tuning. No packages = the section doesn't render and the builder opens on Pick-a-size exactly as before. The press's live packages ride the artist-readable invited-press payload (the press-scoped estimates route stays closed to artists; drafts/archived never surface). Card art reuses the press builder's own cover/disc renderers so the artist sees pixel-identical package art to what the press designed. Verified authed both themes incl. the prefill click. **Deviations for Ruby**: (1) the rail is theme-aware like the rest of this page (handoff was dark-only); (2) the handoff's four mock packages are replaced by the press's real saved LIVE packages — a press with none shows no rail (two demo packages seeded on MRP in dev so you can see it on the sampler); (3) color prefill is name-matched into the press's catalog — when the package's swatch name has no catalog match the toast says to pick the color below rather than guessing; (4) the handoff's note pill ("Most artists start here") has no data source yet, omitted.
- **White Label moved into pinned Settings + settings page built to Ruby's Aug 19 handoff (bundle chunk f)** (2026-08-19): the top-level "White Label · Soon" rail row is gone; White Label is now a sub-tab of the pinned Settings tab (old `?tab=whitelabel` deep links canonicalize to `settings=whitelabel`). Settings sub-tabs renamed to the canon — **General** (was Profile) · **Team** (was Staff) · Payouts · Notifications · **White Label**; legacy `?settings=profile|staff` links still land right. The White Label page is Ruby's handoff verbatim: config sections LEFT (Domain — GoodTunes subdomain "Ready now" vs your-own-domain with the DNS-verification word+icon; Brand color — six presets + hex field with the "Too light on dark" contrast check; Logo kit — light/dark tiles fed from the press's real logo set; Rep identity; the Always-GoodTunes card) and the sticky LIVE PREVIEW right (Estimate/Email segmented, accent + logo + domain re-skin keystroke-live, earned confirm rendered in their accent, gold flips button text dark). "Save changes" earns its blue only when dirty. Verified authed both themes on the god view; design-lint re-baselined (6944 — handoff-verbatim sizes). **Deviations for Ruby**: (1) config is front-end-local for now — no persistence endpoint yet, same staging posture as the estimate page's Share/Ask/Start sheets; say the word and we'll wire storage + the real skinning; (2) Payouts and Notifications stay as Settings sub-tabs (they're live functionality your rail canon didn't list); Contacts lives inside Team (one people surface today, not two); (3) logo "Replace" hints point at Details rather than uploading in place; (4) accent default is GoodTunes blue until a saved accent exists; rep identity shows the press's contact email (no per-rep records yet).
- **Press rail restructured to the canon PRESS_NAV (bundle chunk e)** (2026-08-19): the press portal + super-admin god-view rail (one registry, both chromes) now reads Dashboard · **Details** (new — the press profile surface hoisted onto the rail: services, icon/wide logo, bio, address, website, contact; same body as Settings → Profile) · Clients · Create (Estimates · Packages) · Projects · Acquisition · Product Specs (**MRP Packages** · **GoodDeed® Certificates** · Specs · Templates) · **Components format-first** (Vinyl · CD · Cassette · Pricing) · Referrals, Settings pinned bottom. The per-component pages (Jackets, Inner Sleeves, Center Labels, Inserts, Stickers) are now an in-page segmented pill row on the **Vinyl** page (visited segments stay mounted so build state never resets, per the catalog-pills rule); segment mirrors into `?comp=` for deep links; legacy `?tab=comp-jackets|…` URLs canonicalize to `comp-vinyl&comp=<segment>` so old links land with the right rail highlight + breadcrumb. **CD** and **Cassette** are first-class rail pages rendering the media-catalog build surfaces (honest "No CD catalog yet" empty state if the press has none). The Product Specs catalog leaf is labeled per press — initials for multi-word names ("MRP Packages"), full name otherwise ("Hellbender Packages"). Verified authed both themes on the god view. **Deviations for Ruby**: (1) White Label still sits top-level above Settings — it moves inside Settings when the White Label settings body ships (next chunk); (2) the catalog page's own headings still say "GoodTunes Packages" / "Build your GoodTunes® packages" — say the word if the body copy should follow the rail rename; (3) Details keeps the existing profile body (services toggles + profile card) rather than a new bespoke page.
- **Package builder rework + MRP Packages catalog built to Ruby's Aug 19 handoffs (bundle chunk d)** (2026-08-19): (1) **Builder** (`PressPackageBuilder.tsx`) — client picker fully removed (packages are catalog objects, not per-client sends); new `#step-artist-card` section (name, 60-char sell line, cover picker: 6 PPB gradient tiles + Magic Background matched to the vinyl + an Upload tile with a quiet well, 460×260 live preview identical to the artist rail); save section reworked around the **minimum run** ("Full run total — at the minimum run, the most an artist would pay"), segmented min-run row, ONE blue "Save to catalog"/"Save changes"; all terminal saves land status `live`; edit mode hydrates from an opened row with an edit-aware heading + stats band. (2) **MRP Packages index** rebuilt to the handoff: card grid of mini artist-card faces (scaled 460×260 stage, disc + top scrim + sell line), word+icon Live/Draft/Archived pills (draft/archived dim), hover frosted ••• menu (Edit / Archive "Keeps its estimate history" / red Delete + 400px confirm), pinned "$X.XX / unit at N minimum" row, "How artists see it" preview sheet, dashed "Your next package lands here" hint, save-arrival whisper (blue ring draw + "Saved" chip via `?saved=<id>`, stripped after). (3) **Server** — `archived` added to package statuses; new scoped DELETE estimates route. Verified authed both themes on the god view; design-lint re-baselined (6916 — handoff-verbatim sizes + one card-face false positive). **Deviations for Ruby**: stats-band estimates/projects counts omitted (no per-package usage data yet); Archive AND Delete both show on every row (no has-sales signal to split them); the Upload cover tile is a frontend-only stand-in (no persistence); single name field — the artist-card name IS the package name.
- **Estimate send loop + client estimate page built to Ruby's Aug 19 handoffs (bundle chunks a–c)** (2026-08-19): (1) **Canon sweeps** — appearance switcher is now the WORDED Light/Dark/System pills under an APPEARANCE eyebrow (AdminUserMenu, all portals); artist wall cards lost the hover chevron; Estimates index header "Build estimate" CTA is a quiet dark-gray-outline pill per the new header-action canon. (2) **Artist Test page per-panel ending** — per-panel uploads now SEAT art locally (object-URL) with the honest "Looks right. Not yet measured." status card (CircleDashed, never a green Passed), "Art seated · not yet measured" header chip, stitched full-template view once all panels seat, and quiet per-panel Replace pills. (3) **Estimate send loop** — builder's Send flow now takes the artist's email + an optional manager name/email pair; Send earns its blue only when an artist is associated AND ≥1 valid email (gray hairline outline until then). POST `/api/press/:id/estimates/:estimateId/send` mints a private share token, stamps sentTo/preparedBy/clientName into the payload, flips status to Sent, and emails each recipient ONE line ("‹First› at ‹Press› sent you an estimate for ‹Job›") with a single blue "View estimate" button. (4) **Client estimate page** — new public `/e/:token` route (VIEW-only, link-not-login; never authorizes checkout) renders Ruby's PressClientEstimate handoff verbatim with real data: displayId, sent date + 30-day validity, Prepared for/by, spec line, and the qty-tier curve ANCHORED so the quoted total at the sent run size is exact; first open flips Sent → Viewed so the press sees engagement. Share/Ask/Start sheets are wired per handoff (account creation begins only at Start — those three actions are still front-end-local, no server endpoints yet). Dark-only; exempted from the first-paint light-theme detector on the admin host.
- **Rail standard built to Ruby's nav-restructure handoff (chunk 3)** (2026-08-19): (1) Every leftnav partner portal (artist, press, printer, label, manager, non-profit, publisher) now pins **Settings to the rail bottom** — hoisted out of the scrollable nav into its own hairline-topped foot row above "Powered by GoodTunes". (2) Artist rail order: **Shopify moved to directly after Reports** (before Referrals). (3) The **super-admin press detail page (`/admin/manufacturers/:id`) lost its horizontal tab bar entirely** — it now renders a partner-scoped LEFT RAIL (a new AdminFrame `sidebar` override keeps the shared top bar/banners/theme): back link "Presses", identity block (logo tile → logo editor, identity-icon chip, name, domain link, "View as this partner" pill), then the press's own rail mirrored exactly from the registry (collapsible groups, one open at a time, active group opens on arrival; White Label shows "Soon"), a quiet hairline then the operator extras **Overview / Contacts / Analytics with NO "Admin" group label** (per Bill round 2 — the extras themselves were kept because they're real operator surfaces: profile editing, plant contacts, analytics; **deviation flagged**: the handoff's "Details replaces Overview / Contacts inside Settings" restructure was NOT built — those bodies are press-portal modules we don't own the redesign for yet), and Settings pinned to the rail bottom. Refresh-from-website + Delete moved onto the Overview body (top-right quiet icons). Content pages keep a light "Press name › Section" breadcrumb; legacy `?tab=` deep links unchanged. **Deferred, flagged for Ruby**: the press rail's format-first Components restructure (Vinyl · CD · Cassette · Pricing with per-component pages as in-page segmented tabs) and the new "Details" rail item — both need new page bodies, not just rail re-plumbing; today the Components group keeps the existing per-component children on both the portal and the god view. Suite green (1487/1487), design-lint re-baselined (6881; new lines are rail-canon sizes). Needs a republish.
- **GOLDENROD raw-template flow + wall art flags built to Ruby's Aug 19 restructure handoff** (2026-08-19, visual-only per gogoods's ruling): the artist Test page's pre-art state is now the full SceneRawTemplate — "Add your art. / Straight onto the template." headline, Add-panel-images vs Upload-a-full-template entry chips, raw "Pending — no art uploaded yet" status card, file header card with No-art-yet/Not-tested chips and hover-rename (dead-end), view toolbar (Full Template · Back · Front · Spine chips with honest locks, history circle, faint proof circle, locked Replace pill, ••• File-history/Download-raw menu), and panel viewports cropped from the REAL rendered template sheet (red-linework fallback when the press has no template PDF). Full-template drops/dialog run the real upload + measured check (progress bar inside the dialog); per-panel upload confirms close quietly — no seat, no fake state. The hasArt toolbar's Replace pill now opens Ruby's chooser (Full template → real flow; A single panel → quiet dead-end; supersedes the Aug 18 one-click ruling) plus the same ••• menu. Wall: money flag moved top-LEFT, "Needs print-ready art" flag stacks under it (server-derived: vinyl release with no completed-art upload), badge line leads with "No print art yet — start from the blank template", and every cover gets the hover ••• menu (Use Front/Back panel, Upload your own thumbnail — all quiet dead-ends). **Deviations flagged for Ruby**: (1) PANEL_REGIONS crop fractions are the handoff's MRP-12″ values used as a visual approximation for every template until per-template regions exist; (2) OverlayChipRow/StitchedPreview/transparency toggle NOT ported — they're unreachable before art seats, deferred until per-panel seating is real; (3) needsArt wall cards still open the release page (not straight into the raw scene) since Assets is the real entry; (4) history circle stays testid `button-history` per the chunk-1 note. Needs a republish.
- **Artist Test page delta built to Ruby's Aug 18 handoff (rev in `handoff/artist-template-test`)** (2026-08-19): permanent toolbar architecture is in — the same buttons in every state: circular History icon revealing the File-history POPOVER (right-anchored panel, same grammar as the press Test/Certify surface; the standing history card is gone), a "Replace" pill that opens the upload card AND shows the drag-drop box in one click (Cancel backs out; locked slots show the disabled "Upload locked" pill in the toolbar instead), and the Download-test-proof circle that expands left on hover (disabled-faint before there's art). New pre-upload state: a known slot with no upload renders "Pending — not tested yet" (Clock, zero blue) plus the raw template (real template PDF when on file; otherwise the handoff's red-linework Front/Back panels with per-panel Choose-file / Paste-URL upload menus wired to the real measured check). One deviation flagged for Ruby: the handoff's history button testid `button-layers` collides with the viewer's real Layers button — history uses `button-history`. Needs a republish.
- **Viryl file-hygiene false positive fixed** (2026-08-18): live-test flagged "Template layers still present" on Andrew's all-vector test even though the GT layers were empty — flattening leaves the layer *entries* behind with no content on them. Hygiene now fails only GT layers that actually paint content; empty leftover entries pass with a note. Verified against Andrew's VIRYL_TEST file (all art on "Layer 1", 10 empty GT entries). Needs a republish.
- **Press-panel master downloads fixed + vector-only art passes resolution + cut-rect parity restored** (2026-08-18): (1) "Download all masters" / View Masters saved 0-byte files — masters are private objects and the bare links carried no auth; downloads now stream through a new authed route (operators + the album's press). (2) Vector-only art files (no raster images) now PASS the Image-resolution check on both the press live-test and artist check — pixels aren't required (Viryl). (3) Re-threaded the CALIFORNIALAND cut-rect fix into the artist check route after the #3184 merge (server-stored press rect only — client rects deliberately not trusted). Needs a republish.
- **Pasted art links now mirror into our storage + get a real preview** (Niina's blank Center-labels viewer, 2026-08-18): when a completed-art file arrives as a pasted https link (the press panel's paste path), the check route now downloads it into our object storage (standing mirror rule) and rasterizes the same trim-area preview a direct upload gets. Before, pasted files were stored as external URLs with no preview, so the artist Test page showed a blank "No preview could be generated" viewer for press-pasted files. Direct uploads were always fine — Niina's own re-upload previewed correctly. Needs a republish.
- **Artist art-test false flags fixed — cut-rect parity with the press live-test** (Bill's CALIFORNIALAND jacket, 2026-08-18): the artist Test page's check route now measures content bleed against the template's real cut rectangle, the same way the press Live-test page does. Before, full-artboard exports (files whose PDF Trim box equals the Bleed box) false-flagged "Bleed ≈0" and a low image-resolution estimate on the artist page even though the identical file passed on the press page. The press live-test now also saves the template's client-read cut rectangle onto the template spec (new `measured_cut_rect_inches` column, both DBs) so future artist checks reuse it automatically; the artist check route additionally accepts a client-supplied cut rect (same contract as the press route). Note: existing templates (incl. MRP 12″ single jacket) have no stored cut rect yet — it seeds itself the first time a press live-test runs on that slot after this ships. Needs a republish.
- **Artist Test page: upload/replace, file history, production lock** (Ruby's Aug 17 handoff, built 2026-08-18): the artist template-test page's "Upload another file" is now a real replace flow (quiet hairline pill + helper line, direct upload → the same measured checks the press panel runs → fresh checks card); a new "File history" card lists every upload and press download (filename, dimensions, timestamp, result as word + icon: Current · Passed · Replaced · Downloaded by press — append-only, nothing deleted, new `completed_template_file_events` table on both DBs); and when the press downloads a file for production the slot LOCKS — the artist's pill flips to a disabled "Upload locked" (Lock icon + word) with a quiet banner naming the press + download time, enforced server-side (409 on artist replace; press downloads now route through a tracked endpoint that logs + locks; operator downloads never lock; only the press will unlock — that control is a later handoff). Needs a republish.
- **Crisp GT overlays at any template zoom** (task #3162, applied manually 2026-08-18): the press-template Live-test GT overlays no longer blur or alias when the operator zooms — overlay geometry re-rasterizes at the effective zoom (new `cropDimensions.ts` helper with unit tests, `PressTemplateLiveTest.tsx` wiring), so Cut/Bleed/Safe traces stay crisp at any magnification. Needs a republish.
- **Canon: no boxed page headers + range toolbar only on metrics screens** (Ruby's corrections via gogoods, 2026-08-18): ratified both rules in `docs/apple-canon.md` ("Page headers & the range toolbar") and applied them to the artist portal — the shell's boxed section-title strip is gone on every tab (each page's in-content two-tone heading IS the title; Orders/Referrals/Shopify gained their own two-tone headings since they had none), and the Today/Last-7-days/Compare toolbar is removed from Releases, Orders, Referrals, Shopify and Settings (Dashboard keeps its in-content range pills, Reports gained an in-content range switcher beside its sub-tabs so its analytics panes are never pinned to a hidden window). Orders now lists all-time (an explicit all-time window is pinned so the server's 30-day default can't silently hide older orders now that there's no picker). Needs a republish.
- **/release-info standalone page** (gogoods, 2026-08-17; content updated to rev 5 on 2026-08-18): Bill's self-contained GoodTunes Timelines HTML now serves at `/release-info` on every host (e.g. get.goodtunes.music/release-info) — same pattern as `/investors` and `/switchbridge` (file in `server/assets/release-info.html`, unlinked, noindex header + injected meta, no-store so a re-upload takes effect immediately, wins over the SPA fallback). Needs a republish to go live.
- **Two-up label template preview fixed** (task agent, applied by hand 2026-08-17): multi-up label pages (e.g. Hellbender's two-up 8.5 × 4.25 in center-label sheet) render at true aspect with rectangular rings — no fabricated single-die circle, no spindle hole, dieline guides ignored for all label specs; the Live-test overlay only draws round when a single curve subpath spans (≥80% of) the merged layer bbox. Platform merge for task #3156 exhausted its budget, so the commit was cherry-picked onto main directly and post-merge run — the task card should be **cancelled, not re-applied**. Needs a republish.
- **Operator detail pages now mirror the partner portals ("one body, two chromes")** (gogoods, 2026-08-17): the super-admin press page (`/admin/manufacturers/:id`) and artist People page (`/admin/people/:id` for artist-shaped people) now render the *same portal modules* the partner sees, in the same order, followed by a hairline divider and the operator-only extras. Press side: the strip is built from `modulesForRole("press")` (grouped tabs with sub-pills; White Label shows "Soon" in god view), body rendered by the portal's own exported `PressTabBody`; operator extras Overview (now hosting the relocated GoodDeed printing editor), Contacts, Analytics sit past the divider; legacy `?tab=` deep links map onto the new shape (e.g. `catalog&section=specs` → specs). Artist side: Dashboard / Releases / Orders / Reports / Referrals / Shopify / Settings come from the portal's exported `ArtistTabBody` with god-view `?personId=` threaded through every read; extras past the divider keep Overview, Cover, Members, GoodTunes® Releases (the operator panel with Add Album — intentionally kept beside the mirror wall), Streaming, Gear, Splits, Payouts, Permissions; old `?tab=dashboard` deep links land on the mirror Dashboard, `?range=` re-seeds on later navigations too. The Referrals mirror hides the invite panel (invites are artist-session actions; the operator sees the artist's numbers with a quiet note instead of a dead form). Contact-shaped people and press-mode pages are unchanged. Both suites green (1455/1455), design-lint re-baselined. Needs a republish.
- **Press Estimates & Packages shipped (Ruby handoff `handoff/press-estimates-packages`)** (gogoods, 2026-08-17): the press portal gains a new "Create" rail group with **Estimates** and **Packages** — Ruby's four walk files copied verbatim (index pages + the full quote/package builders with the layered vinyl renderer), only the MOCK consts swapped for real data: the client roster comes off the press's People, pricing overlays per-press component prices where the catalog carries them (labels/stickers; the rest keep the handoff's demo defaults), and saves persist to the new `press_estimates` table (one table, `kind` estimate|package; full builder state in payload jsonb). The server owns the human-facing estimate number (press initials + MMDDYY + per-day sequence, e.g. `MRP-081726-01`, minted under a per-press lock with a unique backstop). Deep-linkable `?tab=estimates&view=builder&estimate=<id>`; scoped reads + editor-gated writes (Staff sees read-only builders); dead-end CTAs are quiet no-ops per the handoff README. Both themes verified at 1280 (light + charcoal dark). Suite green (1458/1458), schema drift clean (143 tables both DBs). Needs a republish.
- **Artist portal restructured to Ruby's handoff (`handoff/artist-portal-restructure`)** (gogoods, 2026-08-17): the artist portal now runs the new seven-item shape — Dashboard, Releases (the wall of release cards), Orders, Reports, Referrals, Shopify, Settings. Audience / Acquisition / Buyers moved inside the Reports hub ("Reports. How am I doing?" — Audience · Acquisition · Buyers · Payments · Earnings on `?rtab=`; stale `?tab=audience`-style deep links land on the hub); the "Kept separate on purpose" never-netted payments/earnings framing is live with real ledger data. Clicking a release opens the new five-tab release view (Dashboard / Details / Assets / Store / Payments) — presentational code copied verbatim from the walk files with only the MOCK consts swapped for real data: real formats + statuses, real sales/plays/GoodDeeds numbers, real vinyl-art blocks off the completed print-file scan (each block links into the Artist Template Test page), real store mode + share links + go-live checklist, real payment milestones. The old embedded operator album view is removed from the artist portal (delete-first per the handoff); dead-end CTAs are quiet no-ops. Settings gains Team + Connections (Shopify, payout status). New scoped server reads back it all (`/api/artist/wall`, `/api/artist/albums/:id/portal`, `/api/artist/ledgers`, `/api/artist/team`), artist/label/manager + super-admin god view (`?personId=`) only. Both themes verified (light + charcoal dark). Suite green (1458/1458), schema drift clean. Needs a republish (also still pending: the Artist Template Test page + raster "Physical size" row removal from Aug 16).
- **Artist Template Test page shipped (Ruby handoff `ba099bf`)** (gogoods, 2026-08-16): the artist-side read-only proof view of an art file checked against the press template is live at `/artist/albums/:id/art-test/:componentId` — Ruby's `handoff/artist-template-test/ArtistTemplateTest.tsx` copied verbatim (press-only affordances already removed in the handoff: no GT-layers row, no Save/Cancel, no trail line, zero filled blue), only the MOCK consts swapped for real data from the album's completed print-file scan (per Ruby's ruling — the artist's own art, not the press's internal run history; the existing completed-art read already admits the album's own artist/label partners, so no new server route). The check card shows the real per-check rows word+icon (Pass / Flagged / Unverified / Advisory — real statuses beyond the handoff's pass|na keep the quiet grammar with an honest word, flagged for Ruby's next round); the mock Sun/Moon toggle was replaced by the operator Light/Dark/System preference per the handoff README. Entry: direct URL plus a clearly-marked temporary "Open test view (temporary)" link on each completed-art card (Physical → Art) until the restructured Assets tab (separate upcoming handoff) becomes the real entry point. Still decorative this round, next wiring pass: overlay chips (must drive the press live-test overlay engine, never a reimplementation), layers button, Download test proof, Try another file, upload-in-progress state. Needs a republish.
- **Big art files no longer fail the live inspection with "unavailable"** (gogoods, 2026-08-16): gogoods' 59 MB CMYK jacket JPEG showed "Ink + ppi inspection unavailable" on production — the deployment edge rejects large request bodies (413) before they ever reach the server, so the measurement request silently never arrived (dev has no such cap, which is why the same file measured fine there). Files over 20 MB now upload straight to object storage with the signed-PUT flow (same live progress bar) and the inspection endpoint reads the bytes back from storage — same checks, same results, any size up to the 300 MB cap; the uploaded art object stays private. Small files keep the direct path. Applies to both JPEG/PNG and PDF art. Verified live: gogoods' JPEG now measures on production (CMYK pass, ≈300 PPI pass).
- **CMYK JPEG art now shows in the preview pane** (gogoods, 2026-08-16): after the measurement fix went live, the JPEG measured but the art frame rendered blank — browsers can't reliably decode CMYK JPEGs, so the client-side preview image failed exactly on the files that pass the ink check. The inspection endpoint now returns a resized sRGB preview alongside the check rows and the page swaps it into the overlay, so raster art always renders color-correct. PDFs keep their existing client-side render. Also, the File hygiene row for rasters now passes ("flat pixels can't carry template layers") instead of the misleading "Layer check needs a PDF" — drag-and-drop JPGs are first-class finals.
- **Raster overlay un-squished + Artboard size measured without a PPI tag** (gogoods, 2026-08-16): the overlay was stretch-filling the raster into the anchor box (squished JPG) — the server scan now returns pixel dims and the client contain-fits at the image's own aspect. And with no PPI tag, the Artboard size row now measures the frame's *proportions* against the slot's artboard (1% tolerance, orientation-aware) instead of "Not measured"; gogoods' jacket JPG now shows 4 measured passes. Physical size stays raster-N/A by nature (pixels carry no mm), with clearer wording pointing at the Artboard/resolution rows.
- **PDF image-resolution row is honest about being a worst-case estimate** (gogoods, 2026-08-16): the scanner reads embedded image pixel dims but not placement, so PPI assumed full-artboard placement — gogoods' jacket PDF "failed" at ≈282 worst-case when its actual bleed-size placement is ≈345. A sub-floor estimate is now "Not measured / can't confirm" instead of a hard Fail; a passing worst case stays a real Pass. And per gogoods' insight (the art visibly sits at the template's bleed frame), the client now sends the loaded template's GT Bleed box along with the inspection, and the server measures image PPI against that intended footprint too: worst-case ≥ floor = certain Pass; bleed-frame ≥ floor = Pass (noting the worst case); below floor even at the bleed frame = Fail; no bleed box known = "can't confirm". gogoods' jacket PDF now passes at ≈363 PPI at the bleed frame. Applies to the 300 PPI and 1-bit 800 PPI rows on the live test page; the prepress validator already labeled its estimates. Needs a republish.
- **Panel views keep the Bleed and Cut toggles** (gogoods, 2026-08-16): the cropped Front/Back/Spine views filtered overlay toggles to panel-named zones only, so the template-wide Bleed and Cut lines vanished from the dropdown. They now stay toggleable in every view. Needs a republish.
- **Full-artboard rasters preview edge-to-edge** (gogoods, 2026-08-16): a JPG whose proportions match the full template sheet now overlays the whole template (edge-to-edge), instead of shrinking inside the GT Bleed box; bleed-proportioned art still anchors to the bleed frame (closest-aspect-wins: full-sheet placement only when the sheet is both within 2% and a strictly better aspect match than the bleed anchor). Needs a republish.
- **Failed ink/resolution measurement is honest + retryable** (gogoods, 2026-08-16): a network hiccup during the server scan used to leave "inspection unavailable", a misleading "Pass!" header, and (for CMYK JPGs) a blank preview until a full refresh. The header now says the measurement didn’t finish, the verdict logs as "Incomplete", and a "Re-run measurement" button retries the same file without re-picking. Needs a republish.
- **"Safety — Not measured" row removed from the checklist** (gogoods, 2026-08-16): safety is a human eyeball check that lives in the overlay toggles; the results list now holds only what the system actually measures. Needs a republish.
- **Results stay open when the measurement finishes** (gogoods, 2026-08-16): the checks banner used to auto-fold on a clean pass, closing the list mid-read. It now stays open and replays a gentle settle + ring pulse in the verdict color as the "it’s done" cue — the viewer folds it when they choose. Needs a republish.
- **Shelf "Failed" after a clean live pass — server Bleed check fixed** (gogoods, 2026-08-16): the certification run hard-failed Bleed ("≈0\" from the file's own PDF bleed box — no certified template line"). Three-part fix: (1) the live-test client now sends the template's own bleed line (read from its GT Bleed/Cut layer boxes) with the server test; the server uses it when the spec has no stored line and persists it as the measured line. (2) Rendered-content bleed measurement now also overrides PDF boxes that FAIL the line — full-artboard exports stamp TrimBox==BleedBox (declaring 0\" bleed) while the art physically covers the sheet. (3) The live-test client also sends the template's cut rectangle (from its GT layers); sheet-with-margins templates (MRP jackets) measure rendered content against that verified geometry — without it the check stays honestly "unmeasured", never inferred. (4) Per-side bleed-band coverage gate: isolated crop marks touching the page edges can no longer fake bleed via the global bounding box. (5) Raster art (JPG/PNG) no longer shows a permanent "Physical size — Not measured" row on the live-test checks — pixels can't carry physical mm; Artboard size + Image resolution cover slot fit. (6) View mode: opening a CERTIFIED template from the shelf auto-loads the certifying test run's saved art through the same live pipeline (overlay + measured checks re-hydrate), read-only — Close changes nothing, "Try another file" still available. New scope-checked run-file download route serves the stored art. Verified in dev against the real CALIFORNIALAND jacket PDF: Bleed now passes from rendered content (≈0.438\" past trim); validator tests 110/110. Needs a republish; re-run Save on the jacket slot to replace the failed run.
- **Dedicated-press branding fixed on the artist package page + Pressing plant tile** (gogoods, 2026-08-16): an artist with a dedicated press (e.g. TommyGunn → Memphis) saw the generic product-mark icon (reads as the PMP logo) on the jacket in the artist-view "Design your package" page when the press hadn't uploaded a default jacket — the builder now falls back to the same bundled per-press grayscale artwork the admin surfaces use (resolved by press domain), so a Memphis album never wears another press's mark; the disc center label also gained a fallback to the invited press's own logo. Separately, the People → Overview "Pressing plant" tile rendered the raw press logo (dark artwork) black-on-dark — it now renders the mark as white on the dark admin theme, per the press-mark rule. Also fixed a latent hook-order crash in that panel. Needs a republish.
- **JPGs are now measured in the live art check, not "Visual only"** (gogoods, 2026-08-16): MRP wants art-only files at the proper artboard size — no template — so a correct JPG is a legitimate final. The Template. Test. Certify. live check now measures JPEG/PNG uploads on the server: color space (CMYK JPEG passes; RGB fails; PNG always fails color since the format can't carry CMYK — we never silently convert), physical size from pixels + the embedded PPI tag against the slot's artboard (finished + bleed, either orientation, bigger-trims-away grace), resolution against the slot's PPI floor (default 300), and bleed by frame coverage (a raster has no trim box, so a frame covering the full artboard carries its bleed by definition — with an honest "confirmed at prepress" note when the file has no PPI tag). PDFs keep the deeper trim/bleed-box measurement. Needs a republish.
- **Art Blocks brief amended per Bill's corrections memo; dual-theme ruling ratified** (Bill via gogoods, 2026-08-16): the Art Blocks builder brief (`handoff/art-blocks/README.md`) is updated before Ruby's pass — "Album"→"Release" and "SKUs"→"Variants" in artist-facing language; the surface lives at Release → format tab → Art section (matching Ruby's CALIFORNIALAND mock, no format picker — format is fixed at draft creation); the surface is now state-aware (Draft: no press name, generic baseline geometry + strictest pool thresholds, "Presses require CMYK" copy, "Save artwork" CTA; Converted: re-verify against the assigned press, "your press" copy, send-to-press CTA); press-assignment precedence chain written in (referral origin locks → MRP default → super-admin reassign; blind comparison future layer); spec model split into thresholds (checked once at the strictest pool value, portable forever) vs geometry (re-verifies at assignment/switch, mismatches name only affected blocks); empty states use a neutral icon, never a blurred artist image. **Theming ruling:** the "partner portals are light-only" canon is retired — all operator/partner surfaces are theme-aware with light + charcoal-dark modes (never navy; fan navy shell untouched); `docs/design-system.md` and `handoff/style-guide/apple-canon.md` are amended; Ruby defaults to dark in mocks and ships both token sets. Remaining work items (not yet done): migrate hardcoded-slate portal screens onto theme tokens and sweep every portal screen in both themes at 1440/1024/768.
- **Live ink check now measures bleed too** (gogoods, 2026-08-16): the Template. Test. Certify. live banner could say "3 of 3 passed" while the full server test failed on bleed — confusing pass-then-Failed whiplash on the tile. The live art inspection now measures bleed from the art file's trim/bleed boxes against the slot's certified template bleed line (same canon as the full test): meets the line = pass row, short = fail row right in the banner, no line on the slot = informational only. Needs a republish.
- **History & tests sheet now shows server-logged runs** (gogoods, 2026-08-16): the live-test History & tests panel said "No art files tested" even when real test runs were recorded on the server (it only listed the current browser session's tests). It now merges the server's run log — current revision and superseded revisions alike — with dates and Pass/Flagged verdicts. Needs a republish.
- **Live ink/PPI check: real-time progress bar + rename fix** (gogoods, 2026-08-16): the Template. Test. Certify. verdict banner now shows a thin live progress bar while the art PDF streams up ("Uploading art for ink & resolution check… NN%"), then pulses while the server measures — so a room full of press folks can watch the CMYK/300ppi verdict land in near real time. Separately fixed: a template renamed during its FIRST attach to a slot lost the rename on Save (the display-name write only ran on Replace saves); renames now persist on first attach and on the multi-option confirm path too. Needs a republish.
- **Live ink/PPI inspection: 5-minute upload window** (gogoods, 2026-08-16): the Template. Test. Certify. "Color & resolution" check was falling back to "inspection unavailable — runs again at prepress" on production because the server's 60-second scan cap also covered the file *upload* — a jacket-spread art PDF over a home connection outlives a minute. The cap is now 5 minutes, a genuine timeout says so plainly ("The upload took too long to inspect…"), and the server logs the failure reason so we can see why a scan bailed. Needs a republish to reach production.
- **Canon press rail + controls-row fix (handoffs `f9adddf` + `fd92b0f`)** (Bill, 2026-08-16): the press portal's left rail now follows the canon — the "Catalog" group is renamed **"Product Specs"** (GoodTunes Packages, GoodDeed Certificates, Specs, Templates — Templates keeps the LayoutTemplate glyph per Bill), the **Components** group (Vinyl, Jackets, Inner Sleeves, Center Labels, Inserts, Stickers, Pricing) stays collapsible with the rotating chevron, and **White Label is promoted to top-level above Settings** with the press-facing **"Request"** pill (the super-admin god view shows "Soon" instead). The group holding the active page opens on first load, others start closed (existing one-open-at-a-time accordion behavior kept — see note for Bill below). The Templates breadcrumb follows: "Product Specs · Templates". Other portals (artist/label/manager/printer) keep their "Catalog" group label — the rename is press-only. The controls-row divider fix (`fd92b0f`) was already live in our index — the divider has ridden with the size pills since the Aug 15 round, so no double hairline on CD/Cassette/Stickers. Note for Bill: our rail persists the open group across visits and opens at most one group at a time (the AdminFrame accordion); the canon only specifies first-load state, so we kept the accordion — say the word if independent multi-open groups are wanted. Needs a republish to reach production.
- **Live-test correction round applied (Addendum 6, handoff `49df060`)** (gogoods, 2026-08-15): the live-test page now matches Ruby's correction round while keeping the live ink/PPI wiring. The step rail is gone and the heading always reads "Template. Test. Certify."; the header name row carries the actions — a Save/Close combo (filled blue **Save** only when something changed, otherwise a quiet **Close**) with Cancel appearing beside it only when dirty — and the view chips (Full Template / Back / Front / Spine) moved up into the header card. The page's one status voice is a banner above the preview: **Pending — not certified yet** (quiet amber ring with a point of light orbiting the border, expandable to "No art files tested yet" + a Choose-an-art-file action) when an uncertified template has no test, becoming the **Pass / Fail / Visual only** results banner once an art file lands (arrival settle + one soft ring pulse in the verdict color, collapsible check rows, "N of M passed" inline; shows a neutral "Measuring ink & resolution…" state while the live scan runs — never claims a pass early). Test & Certify pulses a gradated amber fill on the same 3.6s rhythm until certified; layerless PDFs anchor to the full page instead of blanking; the fine-print line truncates with full text on hover; the Layers popover type sizes came up to the handoff's. Templates index: the Pending/caution amber deepened for legibility (`#b45309` light / `#f59e0b` dark, same hue as the Pending ring), and the ⓘ popover now also covers **Failed** tiles ("What happened?" — last test failed, open to test a fixed file). Needs a republish to reach production.
- **Live ink + resolution checks on Template. Test. Certify.** (gogoods, 2026-08-15): the "Color & resolution — runs at prepress" row is now measured live. Picking an art PDF streams it to a new read-only inspection endpoint (nothing stored, 300 MB cap, 60 s timeout): CMYK or spot/PMS ink passes (embedded RGB previews ignored), RGB-only fails; image resolution is the same full-artboard lower-bound estimate as certification against the 300 PPI floor (1-bit line art against 800). While the scan runs the header shows "Measuring ink & resolution…" and never claims a pass early. Safety stays a visual check by design — "important elements inside the line" needs human judgment, which is what the Safety overlay toggle is for. Needs a republish to reach production.
- **Standing rule: pasted template links are downloaded, never referenced** (gogoods, 2026-08-15): any external https link pasted as a press template file or certification test file (Dropbox etc.) is now downloaded into our own object storage before it becomes a source of truth — same policy as audio masters — so measurement and preview self-heal never depend on the external link staying alive. Applies to the press-portal attach route, the certification test-run route (which now also refuses non-https non-upload locations), and the operator-side manufacturer template-specs save. Non-PDF links (zips) still get the honest "doesn't point at a PDF" error; a failed save cleans up the downloaded copy so nothing orphans. Needs a republish to reach production; existing Dropbox-linked slots heal the moment someone re-attaches them.
- **Templates shelf: Ruby's five prod deltas resolved** (gogoods, 2026-08-15): Ruby's screenshots were of production, which still runs the pre-Addendum-5 bundle — the white divider, dark/blue no-preview tiles, always-visible fine print, and rest-state blue outlines are already fixed on main and just await Bill's republish. Two genuine bugs fixed on top: (1) production's deploy image was missing the PDF rasterizer (`pdftoppm`) so every template preview render failed and the failure was stored durably (`preview_urls=[]`) — poppler is now in `replit.nix`, and the shelf retries durably-failed spec/run previews once per server instance, so tiles self-heal on next view after republish; (2) the "Needs review" chip is out of the status model — imported-but-unconfirmed revisions now present as **Pending** with their own ⓘ popover ("Imported from your earlier uploads — we couldn't confirm it matches this slot. Open to confirm it, or archive it."). No-preview filled tiles now show a quiet gray mark on the white preview ground (icon-on-dark stays reserved for empty dashed slots). Needs a republish to reach production.
- **Templates page restyle + "require a passing test" policy (Addendum 5)** (Bill, 2026-08-15 — handoff `fa3a812`): the press Templates index tiles are now flat-top GoodStudio cards — template preview (or a quiet icon panel) up top, 16px name, format chip + date, a Pending ⓘ popover ("Attached, not yet certified — it certifies itself when a finished file passes. Open to test."), hover detail ring; the tile rename dialog is gone (renaming lives on the live-test page's name field). The live-test page states its business: breadcrumb shows the template's name, the heading adds "Certify." only while the template is still Pending, an 18px name row carries the Certified badge or a "Not tested" chip plus a "Last test: …" line from the real trail, the Test button reads **Test & certify** with a gentle blue breathing ring until certified, and Save fills blue only when there's something to save. Saving a spec-slot session now also submits the tested art PDF to the server test endpoint and certifies the revision when it passes — the trail is real, not just words. New per-press policy on Settings → Profile: **"Require a passing test before a template goes live"** (Off by default — templates are usable the moment they're saved). When On, a Pending template refuses to measure client completed files: the check returns a clear message naming the press and the Pending template instead of silently using an untested canon. Column `manufacturers.require_certified_templates` is live on dev + prod; the code needs a republish to reach production.
- **Artist Package tab: GoodDeed pricing restacked + Print templates moved to the Art tab** (gogoods, 2026-08-15): the Signed GoodDeed block on the artist Design-your-package builder now stacks per the approved mock — Certificate price on its own full-width row first, then "How many" with the No limit / Limit quantity tiles side by side and each tile's follow-up directly beneath it (the "Typically 25% sell certified — est. N of R" estimate under No limit; the cap stepper under Limit quantity). Nothing sits beside the tiles anymore. Separately, the Print templates section ("Everything your designer needs") moved as-is — same styling, same tiles — from the bottom of the Package tab to the bottom of the Physical → Art tab for artist viewers, next to the Completed Art flow it feeds; operator and press-portal template downloads are unchanged. Needs a republish to reach production.
- **Live-test Save quieted + Replace/History shipped (Addendum 4)** (Bill, 2026-08-15 — handoff `4d078f4`): the save-confirm dialog is gone — header **Save** saves immediately and returns to Templates, staying disabled (dimmed, "No changes to save") until something actually changed; reopened saved templates arrive clean. New header **•••** beside Save: **History & tests** panel (Current with a check badge, Superseded with a history icon, per-revision test trail with word+icon verdicts — including the already-saved trail) and **Replace template…** (supersede-in-place: the new PDF persists on the same row — one tile forever, old revision slides into history with its tests). Tile ••• on the Templates page also gets Replace (above Archive, editors only); saved shelf tiles now render *after* the certified canon so tiles never rearrange — new saves append (oldest-first) with the blue pulse; "Start over" is retired. 2LP (12″ double) specs with a real file now get their own tile beside the 12″ canon (empty legacy shells no longer shadow the live spec), and Bill's approved 2LP center-labels template now reads **Certified** with icon+word. Needs a republish to reach production.
- **Artist URL auto-prefills from the artist's name** (gogoods, 2026-08-15): opening an artist's People page now automatically mints and saves their share slug from their name (first available of `name`, `name-2`, …) when none is set — the field is no longer blank waiting for someone to hit Suggest. Artists only (never pure business contacts); the operator can still edit or clear it, and any manual interaction wins over the in-flight auto-save. Needs a republish to reach production.
- **Templates index reworked to Ruby's Aug 15 handoff** (gogoods, 2026-08-15): the detail sheet is retired — a filled tile opens the live-test instrument directly; each tile gets a hover ••• menu with Archive (confirm dialog, icon+word chip on archived tiles) and Restore; view pills All / Current / Archived (Current default) sit beside the size pills, and slots can be archived too ("Archived — not offered"). Upload button is now a quiet "Create New" ghost pill in the controls row. New browser-local drafts: an in-progress live-test session is snapshotted automatically (template PDF + name, IndexedDB, this computer only); arriving empty-handed with a draft offers "Resume where you left off?" (Resume / Discard draft / X keeps it), a successful Save clears it — Save remains the only act that mints a revision. Dialog canon applied across upload/confirm sheets (primary rightmost, quiet Cancel text, one-line subtext + ⓘ) and to three older dialogs (press branding editor, archive-type dialog, referral-payouts confirm). Two new DB columns (`press_live_templates.archived_at`, `manufacturers.archived_template_slots`) land on prod via post-merge on publish. Needs a republish to reach production.
- **Stale "Failed" chip on replaced templates fixed** (gogoods, 2026-08-15): a slot tile showed "Failed" after a successful new upload because a finished-file test run against the *previous, superseded* file still drove the chip. The tile now only shows Failed when the failing run tested the file that's live right now; replacing the template clears the red chip (the old run stays in history). Needs a republish to reach production.
- **Live-test actions read Cancel / Test / Save** (gogoods, 2026-08-14): nothing persists when a template PDF is picked — the file is only read in the browser; it's uploaded and saved (revision minted in slot mode, shelf save otherwise) exclusively on Save. The header actions now say that plainly, Apple-style: quiet "Cancel" text (leaves without saving), then "Test" and "Save" pills (were "Accept & Test" / "Accept & Save").
- **Templates page loads instantly** (gogoods, 2026-08-14): the press Templates payload no longer downloads/rasterizes PDFs before answering — legacy-upload import and preview rendering now run in the background after the response, and a run whose preview can't render records that failure durably (it was retried by every fresh production instance, which is what made admin.goodtunes.music/vendor?tab=templates take ~12s). Needs a republish to reach production.
- **Dashboard greeting matches the clock** (gogoods, 2026-08-14): the super-admin dashboard header now says Good morning/afternoon/evening from the viewer's own device clock, like the artist and press dashboards — it was hardcoded to "Good morning".
- **Slot uploads route through the live test** (gogoods, 2026-08-14 — Ruby handoff): clicking a dashed empty slot tile (or Replace on a filled tile's detail sheet, or finishing "Create new template") opens the handoff upload sheet in slot mode ("Upload your template", "For: <slot>", Choose PDF / Cancel — the slot already knows its name and component). Choose PDF lands directly in the live-test instrument, titled with the slot name and "Originally <filename>". Accept & Save attaches the file to that slot (new revision minted, old one to history) instead of the saved shelf; if the PDF mentions two options (e.g. both center-hole sizes) a styled consent dialog asks before stamping the note (retry-safe — the attach is already done). The old upload modal is retired; its paste-a-URL attach path is gone from this surface (flagged), and its archive action moved to a small "Or archive this file" link on the detail sheet. Header "Upload a template" (no-slot mode) still saves to the shelf.
- **Templates pages re-diffed to the live-test handoff** (Bill, 2026-08-15 — handoff/press-template-live-test): the static template detail page (blank dashed page + measured-geometry list) and the separate finished-file Test page are retired. Opening a template (`?template=<id>`) now lands on the live-test instrument: the stored canon PDF is streamed back through a new same-origin route (`GET /api/press/:id/templates/:specId/file`, SSRF-guarded proxy for external links) and its GT layers render as visible overlays with the Template → Art file → Results testing flow. Index tiles now open the handoff's detail sheet (preview, name, code · rev, status, revision history) with the blue **Replace template** action (editor-gated; replace = new revision, old moves to history, never deleted), plus Open live test and Close. Measured-geometry extraction wiring is unchanged and keeps feeding tile statuses/dims. View-as confirmed to route through the press's own components (no separate implementation existed). Server-side measurement, revisions, and certification data untouched.
- **Press Templates live end-to-end test** (Bill, 2026-08-14 — handoff/press-template-live-test): the press Templates page gained an "Upload a template" button next to the format tabs. Picking a PDF (with an optional name + component tag: Jacket / Sleeve / Labels / Booklet / Other) opens the new Live test page: the browser reads the PDF's GT layers directly (pdf.js — dieline detection, clip-skip, circle/frame-hole handling), renders the template study, and lets the press drop real art files on it to check fit visually, logging a per-art-file verdict trail (Pass / Flagged / Visual only). "Accept & Save" uploads the PDF, shrinks the page-1 preview, and lands the template on a saved shelf on the Templates index (12″ Vinyl section) — circular preview, measured mm size, GT layer count, saved date, and "N art files tested". Reopening a shelf tile pulls the stored PDF back into the live test and appends new tests to the same trail. Editor-gated writes; two new tables `press_live_templates` + `press_live_template_tests` (dev + prod migrated, post-merge guarded). Existing slot/spec/certification pipeline untouched. Full suite green (1456/1456).
- **/switchbridge standalone page** (gogoods, 2026-08-14): Bill's self-contained Switchbridge loop animation HTML is served at `/switchbridge` on the app hosts (e.g. my.goodtunes.music/switchbridge) — unlinked, noindex, same serving pattern as /investors. Note: the bare goodtunes.music apex is the Webflow marketing site, so the page lives on the app subdomains, not the apex.
- **Redemption-email preview + test send** — the album Email appearance panel now has a "Preview email" button that renders the real "Your music is ready" email (per-format hero graphics, button color, subject line) in a popup, using the exact same builder as the production send; a "Send me a test" button in the popup emails it (subject prefixed "[Test]", placeholder link) to the logged-in admin's own address via Resend, so it also shows in the Resend log. Test sends only work where the mail key is configured (production). Out-of-scope partners can't preview albums they can't load.
- Per-album redemption email branding (Task #3120, 2026-08-13): the "Your GoodTunes music is ready" email fans get after a Shopify purchase is now branded per release — a rounded-rectangle hero image at the top (a format-matched custom graphic for vinyl/CD/cassette orders when configured, else an album-wide custom graphic, else the album cover automatically) and an artist-picked "Get my music" button color (the platform blue gradient stays the default). Artists and operators configure both from a new "Email appearance" panel on the album's Overview tab (color picker + upload slots for a default graphic and per-format overrides), riding the standard edit permissions/approval flow. Albums with no settings send exactly as before apart from the automatic cover-art hero; image-blocked clients still read fine (alt text), and Outlook keeps the bulletproof VML button with the custom flat fill. Niina Soleil's CALIFORNIALAND is the first real configuration: vinyl buyers see her red-vinyl + GoodDeed + phone lifestyle shot, CD buyers the CD + phone scene, button in her palette red (#D0342C).
- Shopify custom-app bridge for Niina's store (gogoods, 2026-08-13): the public GoodTunes Shopify app is blocked from installing on live stores while it sits in Shopify's App Store review ("This app is under review" on the install screen). The backend now supports a second, custom-distribution Partner-Dashboard app as a bridge: installs for bridge-listed stores (currently Niina's 71gsth-ev.myshopify.com) auto-route through the custom app — the existing connect buttons and copied install links just work with no URL editing (`&appCred=public` force-overrides for the eventual move-back). An install under the bridge runs the whole OAuth round-trip (state signing, HMAC, token exchange, refresh) under the custom app's credentials, the connected store row remembers which app installed it, and webhook/extension-token verification accepts either app's signature. Awaiting the custom app's API key/secret (SHOPIFY_CUSTOM_API_KEY / SHOPIFY_CUSTOM_API_SECRET) to go live; when the public app clears review, a normal reinstall flips the store back.
- Artist Completed Art access restored (gogoods, 2026-08-13): the artist Physical → Art tab's Completed Art flow (download templates, drag-and-drop the finished PDF, automatic vetting + trim preview) had gone blank — the press-portal access gate added in Task #2725 accidentally 403'd the album's own artist. The gate now also admits the album's own artist/label partners (dual-scope membership check); an unrelated artist stays blocked, and override/remove remain operator-only (the override affordance is now hidden for artist viewers too).
- Artist package builder: stray caption removed + type grid collapses on any pick (gogoods, 2026-08-13): the duplicate "Translucent · T01 Ruby" text under the jacket is gone (the stage caption under the jacket already says it); and the "Pick your vinyl" section in the artist Design-your-package builder folds into the canon one-line summary row (disc thumb, type name, color count, blue "Change" pill — same collapsible-picker tool as the press Vinyl-colors page). It opens only via "Change", and picking a type OR a color collapses it right back (the color row below always shows the new type's default color, so nothing goes colorless). Previously it stayed expanded until a color was explicitly picked, which read as "not collapsing" after switching types.
- Operators can hand-enter fold and safety lines (Task #3101, 2026-08-13, manually applied after the merge card bounced): when a template's PDF carries no readable dieline guides, press editors can enter fold/score positions (X and Y) and a safety inset by hand on the template detail; press-entered values win over measured guides in the printed-areas study and survive template replaces (they're product geometry, not file facts). Idempotent columns added to dev + prod.
- Expired signup codes now say so (2026-08-13): a fan who enters a 6-digit verification code after its 15-minute window used to get "That code didn't match — check the latest email and try again", sending them back to a stale email forever (real fan report via tap-to-report, code entered 2h20m after send). All three code-confirm endpoints (signup verify, real-email confirm, OAuth claim confirm) now return "That code has expired — request a new one" when every outstanding code has lapsed or burned its attempts; a genuinely wrong code keeps the old message.
- Test page matches Ruby's certification mockup (Task #3099, 2026-08-13): both study cards on the press-portal template Test page now always show rendered artwork under the zone rings — template PDF pages are rasterized (new `press_template_specs.preview_urls`, rendered on attach + lazily backfilled on view; replace/archive resets them), and test-run previews are lazily backfilled for historical runs from the stored file. A genuine rasterize failure keeps the two-column grid with an honest "No preview rendered" panel — the page never collapses to one column. Verified side-by-side against the handoff in light and dark press shells (jacket single-page + 4-page labels with Side A/B).
- Artist "Releases" layer + draft flow — design brief for Ruby (Bill, 2026-08-12, build deferred until mocks): before building the Releases container in the artist portal, the flow goes to Ruby first. Brief published at `docs/releases-draft-flow-design-brief.md`: a Release layer above albums in the artist Catalog (name-only New Release → release detail with lanes → Create Draft picks Vinyl/CD/Cassette → lands in the existing Build-a-Quote builder tied to the invited press), derived rollup badges only (no stored status; e.g. "Digital live · Vinyl draft", "Sunset"), auto-save naming "<Release name> — <Format>", and a "pricing pending" treatment everywhere the current mock shows $0.00. Ruby already has the core Build-a-Quote components — this brief covers the layer above it plus the pending/auto-save states. Build resumes once the mocks land.
- Certification passes unused spot-color swatches (Task #3069, 2026-08-12, manually applied after the merge card bounced): the completed-art checker now detects whether a spot color is actually USED (content-stream selections with lexical cleaning, process aliases, spot-bearing image XObjects) instead of failing on its mere presence in the swatch list; unresolved/ambiguous selections and every scan/collection cap force a reason-coded conservative fallback that says honestly whether it's a file problem or a GoodTunes scanner limitation; redacted telemetry; Pantone/color-summary rows updated. 90/90 validator tests green.
- Portal top-bar avatar fills the circle (2026-08-12): the partner-portal header's small round avatar now crops photos edge-to-edge (object-cover, padding only on the fallback icon) instead of letterboxing portrait photos inside the circle — matches the larger identity avatar's behavior. Seen on Niina Soleil's artist portal.
- EasyPost shipping labels for the cert signing round-trip (Task #3091, 2026-08-12): operators buy — on an explicit action — an outbound UPS label (printer → artist/manager signing address) plus a prepaid `is_return` label back to the next destination (hologram/shrinkwrap vendor when assigned, else the routed fulfillment partner), via a fetch-based EasyPost client on the GoodTunes UPS carrier account (`EASYPOST_API_KEY` + optional `EASYPOST_UPS_CARRIER_ACCOUNT_ID`; **key not yet set — endpoint returns a clear 503 until Bill/gogoods add it**). Purchases are advisory-locked and idempotent (partial-failure safe: outbound persists, retry only buys the return leg); failures surface EasyPost/carrier messages verbatim; local pickup is an honest skip/undo. Snapshot lives in `albums.cert_batch_shipping_labels` (jsonb; idempotent DDL in post-merge on both DBs); return tracking mirrors into the Task #3075 return-label fields and fires the fulfillment heads-up email once. Printer portal shows both label PDFs beside the cert print queue; fulfillment portal gains `GET /api/fulfillment/:id/cert-batches` (return tracking + batch stage). Printer-driven flow per Bill: operator pre-saves the artist signing address ("pending"), then the PRINTER enters box size/weight in their portal and one click creates both labels (POST /api/printer/:id/shipping-labels/:albumId, same idempotent core + heads-up mirror); operator direct-buy stays as backup. Open (not built): printer payment, shipping-cost/urgency threshold (v1 buys cheapest UPS), per-printer no-A4 note. Interim UI minimal — Ruby design brief at docs/cert-batch-shipping-labels-design-brief.md.
- Fulfillment partners quote the GoodDeed hologram/shrinkwrap/ship leg (Task #3075, 2026-08-12, manually applied after the merge card bounced): fulfillment portals gain a GoodDeed service pricing tab (jsonb ladder, mirroring the printer per-service ladders); saving a cert batch's return label fires a delivery-verified one-shot inbound heads-up email to the fulfillment partner (atomic claim, released on zero-recipient/send-failure with operator-visible notifyProblem); operators get a per-leg batch routing view (print / hologram / fulfillment) mirroring real order routing — splits → album override → ordered live-partner fallback — with do-it-all presses collapsed to one row. Idempotent DDL applied to both DBs; DB-backed concurrency/precedence/dispatch tests green (12/12).
- Template preview reads the uploaded PDF, not label defaults (Task #3060, 2026-08-12, manually applied after the merge card bounced): the press-template detail preview now reflects the MEASURED PDF facts and the product type — jackets render true-aspect rectangular panels, only labels stay 1:1 circles with the Hole ring, gatefold spines get fold lines, dims/pages/bleed are measured-first with honest mm/page captions, and foldable variants without geometry show a pending-spec note. StudySpec builder factored into buildStudySpec.ts with its own tests (8 green).
- GoodTunes Packages swatch thumbnails wear the press label (Bill, 2026-08-12): the color-swatch balls on the packages page (Build colors grid, type picker, Add-your-vinyl grid, package builder) now overlay the press's own rendered label (brand color + white mark) over photo-based swatches — the source photos carried their own labels, so some thumbnails showed a foreign or blank mark; small disc labels down to ~11px now carry the mark too. Presses without a dedicated label logo fall back to their main mark everywhere, including the package builder.
- Press marks render WHITE on dark component faces (Bill, 2026-08-12): photo-based vinyl swatches (real record photos) now also get the press label overlaid — the source photo's own label (which sometimes carried a leftover mark, sometimes none) is covered by the same rendered black label + white press mark the drawn discs use, so every disc wears this press's logo. And every press-components mock with a dark face (jacket fronts, inner sleeves, inserts, the black vinyl center label, dark center-label styles) now paints the press's uploaded mark as a white silhouette (CSS mask of the upload), so a logo uploaded as dark artwork on transparent (e.g. Hellbender's black rune) reads correctly instead of vanishing. Light faces — the white sticker, white "Black & White" label stock — keep the logo exactly as uploaded. Center Labels and Vinyl also now resolve the mark through the same shared fallback chain (label logo → square → main → light → identity icon) as the other component pages, so presses without a dedicated label logo still show their mark.
- Find-a-press moved into an Advanced modal on the Presses page (Bill, 2026-08-12 — handoff/press-find, verbatim visuals): the super-admin left rail no longer has a "Find a press" item; a quiet "Advanced" outline button next to the Presses search opens the spec-first modal (Format + Quantity, optional Color / Preferred location / Max turnaround → one filled blue "Find presses" pill) over a dimmed page. Results are a hairline ranked list — "Best match" as dot + label, fit note, turnaround, price/unit — and each row's "Invite to bid" opens the same assign-to-artist/label picker (invited-press flow) the old page had. Both themes via the real admin appearance source (mock theme pill stripped); /admin/press-match now redirects to Presses; the old standalone page is deleted. Matching API/algorithm untouched.
- Press Components build (Bill, 2026-08-12 — Ruby's handoff/press-components, press side + super-admin god-view only): the press portal has a new "Components" rail section under Catalog with Vinyl, Center Labels, Stickers, and Pricing live (Jackets / Inner Sleeves / Inserts show "Soon" pills — the artist chooser screens are future-state and NOT wired). All three handoff screens render pixel-verbatim in both themes (apple-canon light + charcoal dark) wired to real data via `GET/PUT /api/press/:id/components`. Vinyl seeds ONCE from the press's existing GoodTunes Packages types/colors (Black-only for a brand-new press) then lives independently — component edits never touch Packages. Pricing is a new quiet canon page (no mock existed): a segmented size chip selector (7″ Single / 10″ EP / 12″ LP, per Ruby's review 2026-08-12) sits above the list and prices are stored per type/color PER SIZE — the chip filters rows to types pressed in that size and each cell binds to that size's value; rows grouped by vinyl type with nested color rows, price cells start EMPTY (blank = artists see no upcharge), commit-on-blur, "N of M priced" counter counts the selected size's view; any earlier single prices were carried into every size the row is pressed in; GoodTunes Package pricing untouched. Also per Ruby's review: the redundant shell heading band above each component page's own hero line is gone — every component page (and Templates) opens crumb → hero. Press identity is data throughout — each press's own `labelLogoUrl` renders on discs/labels/stickers, never Memphis's. Center Labels/Stickers cards gained word+shape Offered / Not offered toggles. Staff are view-only; writes are press-editor gated. Bonus: previously uploaded template PDFs now auto-import on first Templates view — clean files mint a Pending revision ("Imported from an earlier upload"), unreadable or wrong-sized files land as a "Needs review" status (amber triangle) resolved by re-attaching or archiving. New `press_components` table (dev + prod migrated). Full suite green (1291/1291), all four tabs verified live in both themes.
- Press Templates flow wired up (Bill, 2026-08-12 — Ruby's handoff/press-templates, pixel-verbatim): the press portal has a new "Templates" rail item under Catalog. Index: format pills (Vinyl / CD / Cassette; Stickers "Not offered yet"), size pills (7"/12"; 10" not offered), and a slot tile grid (Single/Widespine/Gatefold jacket, Inner sleeve, Insert, Center labels…) mapped to the real template-spec rows. Uploading (drag-drop file or paste-a-link) attaches the PDF, measures it server-side (artboard, pages, inks, fonts, dieline), and mints a revision (R-MMDDYY; prior revision superseded — one live revision per slot). Detail screen: the printed-areas study rendered from the REAL measured/operator geometry, revision status pill (Pending/Certified), and a Certification section — upload or link a finished test file, it runs the same completed-art checks the client uploads get (checked against this press's own template on file), results show as expandable icon+word check tables (Ready / Warnings / Blocked / Unverified), and a passing run's Certify button flips the revision to Certified. Certifying is guarded: only a run against the CURRENT live revision can certify (a replaced/archived template demands a fresh test). Presses without a name-matched checker baseline fall back to the generic vinyl baseline, with their own measured template overriding per field. Staff are view-only end to end; writes are press-editor gated. Test runs are vinyl-only in v1 (CD/cassette slots answer honestly that certification isn't available yet). Revision history + test runs persist in two new tables (dev + prod migrated). Full suite green (1291/1291), verified live in the portal.
- Press portal staff profile (Bill, 2026-08-11 — note for Ruby): opening one of the press's own STAFF members in the portal People directory no longer shows the artist-shaped page. Staff get: no Cover/Releases/Streaming tabs (Overview only); the eyebrow shows their business title (e.g. "Plant Manager") instead of "Contact"; and the Overview is a contact card — Title / Email (mailto) / Phone / Address rows, with a note that GoodTunes manages the details. The Invite button was also brought onto Apple canon (solid brand-blue pill, no gradient/purple, no icon) and "Remove from press" is now a quiet hairline pill. Artists/client contacts are unchanged; contact info is only ever returned for the press's own staff (PII stays stripped for everyone else).
- Catalog page title renamed (Bill, 2026-08-11 — note for Ruby): the press catalog page's main heading now says "GoodTunes Packages" instead of "Catalog", matching the left-rail item it lives under ("Catalog" is now the parent rail section, with GoodTunes Packages / White Label / GoodDeed Certificates beneath it). Same page in the press portal and the operator god-view; the small section pull-down next to the heading in god-view is unchanged.
- Jewel-case render live + correction round applied (gogoods/Bill, 2026-08-14 — supersedes the 2026-08-11 "parked" note): picking "Jewel case" on the CD catalog page draws the real product photo (CD_Mockup PSD case layer, re-exported at the full 1800² frame so the handoff geometry lines up). Per Bill's Aug 14 correction round (handoff commit e35a266, copied verbatim): the booklet art sits ABOVE the case photo at the PSD Label-layer rect (213,213 → 1507×1431 in 1800²) with only an inset shade — no more ghosting under the semi-transparent lid; the cover carries the press's OWN logo on press black (waveform only as fallback); the disc slides out on hover (plain slide, no shine, rest position in the stylesheet not inline — inline `left` would freeze the disc), with longer travel for the wider jewel body. Correction item 3 done per Bill's call (2026-08-15, "remove it everywhere — pills + rail are enough"): the "GoodTunes Packages" pull-down next to the Catalog H1 is removed from all surfaces; the Catalog sections stay deep-linkable via ?section= and remain reachable through the portal rail (god-view via View-as). Task-merge pipeline bounced on the original task so it was applied to main manually (cherry-pick + post-merge).
- Template tile filenames never wrap (Bill, 2026-08-11): on the "Print prep." template tiles (vinyl jacket/sleeve/labels/booklet AND the cassette shell/J-card/O-card/label slots — same component), a long filename now stays on ONE line ending in "…" so every tile keeps the same height; hovering the tile slides the text left (the same Apple-canon marquee ratified for the catalog color cards on Aug 10) to reveal the full name, easing back on mouse-out. This replaces the old middle-truncated text that could still wrap to a second line. Applied on main and pushed; ships with the next publish.
- Bleed measured against the press's certified template line (2026-08-11): the template scan + backfill now record the template's own bleed line (measured_bleed_line_inches; an operator-entered bleed_line_inches always wins), and the completed-art bleed check resolves its expected line in order template line → the file's own BleedBox (a new "Unverified" status the operator can acknowledge) → explicit fail. Every result names its measurement source as visible text, and all statuses render icon + word (colorblind-safe). Applied to main directly after the task-merge pipeline bounced again; columns + backfill run on dev and prod.
- Press portal Staff contacts open correctly (2026-08-11): clicking a contact on Settings → Staff deep-links to the scoped Person page, which previously said "Person not found or not in your press scope" — the press-scope check only admitted people homed to the press or primary artists on awarded albums, never the press's own staff contacts. The scope predicate now also admits the press's own contacts (entity_contacts, manufacturer kind). Verified both directions: contact row present → profile loads; row removed → 404 (cross-press wall intact).
- Theme-aware handoff re-pull applied (2026-08-11): Ruby's re-committed 7 handoff files (press specs ×3, CD/cassette catalog builds ×2, press catalog + package builder ×2) — each now carrying a THEMES map with light + dark token sets — were re-pulled byte-for-byte from github/main and re-applied verbatim to the wired app pages (PressSpecs, PressPackagePricingCatalog incl. the CD/cassette catalog bodies in PressMediaCatalog, PressAlbumPackageBuilder). Each screen now picks its token set from the shell's active theme (charcoal admin dark ↔ light) instead of hardcoding one set — this fixes the dark-cards-on-light-pages bugs on Catalog and Specs. The mock-only floating "View light / View dark" pill was not shipped. The apple-canon "Page layout" (single 1240px container, full-width cards) and "Theming & breakpoints" sections are honored; verified with screenshots in both themes at 1440/1024/768 with no horizontal overflow. Supersedes the earlier rule that CD/cassette catalog bodies were dark-only.
- Full-width page layout canon applied to Press Specs (2026-08-11): the Specs page (Audio + Art) now uses the ratified content container — centered, max-width 1240px, padding 32px 40px 96px, equal 40px gutters — replacing the old narrow max-w-3xl wrapper, byte-matched to the re-committed handoff files. The press rail's Catalog is now a collapsible section per the same handoff: GoodTunes Packages (the vinyl product catalog), White Label (dimmed "Soon" pill, inert), GoodDeed Certificates (the pricing tab, visible to press logins again — supersedes the earlier hide), Specs. Canon rule recorded in handoff/style-guide/apple-canon.md ("Page layout — ratified Aug 11 2026"): every operator/partner page uses this container, no per-page narrow caps; existing pages get swept as touched.
- Artwork checks driven by each press's uploaded template (2026-08-11): templates attached to press catalog spec rows are now measured server-side on attach (artboard size, page count, CMYK/RGB/spot usage, live text, embedded fonts, dieline layer) into stored measured_* fields, and the completed-artwork checks resolve their expected values with precedence operator edit > measured template > baseline > computed — check wording names the press's template on file. The catalog editor shows the scan summary with mismatch flags, a "couldn't measure" note when a scan fails (e.g. the stored link isn't a PDF), and a Re-scan button. Already-attached templates were backfilled (the 13 existing rows all recorded "not a PDF" scan errors — re-attach real PDF templates to measure them). Applied to main directly after the task-merge pipeline repeatedly bounced; migration run on dev + prod, full suite green (1274/1274).
- Press-specific print spec fields + checks (2026-08-11, MRP guide parity): each press can now record its own print standards — press-level defaults on a new "Print rules." section of the Catalog Specs surface (bleed minimum + recommended, safety margin, standard + bitmap/line-art PPI floors, grayscale-required and official-Pantone-only toggles, placed-image format rule, general + center-label advisories, accepted-formats note, and downloadable .joboptions / preflight-profile reference files), with per-component overrides in the template-specs editor. The completed-artwork check dialog now measures bleed from the PDF's own trim/bleed boxes against that press's numbers (fail below minimum, warn below recommended), runs an advisory edge-band raster heuristic ("outer bleed band appears empty" — warn only, never fail), applies a second PPI floor to 1-bit line-art images, verifies grayscale-only ink on B/W-flagged pieces, flags spot colors whose names don't look like official Pantone inks, warns on PNG/GIF-provenance placed images, and lists un-machine-checkable rules (safety area, label knockout) as info-tier advisory rows in the press's own wording. Verdicts cite the press by name ("Memphis Record Pressing requires ≥0.125" bleed; 0.25" recommended"). MRP's guide values are seeded; presses with no values entered keep exactly today's behavior. Uploaders see the press's accepted-formats note + reference downloads on the Completed Art panel. Design brief for the entry surfaces published at docs/press-print-spec-design-brief.md (Ruby mocks in parallel).
- Catalog › Specs page (handoff/press-specs, 2026-08-11): presses now publish "the numbers artists press against" — per-format audio master-file rules (Vinyl: formats/bit depth/sample rate/side lengths per size+speed/cutting guidance; CD: Red Book masters, max length, ISRC, pregap; Cassette: shell side lengths) plus one set of art rules (resolution floors, bleed/safety geometry, color mode/PMS/formats/fonts). Handoff design rendered verbatim (dark canon body; Audio|Art pill left, Save right — gray-outline disabled until something changes, then the page's one blue pill). Press portal gets a flat "Specs" rail item under Catalog; super admins see the same page via a quiet section pull-down next to the Catalog heading on the press page (GoodTunes Packages / White Label "Soon" / GoodDeed Certificates / Specs, deep-linkable `?section=`). Values start at the handoff defaults and persist per press (`manufacturers.audio_specs`/`art_specs` jsonb, migrated dev + prod); edits are press-editor gated (Staff read-only), strict server validation.
- CD disc preview silkscreen bands (2026-08-11): the CD render in the catalog build page now paints picked silkscreen spot colors as concentric bands (first pick outermost, even radius shares between hub and edge), dark inks get a subtle edge ring so they read on the charcoal canvas, and the collapsed Silkscreen summary row's disc chip shows the same bands.
- Press payouts via Stripe Connect (2026-08-10): super admins can now pay presses/plants in-system. New System → "Vendor payees" page lists every press with a truthful onboarding status (Not invited → Invited → Onboarding → Active, advancing off the account.updated webhook with a live-retrieve fallback while pending), and emails/re-sends each press its Stripe Express onboarding link. From an album's manufacturing ledger a super admin clicks Pay vendor, picks an Active press, enters the amount, and confirms an explicit dialog naming vendor + amount + project; the Stripe transfer fires immediately (idempotent, double-submit-safe) and the transfer ID is shown. Insufficient-balance errors surface as the clean Stripe message, never a raw 500. Every transfer — success or failure — is recorded per project (vendor, amount, date, transfer ID, initiating admin, linked inbound paid steps) in a money-in/money-out view plus an attempt audit log. Ledger reuses payout_earmarks (sourceKind vendor_payout) + a new vendor_transfer_attempts table; model write-up in docs/press-payout-ledger.md. All routes are super-admin only (partners 403).
- Manufacturing invoices payable by pushed bank transfer (2026-08-10): on a due Shopify+ ledger step the artist now sees "Pay by bank transfer" (primary, no fee) showing Stripe's virtual account details in-app (bank, routing, account number, exact amount, reference, one-tap copy per field, print/save-PDF) plus a "Card +fee" fallback with the 2.9%+30¢ card fee disclosed before confirming. ACH debit removed from this flow. New "Awaiting transfer" state flips to Paid via Stripe webhooks only; short transfers within $15 (configurable) auto-close; larger partials show received vs remaining and a second transfer to the same account completes them; payer details are logged; operators see leftover Stripe cash balances (overpayments) in the Payments tab. Artist gets an email confirmation when funds land. LIVE Stripe account enabled 2026-08-10 (Dashboard wizard; live smoke PI returned full wire instructions) and the production webhook endpoint now subscribes to all bank-transfer events — see docs/bank-transfer-payments.md.
- View-as indicator, per Bill 2026-08-10: the full-width blue "Exit view" banner is gone. While viewing as a partner, a single compact pill sits in the portal header left of Feedback (eye icon + "Viewing as {name}" + small X; soft blue bg, dark blue text). Clicking the pill or the X — or pressing Esc — exits the view and lands the operator back exactly where they were before entering view-as (path captured at mint, safe-checked; falls back to the admin dashboard). The header keeps its normal height, so content shifts zero pixels; the old banner dismiss/restore logic was removed.
- Dashboard empty-state rule, per Bill 2026-08-10: the work-queue "caught up" state is the same slim one-line bar as "Needs your attention" (white card, hairline border, py-3.5; green 16px check + 13px semibold "You're all caught up." left; quiet 12.5px "New work appears here the moment it needs you." right) — never a large centered box. Applied to AdminDashboard (previously rendered nothing when empty), ArtistDashboard (previously a big hero card), and PressPortal (aligned to the exact spec). Testid `work-queue-empty` on all three. Rule extends to any future dashboard queue section.
- Catalog "Pick a size" row, per Bill 2026-08-10: a size hidden via "Don't offer this size" now leaves the row entirely (no grayed card). A quiet "+ More sizes" affordance appears at the end of the row whenever at least one size is hidden; it opens a small dropdown listing the not-offered sizes (label · sublabel), and picking one re-offers it with all pricing and colors intact. Hidden when every size is offered. CD/Cassette have no size rows yet; the same pattern applies when they gain them.
- Catalog color cards, per Bill 2026-08-10: long color names (e.g. "Iridescent White w/ Blue") no longer wrap to a second line, so every card is the same height. The name ellipsizes with a soft fade; hovering the name slides it across (Apple-style marquee) to reveal the rest, and the full name rides on the tooltip.
- Press Catalog page (Vinyl/CD/Cassette pills), per Bill 2026-08-10: (1) switching format pills no longer resets the other tabs — all three panels stay mounted (CD/cassette after first visit) and toggle visibility, preserving selected size/type/color, unsaved edits, and per-tab scroll position; (2) the Vinyl pill no longer force-resets to the first offered size on click, so 12" LP stays the first-load default and the press's in-session selection always wins; (3) "Pick a type" completes the choose-then-collapse loop, per Bill 2026-08-10: selecting ANY type while the grid is open — different or the current one — collapses it back to the one-line summary row (disc, name, "Type · N colors", Change) and the "Build colors" section updates to the new type in the same motion. There is no state where a type is picked and the grid stays open (reorder mode is the one exception — the grid stays open for dragging). Standing rule for any collapsible picker built this way: open to choose, choice closes it. (4) "…" menu polish, per Bill 2026-08-10: Edit… on TYPE tiles now opens the filled-in Edit type popover (a reset effect was snapping the popover back to the menu on every re-render, so Edit… looked dead); the two-item Edit…/Archive dropdown is content-sized (~176px) for both types and colors instead of editor-width; and the Edit color popover gained the same quiet red bottom "Archive color" row the type editor has — inline confirm, archive flags (archived_at) never deletes, selection falls back to the first remaining color. (5) Archive-type cascade confirm, per Bill 2026-08-10: archiving a TYPE now raises a small dialog ("Archive Splatter?" / "This archives the type and all N colors under it. Artists won't see them for new projects — anything already in a project keeps working. To restore it later, contact GoodTunes." / quiet Cancel + red Archive) with the type's real color count; the server already flags the type AND its colors with the same archived_at stamp so a GoodTunes admin can restore the whole set. Colors keep the lighter inline confirm — only the type-level cascade gets the dialog. (6) Fixed: clicks inside the type tile's "…" popover were bubbling (React portals keep the component-tree event path) into the tile's select handler, so Edit… selected the type, collapsed the picker, and dropped the operator into the colors panel instead of the type editor; events now stop at the popover boundary. (7) Picking a color from the type-section search also collapses the type grid (same collapse-on-choice rule as tapping a tile; reorder mode still keeps it open).
- Admin customer detail: operators (super_admin) can now edit a fan's shipping/billing address via an Edit link on each address card — for fans who moved. Editing the shipping address can optionally re-point OPEN orders (paid, not yet shipped or handed to fulfillment) so the next shipping label uses the new address; orders already at the fulfillment partner are untouched. New `PATCH /api/admin/customers/:id/address`. The "Make admin…" action was removed from the customer page per Bill (customers are never promoted from there; the server route remains for deliberate API use).
- "Good Press" white-label demo press created on prod (and dev for parity) via `scripts/create-good-press.ts` — full clone of Viryl Technologies' catalog (3 vinyl formats, 24 tiers, 117 colors, 5 jackets, all 56 confirmed price ladders) with Viryl identity blanked (no domain/website/bio/contacts) and the new GoodTunes press mark as its logo. Vinyl-only (GoodDeed/fulfillment off). Idempotent re-run; deterministic child ids.
- Logo format policy (ratified 2026-08-10): press product surfaces (placeholder covers, vinyl center labels, configurator) stay SVG-only from the existing logo fields — untouched. New optional press "identity icon" (PNG/JPG/SVG, `manufacturers.identity_icon_url`, migrated dev + prod) used only for in-app identification — avatar chips, press pickers, dashboards, activity feeds — falling back to the SVG logo when unset. Uploaded via a small corner chip on the press page's logo tile. Non-press accounts (artists, NPOs, labels) verified already PNG/JPG-safe — no changes needed.
- CD + Cassette catalog build pages (handoff/cd-cassette-catalog v1): the CD and Cassette pills on the Catalog page are live, each rendering the handoff design verbatim (dark canon body). CD: case (Sleeve/Jewel) → print (Silkscreen with up to 3 spot colors + press-added custom inks, or full-color offset) → booklet (jewel only) → run pricing. Cassette: case (J-card/O-card) → 8 stock shells (real product photos) → imprint → run pricing; tape length derived from runtime, never picked. Per Bill (2026-08-10), the mock "GOODTUNES · DEMO ALBUM" sample text was removed from the shell render — the paper-label option shows a blank sticker strip, on-shell print shows the clean shell photo. Cassette "Print prep" now has the same per-piece template slots as vinyl (On-shell print, J-card, O-card, Paper label) — same add/replace dialog, upload or paste-a-link, persisted filenames, read-only for Staff. Custom spot inks, run prices, and turnaround overrides persist per press (new `cd_catalog`/`cassette_catalog` columns, handoff defaults when untouched). Deep-linkable via `?media=cd|cassette`.
- Press package pricing Catalog page ("Item 28" design): rewind control, 140g/180g shared run-size price books, centered template add/replace dialog with persisted filenames, size/template hide controls (Booklet hidden by default), final copy, "Make it yours" branding dialog with server persistence.
- Apple-canon visual sweep across all 53 super-admin pages, light + dark (two-tone headings, canon hovers/dialogs/empty states, status dots).
- Apple-canon sweep of all partner/operator portals (artist, press, NPO, label, manager, vendor, printer).
- Dark-mode hover fixes (rows turning white).
- Combined press dashboard.
- SVG upload support for press logos, with stored-XSS hardening on the server.
- Press branding dialog made draft-until-Save.
- Dark-mode press logo inversion on the presses list and dashboard.
- Catalog build page tablet layout: two-column from ~900px, sticky left jacket column.
- Admin Albums grid placeholder tiles match the catalog jacket exactly (press label mark, white, on the press's label background); press-less albums show Memphis branding as a display-only default.
- Fixed admin horizontal page overflow on the Catalog tab.
- Rounded "View as this partner" button into a pill.
- Sticky catalog vinyl stage anchored to the top instead of centered.
- Renamed "Reorder" to "Rearrange" in the catalog.
- Removed the bio/turnaround card from the press Catalog tab (data kept, still on Overview).
- Artist self-delete of unsold albums; press/label delete of self-created albums.
- Fix for artists getting "Out of scope" on label-attached albums.
- Early-access 404 fix on prepping albums.

- Playground handoff doc for the Project/Structure flows: `docs/playground-flows.md` (2026-08-10). Names the seven flows (Project flow first — Artist > New Project > Formats > Variants; Evergreen second, needed this week for Raynes), lists what's already built for Playground to pull from vs. greenfield, and voids the 08-09 brief's design advice — the existing apple-canon light + dark variants are the only design authority. Reaches GitHub via the normal mirror sync on the next merge.

### Awaiting design (briefs sent to Ruby)
- **Art Blocks builder** (Bill, 2026-08-16): brief published at `handoff/art-blocks/README.md`. Artist-facing drag-and-drop art upload surface inside the album Physical → Art tab — one drop zone per component (jacket front/back/spine, center labels A+B, inner sleeve, gatefold spread, etc.), instant pass/warn/fail checks (format, dimensions, PPI, CMYK vs RGB), bleed/trim preview toggle with guide overlays, and a "Looks good" confirm that hands art to the press template stitching pipeline. Spec tables, check semantics, 9 per-block states, and a block-map companion table are all in the brief. Build task follows once mocks land.

### In progress
- (none right now)

### Not started (proposed task queue)
- ~20 proposed items awaiting go-ahead, including: fan pre-save card, Codemagic sync-failure alert, various test-coverage tasks, dock overlap fixes, Buy-sheet vinyl-fit warning.

## 2. Handoff ledger

- **Memphis white-label client portal** (`client/src/pages/mrp/` — PressClientEstimateMRP, PressClientEstimateAcceptedMRP, PressClientNextStepsMRP, ArtistDashboardMRP, ArtistDashboardNextStepsMRP, ArtistProjectHomeMRP; MRP-flavor estimate email in `server/mail.ts`; light MRP card in `WhitelabelLanding.tsx`) — matches `b912fb6` (7 screens + assets, light-only skin: white canvas, gold #D9C153, square corners, Poppins). Delete-first: the dark `/e/:token` presentation, dark MRP landing card, and dark MRP sign-in are gone on `mrp-light`-skinned hosts; other presses keep the previous surfaces. Wired to real data per the README "Must work" list (estimate sheets, start-project + account create, host-scoped login, uploads, ask-Brandon messaging, dashboard KPIs/chart/activity/top-projects from `/api/press-client/portal|dashboard`); everything else decorative. Dark-mode addendum `3acb222` NOT applied (follow-up).


Per handoff law (`handoff/README-template.md` rule 3): each page built from a handoff records the handoff commit SHA it currently matches. When a newer correction round lands on main, that pointer is stale and the page owes a re-diff.

- **Artist portal restructure** (`client/src/pages/artist/restructure/` — releases wall, five-tab release view, Reports hub, Settings; `ArtistDashboard.tsx` seven-item shape) — matches `a533652` (full walk file, all 7 scenes, README dead-ends shipped as quiet no-ops). Built in app commits `3f3c248` + `4f5c233` (review fixes: payments grouped-project contract, rtab-preserving stale deep links).
- **Press Estimates & Packages** (`client/src/pages/press-create/` — PressEstimatesIndex, PressPackagesIndex, PressQuoteBuilder, PressPackageBuilder; Create rail group via registry) — matches `b4e3bf0` (full handoff dir, README dead-ends shipped as quiet no-ops; review fixes: awaited/serialized persistence, canEdit read-only gating, no blank-name live packages).
- **Operator mirror of partner portals** (`AdminManufacturer.tsx` strip from `modulesForRole("press")` + `PressTabBody`; `AdminPerson.tsx` artist mirror via `ArtistTabBody`/`useArtistRangeQs`/`ARTIST_PORTAL_TABS` exports from `ArtistDashboard.tsx`) — one body, two chromes; operator extras past a divider; god view rides `?personId=`; review fixes: invite panel hidden in god view, URL-driven tab/range re-sync.
- **Press rail canon** (registry-driven `OperatorShell` rail — our component per the standing rails rule) — matches `f9adddf` (Product Specs rename, Components group, White Label top-level w/ Request pill, LayoutTemplate icon).
- **Press Templates index** (`PressTemplatesIndex.tsx`) — matches `fd92b0f` (controls-row divider rides with the size pills; breadcrumb "Product Specs · Templates" from `f9adddf`). Chain absorbed: `49df060` (Addendum 6 — amber pending accents, failed-ⓘ popover), `e01acd0` (Templates rework — no detail popup, ••• archive, All/Current/Archived pills, Cancel/Test/Save header), `3f44ecb` (tiles rest quiet, fine print on hover), `4d078f4` (Addendum 4 — stable tile order, supersede-in-place), `fa3a812` (Addendum 5 — GoodStudio tiles, Pending ⓘ).
- **Press template live test** (`PressTemplateLiveTest.tsx`) — matches `f9adddf`/`fd92b0f` (rail canon lands via the shared shell; page body unchanged since `49df060`, Addendum 6 — header Save/Close combo, view chips in header, Pending/Results banner above the preview, orbiting pending ring; live ink-check wiring kept, with a neutral "Measuring…" banner state adapted on top). Chain absorbed: `e01acd0` (opening-state arrival, header actions), `47f5785` (resume-draft sheet + upload-step subtext), `4d078f4` (Addendum 4 — quiet Save, Replace/History), `fa3a812` (Addendum 5 — Test & certify, truthful Save).
- **Templates policy card** (press Settings → Profile, "Require a passing test…") — matches `fa3a812`.
- **Dialog canon** (primary rightmost, Cancel as quiet text, one-line subtext, upload-sheet X close) — matches `3aca2b1`; applied across the templates pages' dialogs and sheets.
- **CD catalog build page** — matches `e35a266` (correction round: jewel cover layer order, disc hover slide-out; item 3 dropdown removal held pending Bill) over `a7c4230` (theme-aware v1).
- **Cassette catalog build page** — matches `ead5e71` (theme-aware v1).
- **Press catalog** (`PressPackagePricingCatalog.tsx`) — matches `3fc5edb` (theme-aware round).
- **Artist package builder** (`PressAlbumPackageBuilder.tsx`) — matches `4ddc047` (theme-aware round).
- **Press specs pages** — match `df8f73a` / `3a780fc` (theme-aware round).
- **Help & feedback dialog** (`client/src/components/operator/FeedbackLauncher.tsx`, all partner portals via OperatorShell; pin overlay in `AdminFeedback.tsx`) — matches `88d5937` (+ README `aecfcb9`). Delete-first: old shadcn form/tabs dialog retired; dialog markup verbatim, PageThumb/BigPage interiors swapped 1:1 for the real capture per README; empty state = Ruby's sanctioned quiet line; publicReply sub-line kept beyond the mock (flagged).
- **Press vinyl styles page** (`client/src/pages/press-components/PressVinylStyles.tsx`, replaces `PressVinylComponent.tsx`) — matches `6f99937` (delete-first verbatim port; mock/seed data swapped for the press's vinyl config, GEN_BASE repathed to `/vinyl-gen/`, per-color photo upload wired to postAdminImage, center labels on the shared white-mark logo chain; Components chip row → Templates-style segmented control per addendum).

## 3. Super-admin press Catalog tab — current behavior
- Each press's detail page has six tabs: Dashboard, Overview, People, Albums, Catalog, Analytics.
- The Catalog tab shows a "Catalog format" segmented control with three pills: Vinyl, CD, Cassette.
- Vinyl is the only live format. It defaults to 12" LP (or the first offered size) and shows: vinyl size tabs, pressing types/tiers, run-size price ladders (140g/180g books), color swatches, print/audio spec templates, turnaround, branding (center-label logo + background), and reorder/hide controls. Saving is explicit ("Edited · Save catalog").
- Handoff-default state (2026-08-10): the page opens on 12" LP with the **Black** type pre-picked and Classic Black selected, so the hero shows the black record in the sleeve. The Black type carries its own three colors (Classic Black / Midnight / Jet) — empty Black tiers were seeded idempotently on dev + prod via post-merge. "Pick a type" collapses to a one-line summary once a type is picked (small disc chip + type name + "Type · n colors" + a blue Change link); Change re-expands the grid, and picking a type collapses it again.
- CD and Cassette pills are live. Each opens its handoff build page (per-format header copy "On disc." / "On tape.", sticky product render on the left — jewel case/sleeve with the peeking disc, or shell photo with the printed piece — and the choice column on the right). Selections are live previews; the persisted parts are custom silkscreen spot inks (CD), the run price ladder, and the turnaround override. Staff see it read-only like vinyl.
- The jacket stage renders a black printed jacket with the press's logo, sticky and top-anchored on desktop.

## 4. Press-facing catalog & artist builder — current behavior
- The press portal (Dashboard, Clients, Projects, Acquisition, Catalog, Settings, Referrals) renders the exact same Catalog component as super-admin, so press users see what operators see.
- Press Owners/Admins can edit everything (formats, prices, colors, branding, templates, GoodDeed printing ladder); press Staff get a read-only view with a notice.
- An "Add your vinyl" colors sub-view handles disc color swatch setup.
- The artist-facing package builder ("Design your package. See what it earns.") pulls from the press's vinyl catalog — sticky jacket preview on the left, decisions and live earnings math on the right. If a press hasn't published a vinyl catalog, artists see a friendly empty state.
- CD and cassette: the press-side catalog build pages are live (same component for press portal and super-admin). The artist-facing package builder still only offers vinyl — CD/cassette builder flow not designed yet.

## 5. Changes made on the agent's own initiative
- Memphis default on press-less album tiles is display-only — no press assignment is written, so it doesn't affect MRP's portal scope, permissions, or pricing.
- Server-side sanitization hardening on SVG logo uploads.
- Design-lint baseline re-snapshots and dark-mode/theming consistency fixes discovered during the sweeps.
- Small robustness fixes from code review (e.g., logo fallback when a press's label mark fails to load).

## 6. Deferred items and why
- CD/cassette in the artist package builder — the press catalog pages are live, but artists can't yet build a CD or cassette package; needs its own design pass.
- CD/cassette price editing — run prices show as fixed pills per the handoff (no edit affordance in the design); presses can't change them in the UI yet.
- The "Print prep" attach row on the CD page is display-only per the handoff (no upload wired). Cassette got real template slots per Bill (2026-08-10) — see below; CD's slots pending Bill's call on which pieces.
- Press referral-link branding (fan arriving via a press's link sees that press's pricing/logo/imagery) — agreed larger future feature.
- Press-switcher for direct-to-GoodTunes albums — being designed in Playground first.
- Actually attributing press-less albums to Memphis — held back: a real press assignment would pull those albums into MRP's white-label portal, permissions, and pricing. Needs a product decision first.
- Cert accrual/true-up work — parked by design until after Shopify submission.
