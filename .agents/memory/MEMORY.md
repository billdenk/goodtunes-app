- [Vendor → Maker/Reseller rename scope](vendor-maker-reseller-rename-scope.md) — gear-supplier `vendor` shows to operators as Maker/Reseller (UI copy ONLY, never identifiers/role value/table/routes/testids); GoodDeed printing/payout "vendor" is a DIFFERENT concept, left alone.
- [Deep links claim my. not apex](deeplink-claims-my-not-apex.md) — Universal/App Links claim only `my.goodtunes.music` (app subdomain), NOT the bare `goodtunes.music` apex; the apex is the Webflow marketing site (A record -> Webflow via Cloudflare) which cannot serve association files; fan-shareable links live on `my.`/`get.`, so dropping the apex claim costs nothing.
- [App Store + Play Store submission readiness](app-store-submission.md) — one-page runbook for reviewers, demo account provisioning, and privacy labels; in-app account deletion is at Account → Privacy → Delete My Account; Buy/Chat are gated by platform-specific flags.
- [client React component tests](client-react-component-tests.md) — no vitest; tests run via tsx --test + jsdom under tsconfig.test.json (jsx react-jsx) with TSX_TSCONFIG_PATH on the `test` validation cmd; needs wouter/framer/scrollTo global stubs + PlayerDock boots collapsed.
- [napi-rs native binaries break esbuild bundle](napi-canvas-build-externals.md) — adding any `@napi-rs/*` (or sharp-style) pkg whose native `.node` ships in platform sibling pkgs requires a `pkg-*` glob in script/build.ts externals; optional-dep siblings aren't in package.json so the existing filter misses them, dev passes, publish fails.
- [Oversized album art downscale](oversized-art-downscale.md) — oversized raster art is never stored/served raw (OOMs mobile WebKit); downscale via sharp shrink-on-load or hard-reject — never silent-raw; backfill only stamps its marker when 0 errored.
- [Schema drift: publish diff + manual fix](dev-prod-schema-drift.md) — publish dialog diffs **dev DB→prod DB** (not schema→prod) so a dev DB missing tables/cols DROPs them from prod *with data*; symptom = admin page empty / panel 500 in prod only when `shared/schema.ts` is ahead; fix = hand-apply CREATE/ALTER IF NOT EXISTS in one txn .sql to the lagging DB ([sweep details, partial indexes, singleton seeds](albums-schema-drift.md)).
- [Press roster dev↔prod drift](press-roster-dev-prod-drift.md) — founding seed mints fresh Memphis/Hellbender ids per clone + PMP/Hoover are prod-only hand-created, so publish would DROP prod presses; reconcile with ID-preserving static INSERTs in post-merge (dev-only). Hellbender/Memphis ids still drift.
- [Admin role columns out of pgTable](admin-roles-out-of-table.md) — users.role/role_scope_id live in the DB but not in shared/schema.ts; read/write via server/auth/roles.ts raw SQL, not drizzle.
- [Partner permissions + post-sale lock](partner-permissions-and-post-sale-lock.md) — five per-scope verbs gate every partner endpoint; only edit_metadata respects the post-sale lock (masters/shopify/payouts stay live); lock returns 403, not a divert; approval-required is a separate "divert to queue" layer on top. UI uses GET /api/admin/albums/:id/edit-access to disable inputs without speculative POSTs.
- [Instrument scrape — brand-identity Product nodes](instrument-scrape-brand-identity.md) — manufacturer sites embed an `@type:Product` for the company itself; skip it or you import the brand-as-gear with the logo as the photo.
- [Shopify-JSON gear scrape](shopify-json-gear-scrape.md) — minimal-meta Shopify vintage shops (Retrofret/Gryphon) import via `/products/<handle>.json` (SHOPIFY_JSON_HOSTS) not HTML; vendor field can be junk → name-only maker, never fabricate a domain.
- ["Applied to dev DB" claims are not trustworthy](migration-claims-vs-reality.md) — task agents run in isolated DBs; after a schema-touching merge verify with `\d <table>` against both DATABASE_URL and PROD_DATABASE_URL before assuming anything landed.
- [Makers vs Resellers](makers-vs-resellers.md) — `vendors` carries is_maker + is_reseller (one row can be both, Gibson). Single AdminVendors/AdminVendor with mode via useRoute. Filter `?role=` must live in URL string, not queryKey object. "Manufacturers" page is labelled "Presses" (URL unchanged).
- [Press catalog shape](press-catalog-shape.md) — per-press vinyl pricing is formats → tiers (jsonb price ladder) → colors; manufacturing comes from the tier's ladder, platform pricing only covers publishing/processing/margin + free-flow fallback. SKU snapshots names not ids.
- [iOS WebKit stacked backdrop-blur](ios-webkit-stacked-backdrop-blur.md) — "A problem repeatedly occurred" on iPhone over a scrolling image list is usually two stacked `backdrop-filter` surfaces, not a JS heap leak; audit GPU layers before chasing data size.
- [user_albums.user_id loose FK](user-albums-loose-fk.md) — user_albums.user_id stores customer_users.id even though schema FK points at users.id; inert FK, same drift as auth_tokens — write the fan ID, never join users.
- [auth_tokens FK keeps reappearing](auth-tokens-fk-recurrence.md) — schema has no `.references()` but old dev DBs carry the leftover FK; publish diffs dev→prod and re-adds it, 500ing customer signup verify. Fix lives in `scripts/post-merge.sh` (idempotent DROP CONSTRAINT IF EXISTS on both DBs).
- [Mux-only fan playback](mux-only-fan-playback.md) — fan player must never fall back to raw audioUrl when Mux signing fails; capabilities.md promises masters never leave as a file. Stop playback instead; admin previews are the only exception.
- [Mirror native iOS volume](native-ios-volume.md) — iOS web volume is read-only so the slider is hidden; native iOS (Capacitor) can reach hardware volume, so the slider is shown there and mirrors the phone's physical buttons.
- [Bonus-video Mux pipeline](bonus-video-mux.md) — album_videos stream signed HLS like audio; the SHARED fan/admin GET /albums/:id/videos — must strip videoUrl for non-admins (else the /objects master leaks); webhook matches songs THEN album_videos; public playback-url lazy-ingests legacy rows.
- [Quickprinter vs press](quickprinter-vs-press.md) — GoodDeed printer/hologram/insertion routing is platform-default on payout_settings (not per-album); `is_quickprinter` is mutually exclusive with `is_maker`; printing ladders are per-paper-size with fixed rungs.
- [Soft-delete trash](soft-delete-trash.md) — 14 admin tables soft-delete via `deleted_at`; every read on those tables MUST filter `isNull(deletedAt)` or rows leak back into player/search after delete. Blobs not copied.
- [admin.goodtunes.music blank page triage](admin-blank-page-triage.md) — headless-screenshot + curl the bundle hash before touching code; every "admin is blank" report so far was stale client state, not a real outage. No service worker, so it's never a PWA cache.
- [Per-copy order entitlements](per-copy-entitlements.md) — multi-quantity orders fan out into `order_copies`; any GoodDeed number floor MUST include that table or refunds/cert reads silently re-mint printed numbers.
- [Vendor pricing bypasses post-sale lock](vendor-pricing-bypasses-post-sale-lock.md) — operational routing (per-leg vendor assignment, per-vendor GoodDeed pricing, payouts) stays editable after first sale; only fan-facing metadata (price/min/qty on the addon) respects the partner-permissions edit_metadata lock.
- [Press catalog unitCents](press-catalog-units.md) — `press_tier_jacket_ladders.price_ladder[].unitCents` is per-unit cents (NOT vendor TOTAL dollars); seed must compute `round(total$ * 100 / qty)` or commerce silently over-charges 100×. Hellbender values are pre-discount; broker_discount_pct applies at lookup.
- [SKU save resolves press from picked tier](sku-press-resolution.md) — vinyl SKU catalog saves derive pressId from the chosen tier row (carries pressId), NOT the album's invited press; cross-press/no-invited-press picks stick; unpriceable picks pin identity instead of reverting to the default color.
- [Press color swatches](press-color-swatches.md) — catalog colors store only a solid `swatchHex` (+ optional photo `swatchImageUrl`), never the in-code gradient; collapse gradients to a representative hex, and only backfill rows where BOTH are NULL so operator edits + imported photos win.
- [Press pricing sources + sync lock](press-pricing-sources-and-lock.md) — a tier ladder is written from PDF seed / Shopify import / manual site load; only `lockedFromSync` rungs survive a re-sync; Hellbender importer can't write the "*Colors" tiers (bare-name mismatch → tiersMissing), only Black/House Mix/12_lp Color.
- [GoodDeed cost stack](gooddeed-cost-stack.md) — per-cert wholesale is the **tiered ladder** rung keyed off the resolved signed-copy count (via `/gooddeed-pricing-preview`), not the flat `payout_settings.cert_cost_cents`; CC fee on cert retail is the only other line, rung already bundles print/hologram/shrinkwrap/insertion + all three shipping legs.
- [Backend has no hot-reload](backend-no-hot-reload.md) — dev runs `tsx server/index.ts` (no --watch); Vite HMR only reloads the client. After editing any server file you MUST restart the "Start application" workflow before curl-testing, or you'll test stale routes.
- [Invite pre-flight deep-link to album](invite-preflight-deeplink.md) — landing an invitee on a specific album needs inviteRole=identity/manager + targetPersonId + preFlightedAlbumId together; quotes ride as album_skus, not on the invite; claimed People return acceptUrl=null (held for review).
- [Add-Admin vs Invite-Artist split endpoints](add-admin-vs-invite-artist.md) — partner "Add Admin" hits POST /api/admin/partner-contacts; "Invite Artist" hits POST /api/admin/invites. Both keyed on invite_subusers, so gating one verb won't separate them — press Staff need invite_subusers for artists but must be blocked from admin adds via pressUserCanEdit() (owner/admin discriminator) + a separate canAddAdmins probe flag.
- [Gear/SuperCredits fan surfaces](gear-supercredits-surfaces.md) — fan gear page + vendor profile are SHEETS in AlbumDetail.tsx (not /instrument/:id); logo+name→vendor profile, trailing link→affiliate_url (gear), globe→brand domain; chat hidden on both.
- [One-time data backfill to dev+prod](one-time-data-backfill-via-post-merge.md) — prod executeSql is read-only & task dev DB is throwaway, so data resets go in post-merge.sh with a `post_merge_data_backfills` marker guard or they clobber operator edits every merge.
- [Observability: health probe + ops alerting](observability-ops-alerting.md) — `GET /api/health` (double-bounded DB probe) + email alert on ANY /api 5xx via res.on(finish); throttle is per-instance in-memory by design. Also covers Sentry (manual DSN, inert w/o it, hand-attach request context bc prod is bundled) + per-instance pg pool cap (PG_POOL_MAX, pool.on("error") or idle drop crashes instance).
- [design-lint baseline is line-content keyed](design-lint-baseline-line-keyed.md) — migrating a button to IconButton re-flags pre-existing baselined raw-hex SVGs (heart/bookmark) as NEW; re-snapshot baseline, don't convert to var; /tmp log snapshot lags, trust fresh local run.
- [Per-sale pool refund reversal](press-pool-refund-reversal.md) — any per-album pool that accrues on paid sales must reverse on refund across 3 sites (handleRefund + shopify inline route + shopify webhook); approve-and-pay endpoints flip status + side effects in one db.transaction.
- [Partner notifications dispatch](partner-notifications-dispatch.md) — empty events=subscribe-all; email-only v1 (others logged skipped); best-effort never throws; per-album last-notified joins log on payload_snapshot->>'albumId'.
- [Object Storage bucket shared dev+prod](object-storage-shared-bucket.md) — one repl-level bucket; dev workspace + published prod app both serve `/objects/uploads/<id>`. Mirror catalog/scraped images ONCE, write the same URL into both DBs; add-memphis-metallic.ts is the template.
- [Object range serving + mp4 faststart](object-range-serving.md) — /objects/uploads/:id must handle ranges itself (edge proxy 500s on suffix `bytes=-N` + large streams); mp4 imports get `+faststart` so they don't need a tail-seek.
- [framer-motion vs Tailwind/CSS transform conflicts](framer-tailwind-transform-conflict.md) — when framer animates scale it owns inline `transform`; Tailwind `active:scale`/`translate-x` and CSS `transition: all` on the same el break — use whileTap, move translate to another el, narrow the CSS transition.
- [Raw-SQL EXPLAIN smoke + phantom columns](raw-sql-explain-smoke-and-phantom-columns.md) — EXPLAIN ${sql} validates raw db.execute columns w/o running; albums has NO created_at and uses artwork/physical_format (not cover_url/format); people has no created_at, email=contact_email; many raw queries still ref the phantom names.
- [Press mode god-view (Dedicated vs All)](press-mode-god-view.md) — per-entity press_mode on people+labels (null=inherit, artist wins over label); SellPanel reads resolved mode off invited-press response; correcting the press stamp must re-resolve unlocked catalog SKU snapshots; package-designer default vendor must honor the matched press even when in HIDDEN_PREFLIGHT_VENDORS.
- [Album bonus media single CMS surface](album-bonus-media-surface.md) — fan album videos/photos come only from /api/albums/:id/videos|photos via AlbumBonusContent (mobile bonusSlot+admin previews) & DesktopAlbumView; never re-add a static editorial dup in musicData.
- [7" booklet either/or bundle](booklet-7inch-bundle.md) — 7" booklet is one-price VARIANT (single CC fee, bundlePriceCents ?? sku+addon); cassette stays stacked; NEVER auto-stamp the $25 default onto legacy null-bundle rows on unrelated saves; bundle cost excludes the vinyl row's own CC fee.
- [Mockup-sandbox conventions](mockup-sandbox-asset-paths.md) — iframes can't load main-app paths (/figmaAssets,/objects); copy assets to artifacts/mockup-sandbox/public/images, ref `/__mockup/images/...`; sandbox keeps hand-maintained parallel fan-component copies in [_shared.tsx](mockup-sandbox-parallel-copies.md) (mirror polish by hand); [verify previews](mockup-sandbox-preview-verification.md) by external_url-screenshotting `https://<domain>/__mockup/preview/<folder>/<Component>` (proxied via app:5000) — first capture may be blank, re-shoot (cache-bust `?v=`).
- [SSRF-safe server OG/social image render](ssrf-safe-og-image-render.md) — when a server route rasterizes a share/OG image from a client-supplied art URL: same-host check (kills metadata IP) + block RFC1918/link-local for spoofed-Host + fetch-yourself (redirect:error, content-type, timeout, size cap) → gradient fallback; do NOT block loopback (dev origin is localhost).
- [GoodDeed share-card corner radius](gooddeed-share-card-radius.md) — approved Story card is subtly rounded, not square; no Meta HIG for Story corners (rounding is device/IG chrome); round only when floating, square if full-bleed.
- [Person roles vs access grant](person-roles-vs-access.md) — people.roles[] are multi-select creative-credit tags (separate axis from single users.role access grant); GET person returns read-only derivedRoles unioned from track/album credits; roles containing "artist" flips shape, replacing the old promote step.
- [Account-creation paths fan-out](account-creation-paths-fanout.md) — any field stamped "at sign-up" (consent/defaults/attribution) must be written on all 5 create-account paths (signup-with-code, /api/register, OAuth signup, OAuth invite-accept, password invite-accept); createCustomer/createUser use restricted insert schemas so stamp via update/raw SQL; never re-stamp reused invite rows.
- [Odesli per-release link resolution](odesli-streaming-links.md) — song.link maps an Apple iTunes collection id → Tidal/Deezer/Pandora deep links but NOT Qobuz (no free source, stays search-fallback); no-key tier ~10 req/min so batch imports must bound concurrency + budget, best-effort never-throw.
- [Letter print-cert signature slot](print-cert-letter-signature-slot.md) — Letter cert signature sits in a FIXED Y slot assuming a 1-line headline; long recipient/title wraps the headline and the signature collides with the footnote. A4 is flow-based, immune. Per-tile `sample` override merges over SAMPLE.
- [Press re-import spawns duplicate "*" tiers](press-import-duplicate-star-tiers.md) — color-library re-import makes `Opaque*`/`Neon*` twins (photos only, no hex/pricing) beside the plain tier; collapse = keep plain, fold photos on, delete `*`; cleanup-memphis-catalog.ts is the template; importer bug unfixed.
- [GoodDeed design lives on canvas](gooddeed-design-lives-on-canvas.md) — cert/share-card *design* is iterated in canvas mockups; ignore stale board tasks asking for cert/card design (only "Match GoodDeed preview to Figma" overlaps); commerce/print-size/unfurl/email tasks stay active.
- [Passwordless fan set-password](passwordless-fan-set-password.md) — forgot-password skips fans with no password; account-initiated "set a password" needs its own requireCustomer endpoint that mints a reset token regardless.
- [Gift of Hope = Nightbirde only](gift-of-hope-nightbirde-exclusive.md) — "Gift of Hope" is the Nightbirde Foundation's own add-on name, NOT a generic label for the custom-addon feature; other charities create their own separately named packages on the same generic mechanism.
- [Vectorize brand/App Store icons](vectorize-brand-app-icons.md) — official SVGs blocked? trace the supplied screenshot with `nix run nixpkgs#potrace` + ImageMagick threshold/negate; tight-bbox center; design-lint ignores SVGs.
- [Per-album NPO donation split](npo-donation-split.md) — up to 4 NPO beneficiaries ≤100¢/unit, one referral_credit each at sale; funded from margin (never change price); editable until first sale then add-from-unallocated only; referral_credits has no album_id (join via order); old (order_id,kind) unique swapped for two partial uniques; refund doesn't reverse credits.
- [ChromeScrim one-blur-per-region](chrome-scrim-one-blur-region.md) — fan-chrome scrim owns the single per-region backdrop-filter while active; in-region controls drop their own blur, handing off across the scrim's EXIT fade via a lifecycle timer (not raw active flag); IconButton glass ≠ blur surface; search+album-menu two-blur edge is safe (opaque overlay + z-60 modal freezes scroll). Plus design-lint naked-icon-button 200-char proximity gotcha.
- [App-review demo account seed](appreview-demo-seed.md) — seed a demo song's master by INSERT…SELECT from a static-seed song (copies that env's own valid Mux ids) to dodge dev↔prod Mux drift; ownership via user_albums grant.
- [Production-partner capabilities](manufacturer-capabilities.md) — manufacturers carry doesVinyl/doesGoodDeed/doesFulfillment (CHECK at-least-one); one partner = many jobs; GoodDeed routing-default printer picker stays vendor-FK-keyed (out of scope, would break pricing).
- [Adding a customer-family host](multi-host-oauth-cookie.md) — a new fan host needs host→kind in 2 files + canonical-redirect exemption + callbackOrigin returning the SAME host (cookie is host-only) + IdP redirect-URI registration, or OAuth 403s.
- [Admin list state in URL](admin-list-state-in-url.md) — admin Albums mirrors tab/view/search/filters into the URL & round-trips the WHOLE list query through detail links as one encoded `albumsReturn` param (not per-control); reusable for other index pages.
- [Per-release share slugs](share-slug-routing.md) — two-part get.goodtunes.music/<artist>/<album> links; artist slug on people.artist_share_slug (globally unique); album slug unique per-artist composite; RESERVED_SLUGS must cover first segments of ALL two-segment routes too.
- [Vinyl disc mask two-tone backdrop](disc-mask-two-tone-backdrop.md) — Hellbender mock-ups have a split gray/white backdrop + gradient seam + shadow; studio band (not flat-tone) crops them, achromatic flat tones only when NOT studio (else eats translucent discs), white/clear/silver/black bail; backfill --remask re-crops stored squares.
- [Invite email send-site fan-out](invite-email-send-site-fanout.md) — sendAdminInviteEmail (positional) fires from 9 sites (5 routes.ts + 2 npoPortal + 2 pressPortal); signature/template changes must thread all; branded inviter avatar + "on behalf of {org}" via resolveInviterBranding.
- [Membership-backed role resolution](memberships-resolution.md) — getUserRole derives the primary hat from the memberships SET (findMembershipForScope for gates); additive + dual-written, legacy columns stay authoritative; synth-from-legacy when table absent so dev≡prod; verb GRANTs stay on partner_permissions, membership.permission_overrides is a P3-only mirror.
- [Unified identity link](unified-identity-link.md) — one human=one account via users.customer_user_id (no FK, partial-unique); fan row canonical; link only on TRUSTED paths (never self-serve register); mirror OAuth identities BOTH ways on EVERY attach/detach (not just link time); never copy !oauth-only placeholder into fan pw.
- [Unified identity P3 hat-switcher](unified-identity-hat-switcher.md) — active-membership (ALS) narrows getUserMemberships→getUserRole/findMembershipForScope so the whole admin shell re-scopes for free; every grant funnels through applyAdminInviteGrant (addMembership on existing, NEVER setUserRole which nukes other hats); password invite-accept refuses a new password for an existing email (takeover guard).
- [Shared synced-lyrics surface](shared-synced-lyrics-surface.md) — mobile Player + desktop immersive player render the SAME `SyncedLyrics` (engine in lib/syncedLyrics.ts); change karaoke timing/scroll/blur in one place or the two surfaces drift.
- [Shared desktop fan rail nav](shared-fan-rail-nav.md) — desktop rail items live in one FanRailNav rendered by BOTH album-page + storefront rails (each keeps own header/footer); Collection lens is URL-driven via ?tab=.
- [Shared album-card component](shared-album-card.md) — one AlbumCard renders Collection/ArtistDetail/Search album surfaces (grid|row); hover Play + "…" menu are pointer-only via useCanHover, touch = tap-to-navigate.
- [Mobile player pointer scrubber](mobile-player-pointer-scrubber.md) — iPhone player progress/volume are hand-rolled pointer-capture sliders (not range inputs) so a finger-drag seeks; volume hidden on iOS (WebKit volume read-only); volume state lives on PlayerContext, never a local useState.
- [createUser seed-albums full-row select](createuser-seed-albums-fullrow-select.md) — route tests that mint a user 500 on drifted clones (createUser SELECTs every albums column); guard with ADD COLUMN IF NOT EXISTS in before hook, never db:push.
- [schema-drift guard](schema-drift-guard.md) — validation step reflects pgTables via getTableConfig vs information_schema, fails on missing table/column (dev+prod); fix = idempotent ADD COLUMN/CREATE TABLE in post-merge.sh; direction-only (DB-only cols ignored).
- [drizzle ANY(${jsArray}::type[]) is broken](drizzle-any-array-spread.md) — JS array spreads to scalar placeholders → "malformed array literal" 22P02 500; use pgArray(); `length>0` guards hide it until the array fills; un-migrated landmines remain.
- [Cached-plan self-heal + DB error unwrap](cached-plan-self-heal.md) — real pg error lives on err.cause (drizzle hides it as "Failed query:"); pool.query is wrapped to recycle the connection + retry once on 0A000 "cached plan"; alerts/[admin-list-error] read res.locals.dbError for SQLSTATE+detail.
- [Order materialization test seam](order-materialize-test-seam.md) — materializeOrderFromSession takes optional {stripe} for tests; UNPAID fixture session inserts order_items but skips paid side-effects, so seed only customer/album/sku.
- [Preflight side-break source](preflight-sidebreak-source.md) — masters-preflight Tracklist/Side-length derive sideBreaks from songs.vinylSide/vinylOrder (the vinyl-order panel), NOT a separate field; never build a standalone side-break editor.
- [Public release staging-preview + view-as-fan](public-release-staging-preview.md) — locked /<slug> Preview&Purchase page already exists; admin slug resolves while prepping (og stays gated) + ?fan=1 lens flips admins into the locked view; Nightbirde data is prod-only.
- [Hide-preview gate](hide-preview-gate.md) — per-track preview embargo already enforced server-side + in client queues; the ONLY fan-UI gate is trackPlaybackState (isPreviewable===false → "locked"); don't re-flatten to store-wide.
- [Admin-aware includeHidden fan-out](admin-aware-includehidden-fanout.md) — every fan-facing album read (detail, buy-options, slug, SKUs) must thread `{ includeHidden: isAdmin }` or god-view 404s staged/hidden releases while the page itself loads; commerce.ts keeps a LOCAL viewerIsAdmin (cycle risk vs routes.ts).
- [Album credits surfaces](album-credits-surface.md) — shared buildAlbumCreditGroups aggregates Apple's 3 buckets w/ role subtitles; DESKTOP modal owner-gated (after buy) & [Apple-polished](desktop-credits-apple-polish.md) (thin rules, non-caps headers, person drill-down swaps modal content not a sheet, rows gated by rich-profile probe); MOBILE keeps pre-purchase SuperCredits teaser — split intentional, don't unify without asking Bill.
- [Desktop not-owned album row](desktop-not-owned-album-row.md) — preview/buy surface deliberately drops shuffle + album-⋯ + per-track ⋯ (showMenu={isOwned}) + padlock glyph; Buy pill defaults "Buy Now", hovers to price in a fixed-width grid cell.
- [Seed-album auto-grant signature](seed-album-autogrant-signature.md) — retired createUser seed grant; find leftovers via album-1..4 + is_preview=false + cert IN (12,7,3,21) + no paid order (cert-IN not exact pairing; comps/purchases store NULL cert).
- [Desktop lyrics test jsdom dispatchEvent](desktop-lyrics-test-dispatchevent.md) — wouter v3 patches history.pushState→GLOBAL dispatchEvent; inline jsdom test setups must mirror window.dispatchEvent onto globalThis or the lg-panel/breakpoint tests throw on setup; lg lyrics panel's documented onCloseLyrics × was never rendered until this.
- [gogoods prod bulk import](gogoods-prod-bulk-import.md) — single-txn importer must batch inserts (chunk 500) or it dies on prod's 5min idle-in-txn cap; gogoods source violates orders' (album_id,good_deed) + payment_intent uniques (resales) → dedup most-recent-wins; never touches users table.
- [goGoods profile-only fans have no source](gogoods-profile-only-no-source.md) — the ~1,871 profile-only goGoods customers (0 orders/0 collection) genuinely have NO txn/collectible rows in the export we hold; not an importer gap, just registered-never-purchased; don't fabricate purchases.
- [Desktop album cert data is order-derived](desktop-album-cert-data.md) — desktop ApiAlbum has NO ownedCertificates/purchases/certificateNumber (mobile spreads staticAlbum demo data); derive ownedNums from the /api/orders cache; ProvenanceSheet events are hardcoded demo, OwnershipSheet needs ownedCertificates stamped on the album cast.
- [Fan search nullable-.length crash + self-diagnosing reports](fan-search-nullable-length-crash.md) — `r?.cat.length` only guards the parent; a null category array in the /api/search payload crashed the whole fan app (useFanSearch backs BottomNav everywhere). Guard parent AND array. Prod stacks: keepNames:true + componentName parsed from componentStack into error reports.
- [Drizzle bare-id correlated subquery](drizzle-bare-id-correlated-subquery.md) — `${table.id}` inside a raw EXISTS/IN subquery renders UNqualified "id" that binds to the inner table (silent wrong counts, or ambiguous-col 500 when joined); use literal `table.id`, verify via toSQL().
- [Publishing payouts](publishing-payouts.md) — mechanical settlement on units PRESSED (flat $/song), separate ledger from sales; track_publishing_splits+organizations+payout_accounts+earmark/release rail already exist; Nick catalog is prod-only; dedup payee strings before creating accounts.
- [Tailwind var-opacity gotcha](tailwind-var-opacity.md) — `bg-[var(--brand-x)]/NN` silently produces no color (Tailwind can't alpha a var); use solid bg + `opacity-NN`, `--brand-*-soft` vars, or inline `style` glow.
- [EasyPost historical pull → order backfill](easypost-historical-pull.md) — shipments index empty, reports are ZIP, reference null → best-effort email+date match (honest 3-tier confidence); shipping_address MUST be StripeAddressSnapshot shape; 2k-row prod write = single json_to_recordset UPDATE.
- [Stripe-Account-Id vs IDP drift](stripe-account-id-drift.md) — re-authing a disconnected Stripe account (auth:reauth_only) replaces the IDP-stored ID but NOT the payout_accounts table row; verify current owner matches the IDP's stripeAccount before writing payouts; fix: upsert row by IDP-ID, never assume it exists.
- [Order payment snapshot only lands at materialization](order-payment-snapshot.md) — card brand/last4/wallet/receipt are captured ONLY in materializeOrderFromSession, which early-returns on already-`paid`; new fields never backfill onto pre-existing paid rows or via webhook replay — write a PI-keyed one-shot backfill if you need history.
- [Artist dashboard ↔ buyer-roster scope parity](artist-dashboard-roster-scope.md) — both must share ONE album scope: primary_artist OR payout-owner AND deleted_at IS NULL; status set (4 paid-ish) already matched; mirror any change to both queries.
- [Person projection read-back + shared people query key](person-projection-readback.md) — a people column written by PUT but missing from toPublicPerson can't be read back (save "reverts"); shared ["/api/people",id] keys with diff queryFns race under staleTime:Infinity; 202=approval-divert not saved.
- [Exclusive-disclosure row + sub-editor coupling](exclusive-disclosure-mode-coupling.md) — accordion `expanded` must derive SOLELY from the controller flag, not OR'd with local `mode`, or opening a sibling can't collapse a row that has a sub-editor open; reset mode on collapse.
- [App bundle IDs (iOS vs Android)](ios-bundle-id-vs-android.md) — iOS App Store app `Io.GoGoods.music` (Apple ID 6448246869), keep in lockstep across capacitor+pbxproj×2+codemagic×2+AASA; Android applicationId `com.gogoods_mobile` is the immutable Play pkg w/ existing installs (Gradle ns/Java pkg stays `fm.goodtunes.player`, assetlinks SHA baked in, CI versionCode queries ALL tracks — [details](android-keep-gogoods-listing.md)).
- [Codemagic App Store signing needs a real private key](codemagic-distribution-cert-private-key.md) — iOS build dies at "Build the signed .ipa" with a MISLEADING "profile requires capabilities" error; real cause (in the code-signing SETUP step log) is no distribution-cert PRIVATE KEY — ASC API key can't supply one, must load a .p12.
- [Native buy gating vs desktop surface](get-my-domain-split-and-native-no-buy.md) — buying is web-only on get.goodtunes.music (→ my.goodtunes.music player); native apps hide Buy via `buyEnabled=!isNative` (Android-off reversible); BUT every "is desktop?" check must be [width-only (useDesktopShell)](fan-desktop-surface-width-only.md), never `&& buyEnabled`, and hide native-forbidden CTAs in-component.
- [Rebase multi-region leftover markers](rebase-multi-region-leftover-markers.md) — one file can have >1 conflict region; resolving the first + git add commits leftover =====/>>>>> markers that only break the build post-rebase; grep marker-count==0 per file before add + sweep repo + re-check schema-drift on dev AND prod.
- [Fan shipping charge](fan-shipping-charge.md) — real shipping from shipping_rates card; base vs $1 markup stay separate cols, markup once/order, recompute server-side, physical no-quote REFUSE (422) never $0, lock Stripe allowed_countries=[picked], persist breakdown in BOTH materialize paths.
- [ArtistDetail not-found gate](artist-page-notfound-gate.md) — fan artist link works; false "Artist not found" comes from gating on the owned-only artistAlbums; gate exists/not-found on ownership-INDEPENDENT goodTunesTitles + artistPerson.
- [Manager partner derived catalog](manager-partner-derived-catalog.md) — manager mirrors label but catalog is DERIVED from roster people (people.manager_id); NO albums.manager_id; not in PAYOUT_OWNER_KINDS; distinct from teammate sub_role 'manager'.
- [Fan album route owner-bypass](fan-album-route-owner-bypass.md) — GET /api/albums/:id 404s a fan's OWN album once hidden/sunrise/trashed; re-read with includeHidden/includeTrashed when userOwnsAlbum. Plus: new fan routes need STOREFRONT_ROUTE_PREFIXES + FanScreen shell or they lose the rail.
- [iPad rail dvh + dock overlap](ipad-rail-dvh-dock-overlap.md) — fan desktop rails must size off 100dvh + env(safe-area-inset-bottom), never raw 100vh/fixed bottom inset (iOS chrome eats it); the compact fan dock only goes edge-to-edge (overlapping the left rail) below 1100px, so reserve dock clearance only at iPad width.
- [Digital GoodDeed cert name confirm](digital-cert-name-confirm.md) — digital-only owners edit the printed name via a per-order field (orders.certConfirmedName), NOT a signed_cert_certificates row (would pollute print queue); editable only when no real cert row exists; PDF Path-2 prefers it over the synthesized name; physical add-on keeps the operator flow.
- [Rail-aware PlayerDock channel docking](rail-aware-dock-channel.md) — desktop fan dock centers on host-supplied channelLeft/channelRight (gutter between rails), NOT window; auto-disables below 1100 to keep the intentional iPad overlap; album lyrics aside now runs full 100dvh flush to bottom like the storefront rail (LYRICS_DOCK_CLEARANCE removed).
- [Dropbox import dispatcher](dropbox-import-dispatcher.md) — one dispatcher feeds all 4 importers; folder + single-file `.zip` shares extract via the central directory (never streaming Parse); zip detect MUST be ext-gated (`.docx`/`.xlsx` are PK-zips too — sniff only when no extension) or lyrics import breaks.
- [gogoods duplicate albums strand buyers](gogoods-duplicate-albums-orphaned-buyers.md) — deleted dup albums leave ownership+cert+orders on a dead row; reconnect to live album, but gogoods double-imports share GoodDeed numbers → guard order repoints against the (album_id,good_deed_number) unique.
- [In-tree native iOS Capacitor plugin](native-ios-capacitor-plugin.md) — native-only iOS capabilities (e.g. system volume) are committed .swift in ios/App, NOT npm; cap sync ignores them, register by hand in 4 pbxproj spots; set volume via hidden MPVolumeView slider; gate on isNativeIOS/isWebIOS.
- [mobile-player platform stub mirror](mobileplayer-platform-stub-mirror.md) — the scrubber test stubs @/lib/platform; any new export Player.tsx imports (e.g. isWebIOS) must be added to the stub + flipped by __setTestIsIOS or the test fails to link.
- [requireAdmin admits partner accounts](requireadmin-includes-partners.md) — requireAdmin only proves isAdmin=true and partner roles are isAdmin too; operator-only mint/god routes need an explicit getUserRole super_admin/admin check on top (partner verbs gate EDITS, not CREATES).
- [Fan account deletion = anonymize-in-place](account-deletion-anonymize.md) — DELETE /api/customer/me scrubs PII + revokes tokens/identities/favorites/playlists/library but KEEPS the row + orders (FK retention); no deletedAt col; UI at Account→Privacy.
- [Native Capacitor remote origin](native-capacitor-remote-origin.md) — native build must set capacitor server.url to the live host; all /api + asset paths are relative, so a bundled capacitor://localhost build hangs the auth gate (navy screen, broken-logo glyph, dead spinner).
- [Capacitor push notifications](capacitor-push-notifications.md) — token needs 3 legs (web-bundle JS + iOS AppDelegate→NotificationCenter forwarding + npm plugin sync) or it silently never arrives; delivery credential-gated (APNs ES256/.p8 + FCM v1), inert `[push:dry-run]` without keys.
- [Silent publish-build failure](silent-publish-build-failure.md) — "I published but nothing changed" = failing esbuild SERVER stage (stale deploy served), not webview cache; curl the live bundle hash + run `npm run build` locally; native iPad "blown-up iPhone" can just be the old deployed bundle (useDesktopShell was gated !isNative).
- [iOS safe-area white status-bar strip](ios-safearea-white-statusbar-strip.md) — pure-white strip over the status bar w/ solid navy just below = body-box gradient doesn't paint the safe-area-inset-top on iOS standalone (background-attachment:fixed worsens it); fix = move gradient to a fixed inset:0 z-index:-1 body::before backdrop, body transparent, html navy backstop, hide under gt-admin. Static config can be 100% correct and still show it.
- [Admin dock AirPlay vs Up Next](admin-dock-airplay-upnext.md) — admin album dock wires AirPlay (own audioRef, iOS-only) but deliberately omits Up Next (no admin right-rail; tracklist already shown).
- [Stripe Tax (checkout + Buy-sheet)](stripe-tax-checkout.md) — automatic_tax needs customer_update:auto to read the typed address; exclusive tax = amount_total is tax-inclusive so snapshot pre-tax (amount_subtotal/_shipping) or double-count; operator registers jurisdictions in dashboard; fan [Buy-sheet "Sales tax" line](stripe-tax-preview.md) (no "estimated", folds into Total) uses GET /api/checkout/tax-quote (needs US postal + Dashboard head-office addr), never a static table.
- [Post-purchase player hand-off](post-purchase-player-handoff.md) — cross customer-subdomain re-auth carries a fresh bearer in the URL **fragment** (not cookie/query, stays out of logs), picked up in main.tsx; gate on isPurchaseFunnelHost; bare /album/:id mounts AlbumDetail with NO albumId prop (use useParams).
- [Native offline download protection](native-offline-download-protection.md) — downloads are Android-only (download UI gate) but the protect+decrypt+playback code is platform-agnostic on isNative; web is a pure localStorage-flag no-op; native files are AES-GCM encrypted in Directory.Cache (backup-excluded) under a non-extractable IndexedDB key (NOT hardware Keychain/Keystore); PlayerContext is offline-FIRST on native (offlineSrcFor → decrypted blob: URL, else Mux); revocation lives in DownloadEntitlementGuard with a success-gated /api/my-albums (fail-OPEN, never purge on error).
- [iOS setPointerCapture kills tap+drag](ios-setpointercapture-breaks-drag.md) — fan DESKTOP/iPad scrubber+volume+lyrics drag must use window-bound listeners (lib/useRailDrag.ts), NOT setPointerCapture (fires immediate pointercancel on iPadOS WebKit); mobile lyrics now has manual scroll TOO, with swipe-dismiss scoped to the header bar so they don't fight.
- [Toast setTimeout hangs client tests](client-test-toast-settimeout-hang.md) — a component that calls toast() in a test arms a 1,000,000ms TOAST_REMOVE_DELAY setTimeout the jsdom harness DOESN'T capture (it only wraps setInterval) → buffered tsx --test process hangs ~1000s after all tests pass (no TAP summary, EXIT 124); wrap+clear setTimeout in the test.
- [Artist Rig surfaces](rig-feature-surfaces.md) — track rigs ride in getAlbumCredits().bySongId[id].rigs (NOT getCredits); mutations must invalidate ["/api/albums",id,"credits"]; Fernando demo is prod-only marker-no-op-on-absence.
- [Rig accessories are inventory-backed](rig-accessory-inventory-link.md) — accessory value field is the reusable GearPicker (search inventory or paste-URL scrape→create shortCategory:"Accessory"); optional rig_accessories.instrumentId, keep {type,value} text for fans; any per-accessory field must thread BOTH clean() helpers + all 3 storage read maps or it silently strips/won't rehydrate.
- [Native plugin version skew + iOS icon](native-plugin-version-skew.md) — new Capacitor plugin called from the remote-origin bundle throws on older installed binaries → un-awaited addListener reject paints the global banner; gate on isPluginAvailable + await (republish fixes it, no new build). Single-size 1024 iOS app icon renders blank in TestFlight; ship the full explicit no-alpha icon set.
- [AlbumDetail mobile hook order (React #310)](album-detail-hook-order.md) — every hook in AlbumDetailMobile/Desktop must sit ABOVE the `if (!album ...) return` loading/not-found guards or the loading→loaded render runs more hooks → React #310 crashes the whole fan app; deployed bundle has no real source map but keeps `a(fn,"Name")` registrations for mapping minified names.
- [Native download cert pinning](cert-pinning.md) — pin the long-lived ISRG (Let's Encrypt) ROOTS not the ~60d leaf; WebView fetch ignores pin config so route downloads through native URLSession/HttpsURLConnection; scope to goodtunes.music only (Dropbox/Mux unpinned to avoid bricking); null=unavailable→fallback but real pin failure must propagate.
- [Android notification icon + splash](android-notification-icon.md) — status-bar small icon MUST be white-on-transparent silhouette (alpha-only, else gray box); ic_stat_notify wired via FCM default meta-data + server payload; splash is white "G" on navy #00062B, not white bg.
- [Apple Pay / Google Pay checkout](apple-pay-wallets-checkout.md) — wallets auto-surface INSIDE Stripe Embedded Checkout (no Express Element); need well-known association file served per fan host + paymentMethodDomains registration (boot hook in applePay.ts); never add payment_method_types; test-mode "active" ≠ prod wired.
- [Mini-player viewport bounding](miniplayer-viewport-bounding.md) — fan mobile mini-player width must cap with `min(390px, 100vw - env(safe-area-inset-*))` not `w-full`, or a transformed ancestor / iOS visual-viewport mismatch runs the centered pill off the right edge on scroll; inner pill must be a definite-width block (no flex) so long titles can't grow the capsule.
- [Person sheet bio strip + gear list](person-sheet-bio-and-gear.md) — Apple-Music "Listen to music by…" boilerplate killed in 3 layers (import/backfill/render+richness); gear list shows unlinked instrument credits as plain rows (vocals/production excluded); SheetClose now defaults glass not fill.
- [GitHub build-mirror push](github-mirror-push.md) — auto-sync to billdenk/goodtunes-app for Codemagic; push MUST fetch remote tip first + upload new LFS objects by `--object-id` or it 500s/GH008-rejects; manual catch-up runs as a WORKFLOW (bash bg reaped).
- [Nightbirde "Hope" canonical album](nightbirde-hope-canonical-album.md) — go-live Hope is the 7" `b250a5a5` (slug `hope`, has songs+SKU+addons); an EMPTY single_lp `54d46505` decoy exists and STOREFRONT_LAUNCH_ALBUM_ID wrongly pointed at it; verify songs+SKU before trusting any "Hope" row.
- [Full tsc isn't the type gate](tsc-gate-vs-tsx.md) — dev runs tsx (no check) + ES5 tsconfig.json has 100s of pre-existing errors; a NEW server route using a forgotten @shared/schema import compiles in dev AND passes the test workflow, surfacing ONLY under full tsc — grep your new symbol, don't trust "dev works".
- [Stripe order recording in prod](stripe-order-recording-prod.md) — "no app orders / tax_cents NULL" is usually data/operator state not a code bug: check `albums.is_prepping` (staged = locked, no checkout) + whether ALL orders are `origin='legacy:gogoods'` (NULL tax = legacy imports) before suspecting materialization; webhook secret is ONE global Replit Secret that can't match both test+live endpoints.
- [App-icon guards + smoke](app-icon-guards.md) — 4 guards (source res/appiconset + built .aab/.ipa), one `app-icon-guards-smoke`; verified false-positive-free vs navy "G" (lum ~0.30<<0.7, 192px floor) + AAPT2 `-v4`/`-v26`; Linux = ImageMagick fallback + loose-PNG path only (iOS assetutil/Pillow checks mac-only, skipped).
- [Anon preview is publish-not-release](anon-preview-is-publish-not-release.md) — "launched but logged-out previews silent" = stale deploy, NOT release flag; playback gate ignores is_prepping; curl live playback-url anon (+?k= tokens), all-401 = republish needed; bare campaign link=notify waitlist, /staging=forces buy.
- [Linked-admin session vs token boundary](linked-admin-session-token-boundary.md) — getAuthFromRequest checks session BEFORE token, so a linked admin/fan whose session kind=customer 401s on the admin host; fix = honor a Bearer token matching THIS host's kind (pure `resolveAuthAcrossBoundary`). Test it as a pure helper, NOT over HTTP (undici strips `Host`; secure cookie needs `X-Forwarded-Proto:https`+trust proxy).
- [Invite capability mirroring](invite-capability-mirroring.md) — AdminInvites must NOT re-derive who-can-invite; computeInviteCapability() in partnerPermissions.ts mirrors the POST /api/admin/invites gate and is surfaced via /api/me/role (canInvite/allowedInviteRoles/allowAdvancedInvite); gate uses SCOPE-level inviteSubusers (NOT per-user override); non_profit never invites (absent from PARTNER_SCOPE_KINDS).
- [Fan preview window](fan-preview-window.md) — 30s previews honor per-song previewStartMs/EndMs (on currentSong via hydrate, no server change); fall back to first-30s, clamp ≤30s; EVERY scrubber AND all 3 SyncedLyrics onSeek must clamp into the window (lyric-tap is the sneaky compliance bypass), highlight stays absolute.
- [Public album read for logged-out fans](anon-album-read-public.md) — GET /api/albums/:id was requireAuth so anon fans 404→"couldn't find that album" on LIVE releases; client staleTime fix only covers prepping previews + launch root primes nothing; fix = optionalAuth public visibility-gated read mirroring by-slug.
- [Finish-signup handle vs legacy username collision](finish-signup-handle-username-collision.md) — fan @handle availability must check BOTH handle AND username cols (write mirrors username=handle, username is globally unique); legacy gogoods imports squat usernames → "available" then 500; unwrap err.cause.code + match customer_users_username_unique for a friendly 409.
- [Branded album-cover placeholder](album-cover-placeholder.md) — One shared AlbumCover decides every cover fill (real art → ghosted artist photo → brand tile); never drop a raw album-art <img> again.
- [Buyer location billing fallback](buyer-location-billing-fallback.md) — digital/donation add-ons collect billing-only at Stripe → shipping_address all-null → reports showed "Location unknown"; every buyer-location read must coalesce shipping→billing (LOC_CITY/REGION/COUNTRY in reports/buyers.ts, alias `o`); NOT the fulfillment CSV (shipping-label only).
- [Fan "no sound" playback diagnoses](webkit-autoplay-unlock.md) — WebKit blocks unmuted audio when async-signed-Mux play() lands OUTSIDE the tap (dock playing, silent; Chrome masks via MEI) → gesture-bound silent-clip bless on the persistent Audio el (success-driven, never restore over real src); SEPARATELY "silent + zero playback-url POST" = album page re-map dropped [muxPlaybackId/muxStatus](desktop-playersong-mux-fields.md) so resolveStream bails (phantom equalizer).
- [Operator sign-in link](operator-signin-link.md) — welcome-back token = generic sign-in link (redeem has no eligibility gate, unlike self-service email); super_admin-only POST /signin-link; build URL via customerOriginFromReq, NOT originForKind (no dev branch).
- [Order Desk auto-push OFF by default](orderdesk-autopush-gate.md) — paid physical orders DON'T auto-hand-off; flow is aggregate→artist confirms press qty→one press order→then routing; auto-push gated behind `ORDERDESK_AUTO_PUSH` (helper in orderDesk.ts), manual "Push to OD" + status webhook always live.
- [Odoo printer integration](odoo-printer-integration.md) — 2nd fulfillment connector parallel to Order Desk; PULLS status on a timer (no webhook), JSON-RPC, env-gated dormant, single-instance `is_odoo_printer`, operator-only "Push to Odoo". Plus: test workflow stays "running" after pass (schedulers don't unref) — read per-file # pass/# fail, don't wait.
- [OAuth duplicate-account recovery](oauth-duplicate-account-recovery.md) — Apple/Google re-auth after an app update mints a NEW empty account (real email→contact_email); merge moves content NOT identities so survivor MUST be the OAuth holder; admin "Combine accounts" tool shipped (recommends OAuth survivor, undo+audit).
- [Publisher payout status lazy-sync](publisher-payout-status-lazy-sync.md) — payoutsEnabled only advances on Stripe's fragile account.updated Connect webhook; status reads (GET /api/publisher/me) must live-retrieve+sync while still false (bounded, best-effort) or the portal banner sticks on "in progress" forever.
- [Interactive content in <label> breaks Safari](interactive-in-label-safari.md) — nesting a combobox/buttons inside Field's <label> swallows clicks on Safari only (Chrome+jsdom honor "do nothing for interactive descendants"); Field has as="div" for picker fields; jsdom can't reproduce so guard structurally (combobox.closest("label")===null).
- [Admin order search deep-link](admin-order-search-deeplink.md) — search opens the matching fan/pressing order via `?orderId=`; pressing page MUST default status to "all" (per-status fetch) or the row is filtered out before it can expand.
- [gibson.com is one Gibson maker](gibson-com-is-one-maker.md) — operator rule: every product on gibson.com (incl. Epiphone, "Gibson Custom", "Gibson Mod™") is ONE Gibson maker; keep gibson.com OUT of SUB_BRAND_PARENT_HOSTS (it mis-promotes a host's own product LINES into separate maker cards).
