- [Authed admin screenshots](admin-authed-screenshots.md) — Screenshot tool keeps cookies across calls: shoot /dev-login?email=<admin> once, then shoot the /admin page; isDev panels hide (host=127.0.0.1).
- [Grant numbers GR sequence](grant-numbers-gr-sequence.md) — comped copies use user_albums.grant_number ("GR NN"), separate from paid certificate_number; no ??1 fallbacks; mint via assignNextGrantNumber + retry.
- [Vendor → Maker/Reseller rename scope](vendor-maker-reseller-rename-scope.md) — gear-supplier `vendor` shows to operators as Maker/Reseller (UI copy ONLY, never identifiers/role value/table/routes/testids); GoodDeed printing/payout "vendor" is a DIFFERENT concept, left alone.
- [Partner portals light-only](partner-portals-light-only.md) — invited-partner/operator portals use the LIGHT admin slate theme (not dark fan navy); the order-status pill is duplicated across 3 dashboards — keep colors in sync with AdminCustomerDetail.
- [Deep links claim my. not apex](deeplink-claims-my-not-apex.md) — Universal/App Links claim only `my.goodtunes.music` (app subdomain), NOT the bare `goodtunes.music` apex; the apex is the Webflow marketing site (A record -> Webflow via Cloudflare) which cannot serve association files; fan-shareable links live on `my.`/`get.`, so dropping the apex claim costs nothing.
- [App Store submission runbook](../../docs/app-store-submission.md) — reviewer / demo-account / privacy-label runbook (repo doc; pair with docs/store-review-readiness.md + native-builds.md); in-app account deletion at Account → Privacy → Delete My Account; Buy/Chat gated by platform-specific flags. [iOS build→TestFlight gates](ios-build-submission-gates.md): build "fails at Publish" AFTER the full Mac build for 2 Apple-side reasons — an APPROVED (not-yet-released) MARKETING_VERSION closes the train (bump it + Account.tsx; version guard only checks READY_FOR_SALE), and external TestFlight needs a one-time Beta App Description in ASC Test Information (internal testing doesn't); binary uploaded VALID = don't re-upload.
- [client React component tests](client-react-component-tests.md) — no vitest; tests run via tsx --test + jsdom under tsconfig.test.json (jsx react-jsx) with TSX_TSCONFIG_PATH on the `test` validation cmd; needs wouter/framer/scrollTo global stubs + PlayerDock boots collapsed. [jsdom dispatchEvent](desktop-lyrics-test-dispatchevent.md): wouter v3 patches history.pushState→GLOBAL dispatchEvent, so inline jsdom setups must mirror window.dispatchEvent onto globalThis. [Toast setTimeout hang](client-test-toast-settimeout-hang.md): a test that calls toast() arms a 1,000,000ms setTimeout jsdom DOESN'T capture → the tsx --test process hangs ~1000s after tests pass (EXIT 124); wrap+clear it.
- [napi-rs native binaries break esbuild bundle](napi-canvas-build-externals.md) — any `@napi-rs/*`/sharp-style pkg whose native `.node` ships in platform sibling pkgs needs a `pkg-*` glob in script/build.ts externals (siblings aren't in package.json; dev passes, publish fails). [npm override vs direct dep](npm-override-direct-dep-conflict.md) — a pkg in BOTH dependencies and overrides needs `"$name"` in overrides or bumping EOVERRIDE-breaks npm install.
- [Schema drift: publish diff + manual fix](dev-prod-schema-drift.md) — publish diffs dev DB→prod DB (missing dev objects DROP from prod with data); fix = idempotent CREATE/ALTER on the lagging DB ([sweep/seeds](albums-schema-drift.md)). [Obsolete create races publish](post-merge-obsolete-create-races-publish.md); ["applied to dev" claims untrustworthy](migration-claims-vs-reality.md) — verify BOTH DBs; [press roster drift](press-roster-dev-prod-drift.md) — ID-preserving INSERTs in post-merge.
- [Admin role columns out of pgTable](admin-roles-out-of-table.md) — users.role/role_scope_id live in the DB but not in shared/schema.ts; read/write via server/auth/roles.ts raw SQL, not drizzle.
- [Partner permissions + post-sale lock](partner-permissions-and-post-sale-lock.md) — five per-scope verbs gate every partner endpoint; only edit_metadata respects the post-sale lock (masters/shopify/payouts stay live); lock returns 403, not a divert; approval-required is a separate "divert to queue" layer on top. UI uses GET /api/admin/albums/:id/edit-access to disable inputs without speculative POSTs. [Operational verbs bypass the lock](vendor-pricing-bypasses-post-sale-lock.md): per-leg vendor assignment, per-vendor GoodDeed pricing, payouts stay editable after first sale; only the fan-facing addon metadata (price/min/qty) respects edit_metadata.
- [Gear scrape gotchas](instrument-scrape-brand-identity.md) — skip the company's own `@type:Product` (brand-as-gear trap). [Shopify JSON shops](shopify-json-gear-scrape.md) — `/products/<handle>.json` hosts, never fabricate a domain. [gibson.com=one maker](gibson-com-is-one-maker.md) — keep it OUT of SUB_BRAND_PARENT_HOSTS. [Makers vs Resellers](makers-vs-resellers.md) — vendors carry is_maker+is_reseller (row can be both); `?role=` in URL string not queryKey.
- [Press awaiting queues exclude SPIN Promos](press-awaiting-excludes-spin-promo.md) — 4 lockstep awaiting/pre-pressing queries need `is_spin_promo=false` or digital-only promos strand as "Awaiting pressing order". [Per-press default jacket image](press-default-jacket-image.md) — manufacturers.vinyl_placeholder_url drives catalog preview + seeds artwork for albums created under a press; non-press creates stay on /album-placeholder.svg.
- [Loose/leftover FK drift](auth-tokens-fk-recurrence.md) — old dev DBs carry leftover FKs the schema already dropped; publish diffs dev→prod and re-adds them (auth_tokens 500s customer signup verify), fix = idempotent DROP CONSTRAINT IF EXISTS in `scripts/post-merge.sh` on both DBs; [user_albums.user_id](user-albums-loose-fk.md) is the same inert drift — stores customer_users.id though the FK points at users.id, so write the fan ID, never join users.
- [Mux-only fan playback](mux-only-fan-playback.md) — fan player must never fall back to raw audioUrl when Mux signing fails; capabilities.md promises masters never leave as a file. Stop playback instead; admin previews are the only exception. [Bonus-video pipeline](bonus-video-mux.md): album_videos stream signed HLS like audio; the SHARED fan/admin GET /albums/:id/videos must strip videoUrl for non-admins (else the /objects master leaks); webhook matches songs THEN album_videos; public playback-url lazy-ingests legacy rows.
- [iOS native black-screen root causes](ios-scene-manifest-black-screen.md) — ANY UIApplicationSceneManifest risks black-screening the store-signed binary (2 prior real-device failures, "declare both roles" is NOT proven safe); any scene-manifest feature must ship behind a gate stripping BOTH the manifest + its entitlement until on-device verified. Also covers the separate `server.errorPath` cold-launch black-screen — see [remote-load-failure-black-screen.md](remote-load-failure-black-screen.md).
- [Quickprinter routing](quickprinter-vs-press.md) + [portal](quickprinter-portal.md) — GoodDeed printer/hologram/insertion routing is platform-default on payout_settings (not per-album); `is_quickprinter` is mutually exclusive w/ `is_maker`, ladders per-paper-size fixed rungs; these vendors get a scoped read-mostly portal (VendorScopeRouter→/api/printer/:id/*), lock→render→mark-printed lives ONLY in shared runCertPrintBatch (admin+printer both call), no routing/pricing/roster writes.
- [Soft-delete trash](soft-delete-trash.md) — 14 admin tables soft-delete via `deleted_at`; every read on those tables MUST filter `isNull(deletedAt)` or rows leak back into player/search after delete. Blobs not copied.
- [Blank / stale-deploy triage](admin-blank-page-triage.md) — "admin is blank" / "published but nothing changed" / native iPad "blown-up iPhone" is stale client state or a failing esbuild **SERVER** stage serving the OLD deploy, NOT a code/PWA/webview-cache bug; headless-screenshot + curl the live bundle hash + run `npm run build` locally before touching code (no service worker, so never a PWA cache). [Silent publish-build failure detail](silent-publish-build-failure.md): esbuild server stage failing = stale deploy served; the iPad case was useDesktopShell gated `!isNative`.
- [Deploy image > 8 GiB](deploy-image-size-limit.md) — publish fails at the FINAL layer step (`image size is over the limit of 8 GiB`) though build succeeds; fix = `.replitignore` dropping `.git` + tooling dirs + non-imported `attached_assets`; keep BOTH `@assets/` imports AND server runtime fs-reads.
- [Per-copy order entitlements](per-copy-entitlements.md) — multi-quantity orders fan out into `order_copies`; any GoodDeed number floor MUST include that table or refunds/cert reads silently re-mint printed numbers.
- [Press catalog shape](press-catalog-shape.md) — per-press vinyl pricing is formats → tiers (jsonb price ladder) → colors; manufacturing comes from the tier's ladder, platform pricing only covers publishing/processing/margin + free-flow fallback; SKU snapshots names not ids. [unitCents](press-catalog-units.md): price_ladder[].unitCents is per-unit cents, NOT vendor total dollars (seed = round(total$*100/qty)). [SKU press resolution](sku-press-resolution.md): pressId comes from the chosen tier row, not the invited press. [Pricing sources + sync lock](press-pricing-sources-and-lock.md): only lockedFromSync rungs survive re-sync; importer can't write "*Colors" tiers.
- [Press color swatches](press-color-swatches.md) — catalog colors store only a solid `swatchHex` (+ optional photo `swatchImageUrl`), never the in-code gradient; backfill only rows where BOTH are NULL so operator edits/imported photos win. [Fan disc preview](fan-vinyl-disc-preview-render.md): fan disc = resolveVinylColor().thumbnailUrl (admin chips/SellPanel use swatch_image_url); unset thumbnailUrl = silent gradient; SKUs snapshot color by display NAME not id → name-match required.
- [Adding a preflight vendor (plant)](preflight-vendor-id-source.md) — new plant = VENDOR_IDS tuple + VENDOR_SPECS; server route schemas must be `z.enum(VENDOR_IDS)` or the new id 400s. [Press template specs catalog](press-template-specs-catalog.md) — always resolveRequired (operator edits merged over measured baseline); vendor→press bridged by NAME.
- [GoodDeed cost stack](gooddeed-cost-stack.md) — per-cert wholesale = tiered ladder rung off resolved signed-copy count (not flat cert_cost_cents); rung bundles print/hologram/shrinkwrap/insertion + shipping legs. [Revoke free grants](revoke-free-grants.md) — operational verb, bypasses post-sale lock if no paid order. [Share-card corners](gooddeed-share-card-radius.md); [Letter signature slot](print-cert-letter-signature-slot.md) — fixed Y assumes 1-line headline, A4 immune.
- [Backend has no hot-reload](backend-no-hot-reload.md) — dev runs `tsx server/index.ts` (no --watch), Vite HMR only reloads client, so RESTART "Start application" after any server edit before curl-testing or you test stale routes; [restart EADDRINUSE](start-app-test-port-race.md) is the `test` workflow holding port 5000 (never unrefs), not a code defect — retry while a client test runs; curl /api/health 503=edge no-backend, 200=up.
- [Invite pre-flight deep-link to album](invite-preflight-deeplink.md) — landing an invitee on a specific album needs inviteRole=identity/manager + targetPersonId + preFlightedAlbumId together; quotes ride as album_skus, not on the invite; claimed People return acceptUrl=null (held for review). [Add-Admin vs Invite-Artist split](add-admin-vs-invite-artist.md): partner "Add Admin" hits POST /api/admin/partner-contacts, "Invite Artist" hits POST /api/admin/invites — both keyed on invite_subusers, so press Staff need that verb for artists but must be blocked from admin adds via pressUserCanEdit() + a separate canAddAdmins flag.
- [Gear/SuperCredits fan surfaces](gear-supercredits-surfaces.md) — fan gear page + vendor profile are SHEETS in AlbumDetail.tsx (not /instrument/:id); logo+name→vendor profile, trailing link→affiliate_url (gear), globe→brand domain; chat hidden on both. [Embedded partner album view](embedded-partner-album-view.md): artist-portal album links open AdminAlbum.tsx in `embedded` mode (no AdminFrame) inside OperatorShell; overview surfaces preview-links ONLY for partners (no fan roster privacy leak).
- [One-time data backfill to dev+prod](one-time-data-backfill-via-post-merge.md) — prod executeSql read-only + task dev DB throwaway → data resets go in post-merge.sh marker-guarded; planner-style TS importers: a missing-prereq skip must FATAL (never stamp marker → partial lock-in on fresh clone), check marker before writing a backup, concurrency-pool image mirroring (nohup dies across bash calls).
- [Observability: health probe + ops alerting](observability-ops-alerting.md) — `GET /api/health` (double-bounded DB probe) + email alert on ANY /api 5xx via res.on(finish), throttled per-instance in-memory. Also covers Sentry (manual DSN, inert w/o it, hand-attach request context bc prod is bundled), per-instance pg pool cap (PG_POOL_MAX, pool.on("error")/idle drop crashes instance), and a non-email mail-health signal (missing RESEND_API_KEY only counts as failure in prod).
- [design-lint baseline is line-content keyed](design-lint-baseline-line-keyed.md) — migrating a button to IconButton re-flags pre-existing baselined raw-hex SVGs (heart/bookmark) as NEW; re-snapshot baseline, don't convert to var; /tmp log snapshot lags, trust fresh local run.
- [Per-sale pool refund reversal](press-pool-refund-reversal.md) — any per-album pool that accrues on paid sales must reverse on refund across 3 sites (handleRefund + shopify inline route + shopify webhook); approve-and-pay endpoints flip status + side effects in one db.transaction. [Partner notifications dispatch](partner-notifications-dispatch.md) — empty events=subscribe-all; email-only v1 (others logged skipped); best-effort never throws; per-album last-notified joins log on payload_snapshot->>'albumId'.
- [Object Storage bucket shared dev+prod](object-storage-shared-bucket.md) — one repl-level bucket; dev workspace + published prod app both serve `/objects/uploads/<id>`; mirror catalog/scraped images ONCE, write the same URL into both DBs (add-memphis-metallic.ts is the template); [/objects range serving](object-range-serving.md) must handle ranges itself (edge proxy 500s on suffix `bytes=-N` + large streams), mp4 imports get `+faststart` to avoid a tail-seek. [Oversized art downscale](oversized-art-downscale.md): oversized raster art is never stored/served raw (OOMs mobile WebKit) — sharp shrink-on-load or hard-reject, never silent-raw; backfill stamps its marker only when 0 errored.
- [framer-motion vs Tailwind/CSS transform conflicts](framer-tailwind-transform-conflict.md) — when framer animates scale it owns inline `transform`; Tailwind `active:scale`/`translate-x` and CSS `transition: all` on the same el break — use whileTap, move translate to another el, narrow the CSS transition.
- [Press mode god-view (Dedicated vs All)](press-mode-god-view.md) — per-entity press_mode on people+labels (null=inherit, artist wins over label); SellPanel reads resolved mode off invited-press response; correcting the press stamp must re-resolve unlocked catalog SKU snapshots; package-designer default vendor must honor the matched press even when in HIDDEN_PREFLIGHT_VENDORS.
- [Album bonus media single CMS surface](album-bonus-media-surface.md) — fan album videos/photos come only from /api/albums/:id/videos|photos via AlbumBonusContent (mobile bonusSlot+admin previews) & DesktopAlbumView; never re-add a static editorial dup in musicData. [Preview gating](bonus-media-preview-gating.md): locked posters/photos are /objects/ masters, CSS blur leaks the raw URL — gate server-side (admin OR userOwnsAlbum) on all 3 surfaces, non-owners get a server-blurred /api/album-media/:kind/:id/preview tile.
- [7" booklet either/or bundle](booklet-7inch-bundle.md) — 7" booklet is one-price VARIANT (single CC fee, bundlePriceCents ?? sku+addon); cassette stays stacked; NEVER auto-stamp the $25 default onto legacy null-bundle rows on unrelated saves; bundle cost excludes the vinyl row's own CC fee.
- [Mockup-sandbox conventions](mockup-sandbox-asset-paths.md) — iframes can't load main-app paths; copy assets to artifacts/mockup-sandbox/public/images + ref `/__mockup/images/...`; [parallel fan-component copies](mockup-sandbox-parallel-copies.md) mirrored by hand; [verify previews](mockup-sandbox-preview-verification.md) by external_url-screenshotting `/__mockup/preview/<folder>/<Component>` (re-shoot blank first capture, cache-bust `?v=`).
- [SSRF-safe server OG/social image render](ssrf-safe-og-image-render.md) — when a server route rasterizes a share/OG image from a client-supplied art URL: same-host check (kills metadata IP) + block RFC1918/link-local for spoofed-Host + fetch-yourself (redirect:error, content-type, timeout, size cap) → gradient fallback; do NOT block loopback (dev origin is localhost).
- [Person roles vs access grant](person-roles-vs-access.md) — THREE axes: people.roles[] creative-credit tags vs single users.role access grant vs per-affiliation business title (entity_contacts/organization_people.role, BusinessTitlePicker, NEVER in roles[] or shape predicate); derivedRoles read-only; "artist" in roles[] flips shape; Permissions tab = artist scope only.
- [Account-creation paths fan-out](account-creation-paths-fanout.md) — any field stamped "at sign-up" (consent/defaults/attribution) must be written on all 5 create-account paths (signup-with-code, /api/register, OAuth signup, OAuth invite-accept, password invite-accept); createCustomer/createUser use restricted insert schemas so stamp via update/raw SQL; never re-stamp reused invite rows.
- [Odesli per-release link resolution](odesli-streaming-links.md) — Apple collection id → Tidal/Deezer/Pandora deep links, NOT Qobuz (stays search-fallback); no-key tier ~10 req/min so batch imports bound concurrency, best-effort never-throw. [Spotify token tier](spotify-token-tier-limits.md) — client-creds token has NO followers/popularity/genres + /top-tracks 403s; only /artists/{id}/albums works (limit≤10).
- [Press re-import spawns duplicate "*" tiers](press-import-duplicate-star-tiers.md) — color-library re-import makes `Opaque*`/`Neon*` twins (photos only, no hex/pricing) beside the plain tier; collapse = keep plain, fold photos on, delete `*`; cleanup-memphis-catalog.ts is the template; importer bug unfixed. [GoodDeed design lives on canvas](gooddeed-design-lives-on-canvas.md): cert/share-card *design* is iterated in canvas mockups; ignore stale board tasks asking for cert/card design.
- [Passwordless fan set-password](passwordless-fan-set-password.md) — forgot-password skips fans with no password; account-initiated "set a password" needs its own requireCustomer endpoint. [Finish-signup handle vs legacy username collision](finish-signup-handle-username-collision.md) — @handle availability must check BOTH handle AND username cols (write mirrors username=handle); legacy gogoods imports squat usernames → "available" then 500; unwrap err.cause.code for a friendly 409.
- [Gift naming = Nightbirde](gift-of-hope-nightbirde-exclusive.md) + [boxes vs album-claim](gift-boxes-vs-album-gifts.md) — "Gift of Hope" is the Nightbirde Foundation's OWN add-on name (other charities name their own); server/gifts.ts hosts TWO unrelated flows: custom_addon_gift_boxes (per-recipient donation boxes, materialized empty on paid, PII gated to admin+scope-matched non_profit) ≠ the `gifts` album-claim link flow. [Per-copy vs whole-order exclusion](per-copy-gift-mutual-exclusion.md): partial uniques DON'T conflict cross-kind, so exclusivity must serialize on the order row (SELECT FOR UPDATE in createGiftRecord); revoke is terminal.
- [Per-album NPO donation split](npo-donation-split.md) — up to 4 NPO beneficiaries ≤100¢/unit, one referral_credit each at sale; funded from margin (never change price); editable until first sale then add-from-unallocated only; referral_credits has no album_id (join via order); old (order_id,kind) unique swapped for two partial uniques; refund doesn't reverse credits.
- [ChromeScrim one-blur-per-region](chrome-scrim-one-blur-region.md) — scrim owns the single per-region backdrop-filter; in-region controls drop their own blur, hand off across the EXIT fade via lifecycle timer. [iOS stacked backdrop-blur crash](ios-webkit-stacked-backdrop-blur.md) — "A problem repeatedly occurred" over scrolling images = two stacked backdrop-filters, not a JS heap leak.
- [App-review demo account seed](appreview-demo-seed.md) — seed a demo song's master by INSERT…SELECT from a static-seed song (copies that env's own valid Mux ids) to dodge dev↔prod Mux drift; ownership via user_albums grant. [Production-partner capabilities](manufacturer-capabilities.md): manufacturers carry doesVinyl/doesGoodDeed/doesFulfillment (CHECK at-least-one); one partner = many jobs; GoodDeed routing-default printer picker stays vendor-FK-keyed.
- [Adding a customer-family host](multi-host-oauth-cookie.md) — a new fan host needs host→kind in 2 files + canonical-redirect exemption + callbackOrigin returning the SAME host (cookie is host-only) + IdP redirect-URI registration, or OAuth 403s.
- [Admin list state in URL](admin-list-state-in-url.md) — admin Albums mirrors tab/view/search/filters into the URL & round-trips the WHOLE list query through detail links as one encoded `albumsReturn` param (not per-control); reusable for other index pages.
- [Per-release share slugs](share-slug-routing.md) — two-part get.goodtunes.music/<artist>/<album> links; artist slug on people.artist_share_slug (globally unique); album slug unique per-artist composite; RESERVED_SLUGS must cover first segments of ALL two-segment routes too.
- [Vinyl disc mask two-tone backdrop](disc-mask-two-tone-backdrop.md) — Hellbender mock-ups have a split gray/white backdrop + gradient seam + shadow; studio band (not flat-tone) crops them, achromatic flat tones only when NOT studio (else eats translucent discs), white/clear/silver/black bail; backfill --remask re-crops stored squares.
- [Invite email send-site fan-out](invite-email-send-site-fanout.md) — sendAdminInviteEmail (positional) fires from 9 sites (5 routes.ts + 2 npoPortal + 2 pressPortal); signature/template changes must thread all; branded inviter avatar + "on behalf of {org}" via resolveInviterBranding. [Mail=Resend](mail-channel-resend-key.md): RESEND_API_KEY gates ALL mail — missing key = SILENT total outage (no [mail-failure] log → admin-OTP lockout); MAIL_FROM/REPLY_TO being set ≠ sending works; verify via a root tsx import of sendAdminOtpEmail (real recipient).
- [Membership-backed role resolution](memberships-resolution.md) — getUserRole derives the primary hat from the memberships SET (findMembershipForScope for gates); additive + dual-written, legacy columns stay authoritative; synth-from-legacy when table absent so dev≡prod; verb GRANTs stay on partner_permissions, membership.permission_overrides is a P3-only mirror.
- [Owner-self-serve stale permission tests](owner-self-serve-stale-permission-tests.md) — artist-scope OWNER (subRole=null synth) implicitly holds OWNER_SELF_SERVE_VERBS (edit_metadata/masters/credits/payouts) regardless of partner_permissions; older "owner→403" perm/delete DB tests are STALE (route now 202) + hit EADDRINUSE:5000 — not your regression.
- [Unified identity](unified-identity-link.md) — one human=one account via users.customer_user_id; link only on TRUSTED paths, mirror OAuth identities both ways. [P3 hat-switcher](unified-identity-hat-switcher.md) — grants via applyAdminInviteGrant (NEVER setUserRole, nukes other hats). [View-as scopes admin too](view-as-token-scopes-admin.md) — View-as token re-scopes ALL requests incl /admin with no exit banner; fresh tab restores.
- [Shared fan player surfaces](shared-synced-lyrics-surface.md) — mobile + desktop render the SAME SyncedLyrics (change once or they drift). [Rail nav](shared-fan-rail-nav.md) — one FanRailNav, Collection lens URL-driven. [AlbumCard](shared-album-card.md) — one card for Collection/ArtistDetail/Search; hover affordances pointer-only. [Mobile scrubber](mobile-player-pointer-scrubber.md) — hand-rolled pointer sliders; volume hidden on iOS web; state on PlayerContext never local useState. [Admin dock AirPlay](admin-dock-airplay-upnext.md) — admin album dock wires AirPlay (own audioRef, iOS-only) but deliberately omits Up Next.
- [Report release-picker GoodTunes filter](report-release-picker-goodtunes-filter.md) — ownedReleasesWithFunnel (partner funnel/release list) filters `is_goodtunes_release=true`; a report DB-test seed must set that flag or the album silently vanishes from partner lists (funnel-by-albumId still resolves it); DB column is `is_goodtunes_release` (no underscore, naive `is_good_tunes_release` throws + aborts the before hook).
- [schema-drift guard](schema-drift-guard.md) — validation reflects pgTables via getTableConfig vs information_schema, fails on missing table/column (dev+prod); fix = idempotent ADD COLUMN/CREATE TABLE in post-merge.sh; direction-only (DB-only cols ignored). [createUser full-row select](createuser-seed-albums-fullrow-select.md): route tests minting a user 500 on drifted clones (createUser SELECTs every albums column) — guard with ADD COLUMN IF NOT EXISTS in the before hook, never db:push.
- [Drizzle/pg query landmines](drizzle-any-array-spread.md) — ANY(${jsArray}::type[]) → 22P02, use pgArray(). [Cached-plan self-heal](cached-plan-self-heal.md) — real pg error on err.cause; retry once on 0A000. [EXPLAIN smoke + phantom cols](raw-sql-explain-smoke-and-phantom-columns.md) — albums/people lack created_at etc.; EXPLAIN validates raw queries without running.
- [db.execute timestamps are strings](db-execute-timestamp-strings.md) — raw `db.execute` rows return timestamp cols as STRINGS not Dates; wrap `new Date(x)` before `.toISOString()` or the shaper 500s (bit the press Albums tab once a sold album entered the set; EXPLAIN smoke can't catch shaping).
- [Order materialization test seam](order-materialize-test-seam.md) — materializeOrderFromSession takes optional {stripe} for tests; UNPAID fixture session inserts order_items but skips paid side-effects, so seed only customer/album/sku. [Drizzle bare-id subquery](drizzle-bare-id-correlated-subquery.md): `${table.id}` in a raw EXISTS/IN subquery renders unqualified "id" binding to the inner table (silent wrong counts/ambiguous-col 500); use literal `table.id`.
- [Stateful preview-pass grants](preview-pass-grants.md) — behind-the-scenes reviewer link (jti pass) is VIEW-ONLY, must NEVER authorize checkout (only the family /staging no-jti pass buys); create/revoke bypass the post-sale lock (operational verb); token one-time reveal, revoke re-checked in DB on every read.
- [Release visibility gates](public-release-staging-preview.md) — locked /<slug> Preview&Purchase page exists; admin slug resolves while prepping (og gated) + ?fan=1 lens flips admins into the locked view (Nightbirde prod-only). [Hide-preview](hide-preview-gate.md): per-track embargo enforced server-side + in client queues; only fan-UI gate is trackPlaybackState (isPreviewable===false→locked), don't re-flatten store-wide. [includeHidden fan-out](admin-aware-includehidden-fanout.md): every fan album read (detail/buy/slug/SKUs) must thread {includeHidden:isAdmin} or god-view 404s staged releases mid-load; commerce.ts keeps a LOCAL viewerIsAdmin.
- [Album credits surfaces](album-credits-surface.md) — shared buildAlbumCreditGroups aggregates Apple's 3 buckets w/ role subtitles; DESKTOP modal owner-gated (after buy) & [Apple-polished](desktop-credits-apple-polish.md) (thin rules, non-caps headers, person drill-down swaps modal content not a sheet, rows gated by rich-profile probe); MOBILE keeps pre-purchase SuperCredits teaser — split intentional, don't unify without asking Bill.
- [Catalog format visibility](catalog-format-visibility.md) — formats can be hidden (UI-only) or deleted (soft-delete); hidden formats don't show in the SellPanel dropdown but stay in the SKU snapshot; deleted formats are stripped from the response. [Desktop not-owned row](desktop-not-owned-album-row.md): drops shuffle + ⋯ menus + padlock glyph (showMenu={isOwned}); Buy pill defaults "Buy Now", hovers to price. [Seed-album auto-grant](seed-album-autogrant-signature.md): retired; find leftovers via album-1..4 + is_preview=false + cert IN (12,7,3,21) + no paid order.
- [gogoods prod bulk import](gogoods-prod-bulk-import.md) — chunk 500 per txn; dedup most-recent-wins on unique collisions. [Profile-only fans](gogoods-profile-only-no-source.md) — genuinely purchase-less, don't fabricate. [Duplicate albums strand buyers](gogoods-duplicate-albums-orphaned-buyers.md) — repoint to live album, guard the (album_id,good_deed_number) unique. [↔Stripe reconciliation](gogoods-stripe-reconciliation.md) — match ONLY by metadata.txn_id, never amount; run as a workflow.
- [Desktop album cert data is order-derived](desktop-album-cert-data.md) — desktop ApiAlbum has NO ownedCertificates/purchases/certificateNumber (mobile spreads staticAlbum demo data); derive ownedNums from the /api/orders cache; ProvenanceSheet events are hardcoded demo, OwnershipSheet needs ownedCertificates stamped on the album cast.
- [Fan search nullable-.length crash + self-diagnosing reports](fan-search-nullable-length-crash.md) — `r?.cat.length` only guards the parent; a null category array in the /api/search payload crashed the whole fan app (useFanSearch backs BottomNav everywhere). Guard parent AND array. Prod stacks: keepNames:true + componentName parsed from componentStack into error reports.
- [Publishing payouts](publishing-payouts.md) — mechanical settlement on units PRESSED (flat $/song), separate ledger from sales; track_publishing_splits+organizations+payout_accounts+earmark/release rail already exist; Nick catalog is prod-only; dedup payee strings before creating accounts.
- [Tailwind var-opacity gotcha](tailwind-var-opacity.md) — `bg-[var(--brand-x)]/NN` silently produces no color (Tailwind can't alpha a var); use solid bg + `opacity-NN`, `--brand-*-soft` vars, or inline `style` glow.
- [EasyPost historical pull → order backfill](easypost-historical-pull.md) — shipments index empty, reports are ZIP, reference null → best-effort email+date match (honest 3-tier confidence); shipping_address MUST be StripeAddressSnapshot shape; 2k-row prod write = single json_to_recordset UPDATE.
- [Stripe-Account-Id vs IDP drift](stripe-account-id-drift.md) — re-authing a disconnected Stripe account (auth:reauth_only) replaces the IDP-stored ID but NOT the payout_accounts table row; verify current owner matches the IDP's stripeAccount before writing payouts; fix: upsert row by IDP-ID, never assume it exists.
- [Order payment snapshot only lands at materialization](order-payment-snapshot.md) — card brand/last4/wallet/receipt are captured ONLY in materializeOrderFromSession, which early-returns on already-`paid`; new fields never backfill onto pre-existing paid rows or via webhook replay — write a PI-keyed one-shot backfill if you need history. [Artist dashboard ↔ buyer-roster parity](artist-dashboard-roster-scope.md): both share ONE album scope (primary_artist OR payout-owner AND deleted_at IS NULL); mirror any change to both queries.
- [Person save gotchas](person-projection-readback.md) — a people col written by PUT but missing from toPublicPerson can't be read back (save "reverts"); shared ["/api/people",id] keys w/ diff queryFns race under staleTime:Infinity; 202=approval-divert not saved. [Local edit vs shared-query re-seed](local-edit-vs-shared-query-reseed.md): a panel mirroring a shared-query field into local edit state must re-seed ONLY on entity-id switch or !dirty, else a sibling save's refetch wipes in-progress edits; 202 divert = "Sent for review", not false "Saved".
- [Exclusive-disclosure row + sub-editor coupling](exclusive-disclosure-mode-coupling.md) — accordion `expanded` must derive SOLELY from the controller flag, not OR'd with local `mode`, or opening a sibling can't collapse a row that has a sub-editor open; reset mode on collapse.
- [App bundle IDs + signing](ios-bundle-id-vs-android.md) — iOS Io.GoGoods.music lockstep across capacitor+pbxproj+codemagic+AASA; Android com.gogoods_mobile immutable ([listing](android-keep-gogoods-listing.md)); [.p12](codemagic-distribution-cert-private-key.md) "profile requires capabilities" = missing dist-cert private key. [CarPlay entitlement](carplay-restricted-entitlement.md) GRANTED; CPNowPlayingTemplate must NEVER go in a container (SIGABRT on connect), root = CPListTemplate.
- [Native buy gating vs desktop surface](get-my-domain-split-and-native-no-buy.md) — buying is web-only on get.goodtunes.music (→ my.goodtunes.music player); native apps hide Buy via `buyEnabled=!isNative` (Android-off reversible); BUT every "is desktop?" check must be [width-only (useDesktopShell)](fan-desktop-surface-width-only.md), never `&& buyEnabled`, and hide native-forbidden CTAs in-component. [Gifting](gift-native-gating.md): giftEnabled=!isNative hides gift CREATION only; /gift/:token claim stays ON for native (free redemption — gating strands recipients).
- [Rebase multi-region leftover markers](rebase-multi-region-leftover-markers.md) — one file can have >1 conflict region; resolving the first + git add commits leftover =====/>>>>> markers that only break the build post-rebase; grep marker-count==0 per file before add + sweep repo + re-check schema-drift on dev AND prod.
- [Fan shipping charge](fan-shipping-charge.md) — real shipping from shipping_rates card; base vs $1 markup stay separate cols, markup once/order, recompute server-side, physical no-quote REFUSE (422) never $0, lock Stripe allowed_countries=[picked], persist breakdown in BOTH materialize paths.
- [ArtistDetail not-found gate](artist-page-notfound-gate.md) — fan artist link works; false "Artist not found" comes from gating on the owned-only artistAlbums; gate exists/not-found on ownership-INDEPENDENT goodTunesTitles + artistPerson.
- [Manager partner derived catalog](manager-partner-derived-catalog.md) — manager mirrors label but catalog is DERIVED from roster people (people.manager_id); NO albums.manager_id; not in PAYOUT_OWNER_KINDS; distinct from teammate sub_role 'manager'.
- [Fan album route owner-bypass](fan-album-route-owner-bypass.md) — GET /api/albums/:id 404s a fan's OWN album once hidden/sunrise/trashed; re-read with includeHidden/includeTrashed when userOwnsAlbum. Plus: new fan routes need STOREFRONT_ROUTE_PREFIXES + FanScreen shell or they lose the rail. [Anon public read](anon-album-read-public.md): GET /api/albums/:id was requireAuth so anon fans 404→"couldn't find that album" on LIVE releases; fix = optionalAuth public visibility-gated read mirroring by-slug (client staleTime fix only covered prepping previews).
- [Rail-aware dock + iPad rails](rail-aware-dock-channel.md) — desktop fan dock centers on host channelLeft/channelRight (gutter between rails) not window, auto-disables <1100 to keep the intentional iPad overlap, album lyrics aside runs full 100dvh flush to bottom (LYRICS_DOCK_CLEARANCE removed); [iPad rails](ipad-rail-dvh-dock-overlap.md) size off 100dvh + env(safe-area-inset-bottom) not raw 100vh/fixed bottom inset (iOS chrome eats it), dock goes edge-to-edge only <1100px.
- [Dropbox import dispatcher](dropbox-import-dispatcher.md) — one dispatcher feeds all 4 importers; zip extract via central directory, zip-detect ext-gated (.docx/.xlsx are PK-zips); WeTransfer works but intentionally unadvertised. [Admin background upload manager](admin-background-upload-manager.md) — module-singleton store (survives dialog close/nav); route new admin upload surfaces through it.
- [In-tree native iOS Capacitor plugin](native-ios-capacitor-plugin.md) — native-only iOS .swift committed in ios/App (NOT npm); Capacitor 6 does NOT auto-register them (no ObjC scan) — a MainViewController subclass MUST registerPluginInstance in capacitorDidLoad (+pbxproj/storyboard) or pluginAvailable stays false; NowPlaying.getBuildInfo reads GTGitCommit for on-device build proof. [platform stub mirror](mobileplayer-platform-stub-mirror.md): scrubber test stubs @/lib/platform — new exports Player.tsx imports need the stub + __setTestIsIOS. [Volume slider](native-ios-volume.md): hidden web iOS (read-only), shown native via hidden MPVolumeView; gate isNativeIOS/isWebIOS.
- [requireAdmin semantics](requireadmin-includes-partners.md) — requireAdmin admits ALL partner accounts; operator-only routes need an explicit super_admin/admin check. [Two guards differ on auth mode](two-requireadmin-auth-modes.md) — commerce.ts requireAdmin is BEARER-ONLY, routes.ts is session-OR-bearer, by design; requireReportScope fail-closes manufacturer/vendor/fulfillment on the whole reports module.
- [Fan account deletion = anonymize-in-place](account-deletion-anonymize.md) — DELETE /api/customer/me scrubs PII + revokes tokens/identities/favorites/playlists/library but KEEPS the row + orders (FK retention); no deletedAt col; UI at Account→Privacy.
- [Native Capacitor remote origin](native-capacitor-remote-origin.md) — native build must set capacitor server.url to the live host (relative /api + assets, so a bundled capacitor://localhost build hangs the auth gate: navy screen, broken-logo glyph, dead spinner); [push notifications](capacitor-push-notifications.md) need 3 legs (web-bundle JS + iOS AppDelegate→NotificationCenter forwarding + npm plugin sync) or the token silently never arrives, delivery credential-gated (APNs ES256/.p8 + FCM v1), inert `[push:dry-run]` without keys.
- [iOS safe-area white status-bar strip](ios-safearea-white-statusbar-strip.md) — pure-white strip over the status bar w/ solid navy just below = body-box gradient doesn't paint the safe-area-inset-top on iOS standalone (background-attachment:fixed worsens it); fix = move gradient to a fixed inset:0 z-index:-1 body::before backdrop, body transparent, html navy backstop, hide under gt-admin. Static config can be 100% correct and still show it.
- [Stripe Tax (checkout + Buy-sheet)](stripe-tax-checkout.md) — automatic_tax needs customer_update:auto; exclusive tax means amount_total is tax-inclusive so snapshot pre-tax (amount_subtotal/_shipping) or double-count; fan [Buy-sheet "Sales tax" line](stripe-tax-preview.md) via GET /api/checkout/tax-quote (needs US postal + Dashboard head-office addr), never a static table.
- [Post-purchase player hand-off](post-purchase-player-handoff.md) — cross customer-subdomain re-auth carries a fresh bearer in the URL **fragment** (not cookie/query, stays out of logs), picked up in main.tsx; gate on isPurchaseFunnelHost; bare /album/:id mounts AlbumDetail with NO albumId prop (use useParams).
- [Native offline download protection](native-offline-download-protection.md) — Android-only download UI but platform-agnostic protect/decrypt on isNative (web no-op); files AES-GCM in Directory.Cache under a non-extractable IndexedDB key (not Keychain); native playback is offline-FIRST (decrypted blob: else Mux); revocation in DownloadEntitlementGuard fail-OPEN (never purge on error).
- [iOS setPointerCapture kills tap+drag](ios-setpointercapture-breaks-drag.md) — fan DESKTOP/iPad scrubber+volume+lyrics drag must use window-bound listeners (lib/useRailDrag.ts), NOT setPointerCapture (fires immediate pointercancel on iPadOS WebKit); mobile lyrics now has manual scroll TOO, with swipe-dismiss scoped to the header bar so they don't fight.
- [Artist Rig surfaces](rig-feature-surfaces.md) + [accessories inventory-backed](rig-accessory-inventory-link.md) — track rigs ride in getAlbumCredits().bySongId[id].rigs (NOT getCredits), mutations invalidate ["/api/albums",id,"credits"], Fernando demo prod-only; accessory value field is the reusable GearPicker (search inventory or paste-URL scrape→create shortCategory:"Accessory"), optional rig_accessories.instrumentId, keep {type,value} text; any per-accessory field must thread BOTH clean() helpers + all 3 storage read maps or it silently strips/won't rehydrate.
- [Native plugin version skew + iOS icon](native-plugin-version-skew.md) — a new Capacitor plugin called from the remote-origin bundle throws on older binaries → un-awaited addListener reject paints the global banner; gate isPluginAvailable + await. isPluginAvailable:false = SKEW (republish fixes) OR ABSENT — but in-tree plugins ALSO never register without the MainViewController subclass (native-ios-capacitor-plugin.md), so a REBUILD alone may not fix it; ios-testflight is MANUAL. Single-size 1024 iOS icon renders blank in TestFlight; ship the full no-alpha icon set.
- [AlbumDetail mobile hook order (React #310)](album-detail-hook-order.md) — every hook in AlbumDetailMobile/Desktop must sit ABOVE the `if (!album ...) return` loading/not-found guards or the loading→loaded render runs more hooks → React #310 crashes the whole fan app; deployed bundle has no real source map but keeps `a(fn,"Name")` registrations for mapping minified names.
- [Native download cert pinning](cert-pinning.md) — pin the long-lived ISRG (Let's Encrypt) ROOTS not the ~60d leaf; WebView fetch ignores pin config so route downloads through native URLSession/HttpsURLConnection; scope to goodtunes.music only (Dropbox/Mux unpinned to avoid bricking); null=unavailable→fallback but real pin failure must propagate. [Android notification icon + splash](android-notification-icon.md): status-bar small icon MUST be white-on-transparent silhouette (alpha-only, else gray box); ic_stat_notify wired via FCM default meta-data + server payload; splash is white "G" on navy #00062B, not white bg.
- [Apple Pay / Google Pay checkout](apple-pay-wallets-checkout.md) — wallets auto-surface INSIDE Stripe Embedded Checkout (no Express Element); need well-known association file served per fan host + paymentMethodDomains registration (boot hook in applePay.ts); never add payment_method_types; test-mode "active" ≠ prod wired.
- [Mini-player viewport bounding](miniplayer-viewport-bounding.md) — fan mobile mini-player width must cap with `min(390px, 100vw - env(safe-area-inset-*))` not `w-full`, or a transformed ancestor / iOS visual-viewport mismatch runs the centered pill off the right edge on scroll; inner pill must be a definite-width block (no flex) so long titles can't grow the capsule.
- [Person sheet bio strip + gear list](person-sheet-bio-and-gear.md) — Apple-Music "Listen to music by…" boilerplate killed in 3 layers (import/backfill/render+richness); gear list shows unlinked instrument credits as plain rows (vocals/production excluded); SheetClose now defaults glass not fill.
- [Codemagic build pipeline](github-mirror-push.md) — mirror push fetches remote tip + LFS by --object-id; catch-up runs as a WORKFLOW. [Changeset Android-only build](codemagic-changeset-android.md); [Xcode pin](codemagic-xcode-pin.md) — explicit xcode: never latest; "archive fails, no code changed" ⇒ Xcode roll. [Android .aab signing](android-release-signing-config.md) — `android_signing` only EXPORTS CM_KEYSTORE_* vars; the `release` buildType must reference them or bundleRelease ships UNSIGNED (fails only at Play upload).
- Empty-decoy-shell duplicate-album trap — verify songs+SKU before trusting any prod row by title alone; the empty shell often squats the clean slug/launch pointer. [Nightbirde "Hope"](nightbirde-hope-canonical-album.md): real is 7" `b250a5a5` (slug `hope`), STOREFRONT_LAUNCH_ALBUM_ID wrongly pointed at empty decoy `54d46505`. [CALIFORNIALAND](californialand-canonical-album.md): Niina Soleil's real one is the row WITH songs; shells trashed + clean `californialand` slug handed over via marker-guarded post-merge `task_2454` (identity-keyed, partial-unique makes it safe); not a launch-constant.
- [Full tsc isn't the type gate](tsc-gate-vs-tsx.md) — dev runs tsx (no check) + ES5 tsconfig.json has 100s of pre-existing errors; a NEW server route using a forgotten @shared/schema import compiles in dev AND passes the test workflow, surfacing ONLY under full tsc — grep your new symbol, don't trust "dev works".
- [Stripe order recording in prod](stripe-order-recording-prod.md) — "no app orders / tax_cents NULL" is usually data/operator state not a code bug: check `albums.is_prepping` (staged = locked, no checkout) + whether ALL orders are `origin='legacy:gogoods'` (NULL tax = legacy imports) before suspecting materialization; webhook secret is ONE global Replit Secret that can't match both test+live endpoints.
- [App-icon guards + smoke](app-icon-guards.md) — 4 guards (source res/appiconset + built .aab/.ipa), one `app-icon-guards-smoke`; verified false-positive-free vs navy "G" (lum ~0.30<<0.7, 192px floor) + AAPT2 `-v4`/`-v26`; Linux = ImageMagick fallback + loose-PNG path only (iOS assetutil/Pillow checks mac-only, skipped).
- [Anon preview is publish-not-release](anon-preview-is-publish-not-release.md) — "launched but logged-out previews silent" = stale deploy, NOT release flag; playback gate ignores is_prepping; curl live playback-url anon (+?k= tokens), all-401 = republish needed; bare campaign link=notify waitlist, /staging=forces buy.
- [Linked-admin session vs token boundary](linked-admin-session-token-boundary.md) — session checked before token, so cross-kind sessions 401; honor a Bearer matching THIS host's kind; test as a pure helper, not HTTP. [Access-denied guard covers /admin/login](admin-access-guard-covers-login.md) — exempt admin auth paths from showAccessGuard.
- [Invite capability mirroring](invite-capability-mirroring.md) — AdminInvites must NOT re-derive who-can-invite; computeInviteCapability() in partnerPermissions.ts mirrors the POST /api/admin/invites gate and is surfaced via /api/me/role (canInvite/allowedInviteRoles/allowAdvancedInvite); gate uses SCOPE-level inviteSubusers (NOT per-user override); non_profit never invites (absent from PARTNER_SCOPE_KINDS).
- [Fan preview window](fan-preview-window.md) — 30s previews honor per-song previewStartMs/EndMs (on currentSong via hydrate, no server change); fall back to first-30s, clamp ≤30s; EVERY scrubber AND all 3 SyncedLyrics onSeek must clamp into the window (lyric-tap is the sneaky compliance bypass), highlight stays absolute; pause()-queued stale timeupdate double-advanced the cap — zero the element clock on advance.
- [Branded album-cover placeholder](album-cover-placeholder.md) — One shared AlbumCover decides every cover fill (real art → ghosted artist photo → brand tile); never drop a raw album-art <img> again. [Artist avatar source](artist-avatar-photo-source.md): fan artist avatars resolve from /api/people; ARTIST_PHOTOS has ONLY Nick Carter, so reading it alone shows the album cover for everyone else.
- [Fan Library sources owned tiles from ownership](library-owner-bypass-any-stage.md) — Collection dbAlbums must build owned tiles off /api/my-albums (getUserAlbums, full album row incl prepping/hidden), NOT intersect the public /api/albums catalog (strips prepping/hidden); getUserAlbums is the ONLY safe place to drop those filters (userId-scoped); public browse (ArtistDetail/FanLabel) stays filtered; AlbumCard notYetReleased badge gated behind explicit prop.
- [Buyer location billing fallback](buyer-location-billing-fallback.md) — digital/donation add-ons collect billing-only at Stripe → shipping_address all-null → reports showed "Location unknown"; every buyer-location read must coalesce shipping→billing (LOC_CITY/REGION/COUNTRY in reports/buyers.ts, alias `o`); NOT the fulfillment CSV (shipping-label only).
- [Fan "no sound" playback diagnoses](webkit-autoplay-unlock.md) — WebKit blocks unmuted audio when async-signed-Mux play() lands outside the tap → gesture-bound silent-clip bless on the persistent Audio el (never restore over real src); SEPARATELY silent + zero playback-url POST = album re-map dropped [muxPlaybackId/muxStatus](desktop-playersong-mux-fields.md) so resolveStream bails.
- [Operator sign-in link](operator-signin-link.md) — welcome-back token = generic sign-in link (redeem has no eligibility gate); super_admin-only POST /signin-link; build URL via customerOriginFromReq, NOT originForKind. [Admin-OTP logout race](admin-otp-logout-race.md) — a fresh OTP sign-in kills all OTHER sessions for that user; concurrent two-device logins log the first out.
- [Order Desk auto-push OFF](orderdesk-autopush-gate.md) + [Odoo printer](odoo-printer-integration.md) — two parallel fulfillment connectors. OD: paid physical orders DON'T auto-hand-off (aggregate→artist confirms qty→one press order→routing), auto-push gated behind `ORDERDESK_AUTO_PUSH`, manual "Push to OD"+status webhook always live. Odoo: PULLS status on a timer (no webhook), JSON-RPC, env-gated dormant, single-instance `is_odoo_printer`, operator-only "Push to Odoo". (test workflow stays "running" after pass — schedulers don't unref; read per-file pass/fail, don't wait.)
- [OAuth duplicate-account recovery](oauth-duplicate-account-recovery.md) — Apple/Google re-auth after an app update mints a NEW empty account (real email→contact_email); merge moves content NOT identities so survivor MUST be the OAuth holder; admin "Combine accounts" tool shipped (recommends OAuth survivor, undo+audit).
- [Publisher payout status lazy-sync](publisher-payout-status-lazy-sync.md) — payoutsEnabled only advances on Stripe's fragile account.updated Connect webhook; status reads (GET /api/publisher/me) must live-retrieve+sync while still false (bounded, best-effort) or the portal banner sticks on "in progress" forever.
- [Interactive content in <label> breaks Safari](interactive-in-label-safari.md) — nesting a combobox/buttons inside Field's <label> swallows clicks on Safari only (Chrome+jsdom honor "do nothing for interactive descendants"); Field has as="div" for picker fields; jsdom can't reproduce so guard structurally (combobox.closest("label")===null).
- [Admin order search deep-link](admin-order-search-deeplink.md) — search opens the matching fan/pressing order via `?orderId=`; pressing page MUST default status to "all" (per-status fetch) or the row is filtered out before it can expand.
- [Hermetic external-fetch test stub](hermetic-external-fetch-test-stub.md) — drive a route that streams an external URL + calls a vendor API with zero network: stub globalThis.fetch (loopback POST uses realFetch) + IP-literal host (203.0.113.x) so dns.lookup short-circuits the SSRF guard offline; .wav skips ffmpeg; [Chorus] marker skips OpenAI.
- [Hellbender disc swatches](hellbender-splatter-swatches.md) — 12" Splatter disc colors are operator-curatable image swatches from Bill's authoritative export (NOT verified catalog); loader is a marker-guarded SCOPED clean-replace wired into post-merge for both DBs, not the boot seed; [extract layered PSDs](imagemagick-psd-layer-extract.md) per-layer loop, trim/extent SEPARATE pass, verify mean.a not avg color; non-Splatter [swatches = cropped PHOTOS](hellbender-swatch-photos.md) (hellbender-photos.json, NOT regressed synthetic discs) restored via `--repoint` (network-free, runs every merge not marker-once, skips the 31 Splatter rows).
- [Partner-admin roles fail OPEN to god-view](partner-role-godview-failopen.md) — a new admin-side role silently gets full catalog (filterAlbumsForPartnerRole null) + all-partners reports (resolveScope null) + hidden album detail (includeHidden=isAdminUser) until fail-closed in NO_ALBUM_LIST_ROLES + requireReportScope + App.tsx /admin guard; requireAdmin only covers /api/admin/*.
- [Release acquisition funnel + attribution](release-acquisition-funnel.md) — first-touch utm rides the envelope into payload _utm_* (jsonb, not columns); funnel aggregated in JS. [Completed step is ORDER-derived](funnel-cross-host-stitch-and-internal-exclude.md) — paid orders not events; excludeInternal drops staff+internal buys. [Partner picker](partner-acquisition-funnel-releases.md) — partners list ALL owned releases; god-view stays trafficked-only.
- [Legacy fan unclaimed-account reattach](legacy-fan-unclaimed-reattach.md) — credential-less imported fans rejoin via the shared `isUnclaimedCustomer` guard (no pw/!oauth-only + 0 identities + !merged); 3 surfaces (social auto-link, Apple-hide `?prompt=claim` code endpoints, post-merge reconcile); reconcile keeps LEGACY row survivor + hand-moves identity (opposite of admin Combine-accounts); real fans PROD-ONLY so dev validation = "0 pairs".
- [Partner feedback system](partner-feedback-system.md) — partner-portal bug/feature reports live in self-contained partner_feedback (NO agent-inbox pipe); one shared FeedbackLauncher in OperatorShell for ALL portals; submitter identity derived server-side from memberships, never client-claimed; operator triage under Queues.
- [Admin trusted-device cookie + route-test prod gate](admin-trusted-device-cookie.md) — gt_trusted_device MUST be SameSite=Lax (None dropped by Safari ITP); two mint sites in lockstep; route-test flips NODE_ENV after registerRoutes. [Stateless OAuth state](samesite-lax-stateless-oauth.md) — session cookie now Lax, so Apple form_post round-trips an HMAC-signed state bag; bearer covers native auth.
- [Express 5 removed app._router](express5-no-router-redispatch.md) — internal route re-dispatch via `_router.handle` 500s; extract the handler into a named fn and call it directly from wrappers.
- [Promise inside res.json() ships {}](promise-in-resjson-serializes-empty.md) — a bare/un-awaited Promise (IIFE/.then) used as a VALUE in a res.json({...}) literal serializes to `{}` silently; await it first (compute into a const). This broke invited-press `catalog` → empty → SellPanel priced from MRP fallback (cross-press leak).
- Partner surfaces REUSE super-admin components (standing rule, docs/design-system.md + admin-conventions.md) — voice/copy + permission-removed affordances only; PersonPicker exported from AddPeopleMenu, hidePaste on session-authed portals (scrape is bearer-only). [Scoped-surface checklist](press-portal-scoped-surface-checklist.md) — scope every read source + gate every destructive verb server+UI. [Catalog write-gating fan-out](press-catalog-write-gating-fanout.md) — pressUserCanEdit canonical, one client canEdit on the catalog GET. [Phantom press homing cleanup](phantom-press-homing-cleanup.md) — clear BOTH press-id cols, scope by press NAME.
- [Partner portal tab-in-URL + super-admin badge](partner-portal-tab-in-url.md) — every tabbed OperatorShell portal must write `?tab=` back to the URL (merge params, history.replace) so FeedbackLauncher's captured href deep-links to the right sub-tab; operator super-admin view = a top-nav `superAdminView` badge, NOT a "(super-admin view)" roleLabel + duplicated header; AdminFeedback "Page" is reference text for operators (scoped "Open in portal" is the click).
- Shopify integration — [prepaid manufacturing mode](shopify-plus-fulfillment-only.md): external_paid/fulfillment_only revenue-safe. [Install approval](shopify-install-approval-model.md): only a target-store admin can approve; "Unauthorized Access" = wrong Shopify login. [map_shopify gate fan-out](shopify-map-gate-fanout.md): every connect/browse surface gates map_shopify, not just attach/detach. [Connect = Overview tab](person-shopify-connect-overview-tab.md) (grep-streaming trap). [Expiring offline tokens](shopify-expiring-offline-tokens.md): request expiring:"1", persist rotated pair BEFORE returning; legacy rows need operator reconnect (409).
- [Pending-change withdraw](pending-change-withdraw.md) — artist self-retracts a still-pending change request; retract = terminal status not delete (audit trail kept), scoped to own+pending+album, filtered out of artist list AND operator queue.
- [Partner-portal embedded sub-route guards](partner-portal-embedded-route-guards.md) — a new embedded detail route under a partner portal (e.g. /artist/albums/:id) must be exempted in BOTH App.tsx's prod-admin-host redirect guard AND main.tsx's first-paint light-theme detector (both allowlist EXACT bare portal paths only), else the prod admin host bounces it to a blank navy /admin + dark flash; prod-only, hits real partners not just view-as.
- [Report CSV export view-as leak](report-csv-export-authheaders.md) — a bare `<a href>` CSV download drops X-View-As-Token → god-view export; fetch report CSVs via fetchBlob (authHeaders)+blob, never an anchor.
- [Artist referral one-year window](referral-one-year-window.md) — artist→artist referrals earn $1/unit for ONE YEAR (anchor=earliest artist_referrals.created_at, null fails OPEN), an ADDITIONAL mint gate beside earns_referral_payout+swap-state; NPO stays ongoing, press unaffected, no clawback; helper shared/referralWindow.ts, only mint site is commerce.ts artist branch.
- [Embedded AdminReports rtab isolation](embedded-adminreports-rtab-isolation.md) — partner portals embed the shared AdminReports as a rail `reports` module (registry.ts, not a navExtras link-out); embedded mode drives its sub-tab via controlled Tabs on `?rtab=` so it doesn't fight the portal shell's `?tab=`; god-view stays uncontrolled `?tab=`.
- [SQL OR-null poisons classification](sql-or-null-poisons-classification.md) — a nullable OR-branch in a kept-vs-excluded row split NULL-poisons the whole predicate; COALESCE(...,FALSE) + assert kept+excluded==total.
- [Partner album embed + free-access revoke](partner-album-embed-and-free-access-revoke.md) — artist album links open AdminAlbum `embedded` (no /admin chrome) inside the portal via `/artist/albums/:id`; partners see previewLinksOnly (comped fan roster HIDDEN — don't even fetch /free-access); operator-only free-access revoke (user_albums delete, 409 if paid) bypasses post-sale lock; extracting mainBody needs a `<>` fragment or trailing dialogs = multiple JSX roots.
- [OperatorShell sidebar is registry-driven](operator-shell-registry-sidebar.md) — partner-portal left-rail icons/sections/labels/order all come from registry.ts (matches AdminFrame); the per-portal `navIcons` prop was removed, rail uses baselined `text-[13.5px]`, and casting modulesForRole to {id,label} keeps icon/section at runtime (rebuilding rows strips them).
- [Operator super-admin fan-out notify guard](operator-notify-guard.md) — every listSuperAdminEmails() fan-out MUST route through notifySuperAdmins() (prod-only + synthetic-REQUESTER drop + dedupe + per-recipient cap); a test requester reaching a real operator inbox is the failure mode a recipient-only guard misses.
- [Shopify+ release ladder](shopify-plus-release-ladder.md) — Submitted-to-press = is_prepping + submitted_to_press_at; fan gates stay isPrepping-only; releaseStatus verb Shopify+-only. Prepaid ledger: Quoted = active quote total → system cost → sum-of-requests; run close-out blocks NEW requests only (pay/edit stay live).
- [Preflight side-break source](preflight-sidebreak-source.md) + [completed-template check](completed-template-preflight.md) — masters-preflight Tracklist/Side-length derive sideBreaks from songs.vinylSide/vinylOrder (never a standalone side-break editor); separately, Press-section "confirm finished print files" (own table, not upload_validations) compares measured FINISHED artboards, paste-a-URL+streamed scan (350-530MB), dieline+RGB advisory/CMYK-wins, labels=1 multipage vs sleeves=1-per-disc.
- [Unreleased catalog view policy](unreleased-catalog-policy.md) — unreleased albums (Prepping/Hidden) are hidden from PUBLIC search/storefront; owners (customers with user_albums grant) see them in Library; operators see them in global catalog. Preview links (reviewer-access) bypass this for the specific album.
- [Invite redirect canonicalization](invite-redirect-canonicalization.md) — invite accept-url must use the SAME host as the incoming link to preserve the session cookie (which is host-scoped); if the link is get.goodtunes.music, the final redirect must be get.goodtunes.music (not the apex).
- [requireRole vs requireAdmin session backfill](requirerole-vs-requireadmin-session-backfill.md) — a gate resolving Bearer-OR-session auth must also backfill req.session, or handlers that re-read req.session?.userId directly still 401 Bearer-only callers.
- [orders.total_cents includes tax+shipping](order-total-includes-tax-shipping.md) — any artist/vinyl-fund report must back tax_cents + shipping_charged_cents out of gross before computing net; only the top-line Gross Sales KPI stays inclusive.
- [CarPlay Now Playing refresh keys](carplay-nowplaying-refresh.md) — nil-wipe on NEXT runloop hop + FRESH MPMediaItemArtwork per track or same-album skips freeze; [cold-connect](carplay-cold-connect-snapshot.md) browse via on-device snapshot. [Native audio-cutout = AVAudioSession churn](native-audio-session-cutout.md) — configure session ONCE + interruption observers; [WKWebView wins now-playing](ios-webview-nowplaying-arbitration.md) — set web `navigator.mediaSession` on native too (JS-only); plugin keeps AVAudioSession + CarPlay.
- [Main-hotfix vs task-merge duplicates](main-hotfix-vs-task-merge-duplicates.md) — a main-agent hotfix a queued task also implemented merges as a DUPLICATE (schema export = app-down build break); dedupe keeping the merged version, Cancel duplicate task cards.
- [Shopify GraphQL migration + review skill](shopify-graphql-migration.md) — app is GraphQL-only (refunds via suggestedRefund+refundCreate; REST/script_tags removed); inventorySetQuantities 2026-04 drops ignoreCompareQuantity (don't bump the pin blind); doc fetch needs nix Node 22; no live dev-store in task envs.
- [Shopify extension redemption polling](shopify-extension-redemption-polling.md) — extensions can't read ORDER metafields; poll our authed endpoint (session token + confirmationNumber). [E2E test hygiene](shopify-test-hygiene.md) — never map tests to a real artist's album (prod QA album a0000000-…e2e0); already-paid orderCreate fires NO orders/paid — create PENDING then orderMarkAsPaid; webhook mint consumes the album's GoodDeed sequence + grants ownership.
- [qa:test order exclusion fan-out](qa-test-order-exclusion-fanout.md) — every admin order/fan read must exclude origin=qa:test (QA-only fans too); Shopify QA-album mints stamp qa:test at the webhook.
- [Album-create scope gate fan-out](album-create-scope-fanout.md) — every album-minting route under requireAdmin must gate artist scope itself (direct create forces own scope; apple-url seeder + duplicate are operator-only).
- [Artist Dashboard tier discipline](artist-dashboard-tier-discipline.md) — merged Dashboard (Overview gone, artist only): headline plays = purchaser tier ONLY, four tiers (fan/grant/anon-preview/staff) spelled out never summed; dropping a KPI card ≠ dropping its payload field (AdminDashboard artist-early view still reads topAlbum); label/manager portals intentionally keep Overview.
- [Album tab panels full column](album-tab-panel-full-column.md) — no max-w wrapper at any album tab panel root; cards must match the tab rule width (regressed twice: Sell, Shopify checklist).
- [Team accounts roster](team-accounts-roster.md) — account-centric partner roster (Partners → Team accounts, super_admin-only); invite directory is invite-centric and misses "Add Admin" accounts; operator exclusion must be membership-aware.
- [Investor snapshot re-uploads](investor-snapshot-updates.md) — Bill's standalone exports LACK the noindex meta; re-inject after charset on every swap; file streams from disk (no restart), prod updates on publish.
