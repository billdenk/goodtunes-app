import { DesktopGiftScreen } from "./_desktop";

// Desktop album-purchase gift — compose the gift on the left, order summary +
// the card the recipient will receive on the right.
export default function DesktopGift() {
  return <DesktopGiftScreen />;
}
