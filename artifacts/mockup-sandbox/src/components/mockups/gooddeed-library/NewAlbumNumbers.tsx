import { AlbumNumbersScreen } from "./_shared";

// Direction 2 · Album opened — the real GoodDeed numbers revealed cleanly
// (#310 • #311 • #312) in their own quiet card, off the artwork.
export default function NewAlbumNumbers() {
  return <AlbumNumbersScreen />;
}
