AS SOON AS YOU'VE COMLETED THE CURRENT TASK, 

Pull the latest from branch design/press-catalog-cleanup-v3 and apply Item 28 in
handoff/press-catalog/CORRECTIONS-2026-08-09.md. It applies to EVERY press
instance (MRP, PMP, Viryl, Hellbender), and it supersedes Item 27's "no rewind
button" — there IS a rewind button now.

Summary of Item 28 (full spec + canonical code is in the corrections file and
the two updated reference components):

- Rewind button: small frosted control centered below the record after
  hover-spin; clicking it slides the record out ~1.2s while it turns back,
  then tucks it in. The button must not trigger the hover spin/slide.
- 140 g / 180 g price books: segmented chip above the price list; two books,
  shared run sizes (adding a run size adds it to both weights); caption ends
  with "· 140 g vinyl" / "· 180 g vinyl".
- Print-prep template dialog: clicking any tile (or ⋯ → Add file…/Replace…)
  opens a centered modal like the album "Completed Art" dialog — current file
  left, drag-and-drop + paste-a-URL right. The old hover "Replace" swap is
  removed.
- Hide controls: ⋯ on every size card ("Don't offer this size" → grayed "Not
  offered", reversible) and every template tile ("Hide for now" + a
  "Hidden: … · Show" restore line). Booklet starts hidden for all presses.
- Final copy set: headline "Build your GoodTunes® packages. For the record."
  with a small superscript ®, plus the new section lines — including
  "Turnaround time. From order, to out the door." (with the comma) — and the
  one-line audio subtitle. Turnaround inputs stack below the heading.
- "Make it yours" branding dialog: ⋯ on the jacket opens a centered modal —
  brand color chip-as-picker, SVG-only logo replace, Reset to default; color
  and logo flow to the cover and center label.

Copy the reference components verbatim — do not restyle. Diff the rendered
page against the reference at 1440px; do not report complete until they are
visually indistinguishable (data values aside).