No — that message was me talking to you, not copy for Otis. He hasn't seen any of this yet, so he needs the complete package: the full punch-list brief with 2a folded in, plus the v1.3 file attached. Here's the final assembled version, ready to paste as one message:
Subject: Press catalog — remaining items, per Addendum v1.3 (attached)
The catalog is close. Addendum v1.3 attached — it supersedes v1.2 and records four decisions: the Manage colors overlay as shipped, the stepped readable card width, an additive-only rung policy, and updated read-view rung rules. Read the changelog block at the top. The critical rule: live press data is untouchable — Viryl and others have real rung configurations (50 offered on some products, explicit eye-off elsewhere), and nothing in this work may delete, hide, or reinterpret any rung a press has configured. Layout and rendering changes only.
Remaining items:
Undo/Redo dirty-state bug in the Manage colors overlay. After adding a manual row, Undo and Redo still render disabled while Reset renders enabled. After any change, Undo must enable immediately. Verify by adding a row and clicking Undo — the row should disappear.
Card width (§2.2): implement the stepped readable width — max ~1100px baseline, stepping to a hard ~1400px ceiling at large breakpoints, leading-aligned, never fluid. Extra viewport becomes margin.
Input width caps (§2.1). The Specs URL fields and Notes textarea still run edge to edge with upload/··· floated to the far card edge. Cap text/URL fields ~560px, controls sit immediately beside the field. This holds at every breakpoint — a wider card widens the ladder and whitespace, never the inputs.
Price ladder columns (§2.3). Fixed-width columns, left-aligned as a group, wrapping instead of clipping in edit mode. Do not change which rungs render or their values — presentation only.
Part 2 button/save items if still outstanding: converge Saves to the standard shadcn primary (confirm the gt-admin --primary value first), and move turnaround, audio spec, and spec URLs to auto-save on blur per §1.2.
Acceptance: Viryl's live catalog renders identically in data terms before and after — same rungs, same prices, same Quote chips, same dashes — with only layout and control styling changed. Verify at 1440, 1920, and ultrawide viewports that the card stops at its ceiling and inputs never exceed their caps. Then merge to main promptly; don't leave the task in Draft.