Otis — good work on the trash can, the portal auth fix, and the review pass. But before anything else: do not publish. We're in the Shopify review window and everything holds until I explicitly say go.
Also, the toggle rewording and price-field helper text are getting superseded — I'm simplifying the model. There is no such thing as a digital sale without a GoodDeed. Every digital access purchase always includes the GoodTunes unlock and a numbered PDF GoodDeed. It's intrinsic, not optional. So:
Remove the "Also include the GoodTunes digital unlock + GoodDeed for buyers" toggle entirely. Minting is implied by the mapping existing. Replace with static text under the variant selector: "Buyers get GoodTunes digital access and a numbered GoodDeed (PDF)."
The only checkbox is "Also bundle a printed & signed GoodDeed certificate." Off by default. Helper text: "Adds a physical, signed certificate on top of the PDF GoodDeed every buyer receives."
Remove the cert price field entirely. Signed-cert billing gets computed after the pre-order window closes from the wholesale tiered ladder — don't build the calculation yet (ladder floor is still an open decision on my side), just remove the field.
Migration: any existing mapping with the unlock toggle OFF is treated as ON going forward — that state was a bug. Preserve each mapping's signed-cert state. Log which mappings had it OFF so I can review affected orders.
Backfill: orders #1004 and #1005 on goodtunes-test.myshopify.com (fan billdenk+niinasoleil@gmail.com) hit the $65 variant while its mapping had the unlock off, so they never provisioned. Provision digital access for both, write the $app:goodtunes metafield, and confirm the Order status banner leaves the pending state and the album shows in that fan's library.
Verify the orders/paid webhook subscription is registered on the current install of goodtunes-test — the store was reconnected via OAuth recently and I want it confirmed.
Small fix in the same area: the purchase.thank-you extension target renders blank when the metafield is absent; the order-status target shows a "being prepared" pending state. Make the thank-you target render the same pending state.
All workspace-only. This all ships together — the crash fix, portal fix, form model change — in one publish when I give the word. Task #2843 stays on hold.