Otis — #2925 approved as written, with one addition that changes the verify step.
The fail-closed operator gate over a role hide-list is the right call, and finding why the current visibility logic missed the reviewer's role is the part that matters most — don't skip it even if the gate fixes the symptom first.
Before you build the verify step, confirm the provenance of the reviewer's test environment: the "GoodTunes Sampler" release the reviewer ("AR") used — was it provisioned as a GoodTunes Direct release or a GoodTunes-for-Shopify one? Check whether it has any Shopify wiring: store connection, product mappings, a Shopify tab in its album view. Report what you find.
If it's a Direct release (or has no Shopify wiring): the reviewer evaluated our Shopify app from inside an account that never exposes the Shopify integration — which likely steered them into the operator-only surfaces they flagged. Create a proper GoodTunes-for-Shopify review environment: a fresh partner-scoped account with a release that is Shopify-wired end to end — connected to a dev store (or ready to connect via the portal's own connect flow), mappable products, the Shopify tab present in its album view. This becomes the environment we hand Shopify in Test Instructions, so build it the way a reviewer will use it: log in, reach the Shopify integration within one click.
Then run the #2925 verify sweep as that account, not just any existing well-configured artist: every album-view and person-view tab, every panel, zero role-based error states or dead control walls, Shopify tab present and functional. Screenshots from that session, plus the operator-side screenshots per your step 4.
Also tell me the sampler account's current credentials/state — if it's what our existing Test Instructions point reviewers at, I need to rewrite that section before resubmitting.
Publish when verified. The submission is withdrawn and the screencast link is updated, so this is the last gate before resubmission. #2843 untouched, single session.