Otis — walk-through update. Login works at /admin/login and the shell looks right: confirmed Partners → NPOs is artist-scoped (0 entries), sidebar reads as the artist view. But I found a hard blocker plus two smaller items on the review path, and I need diagnosis before fixes.
HARD BLOCKER — store connect is unreachable for the review account. On Storefront Sessions → Shopify tab, the "Connect a store" step links to /admin/shopify. As shopifyreview, clicking it renders the operator Shopify page for about a second — I saw "Connect a label's Shopify store," the Store owner people-search, GDPR data requests, Connected stores, and a sidebar with GoodDeed pricing and Publishing — then redirects me to /admin/dashboard. So the reviewer following your step 1 ("follow the link to the portal's Shopify section → enter dev store domain → Install directly") gets evicted and can never connect a store. Nothing after step 1 can happen.
Two questions before you build anything: (a) Where is the artist's own store-connect flow supposed to live? Either a self-scoped connect surface exists and the Shopify tab should link there, or it doesn't exist and we need to build it — tell me which. (b) The one-second flash is a render-then-redirect leak: operator UI paints before the gate fires. Same class as the render-then-403 sweep — it needs the same fail-closed treatment, gate before render, not after. Sweep for other routes with this pattern while you're in there.
Storefront Sessions shows 0 tracks. Your spec said 2-track EP. The reviewer verifies playback, bonus content, and the certificate in the player at step 7 — with zero tracks that step is unverifiable. Load the two tracks and whatever bonus content the player should show.
Copy fix: the Shopify tab says "Connect the customer's Shopify store" in the artist view. Should be "your Shopify store" — role-aware per #2918.
Report the diagnosis on 1(a) before implementing, since link-fix vs. missing-feature are very different sizes. All fixes publish before I resume the walk-through. #2843 stays untouched. Single session.
One more for the same batch, low effort: once everything above is verified, rotate the review account password to something with no 0/O/1/l/I characters anywhere — I mis-transcribed the current one twice tonight and a Shopify reviewer gets one attempt's worth of patience. Tell me the new one out-of-band like last time.